# 차 한 잔의 시간 · Time for Tea — Roadmap

> 기준일: 2026-08-29
> 문서 역할: `DESIGN.md`에서 분리한 남은 작업과 우선순위 목록. 제품·디자인의 단일 기준은 `DESIGN.md`를 따른다.
> 상세 설계: `[이번 차의 느낌]`은 `docs/design-tea-feeling.md`, 단어장은 `docs/sensory-vocabulary.md`, 사용자 데이터·백업은 `docs/data-portability.md`에서 관리한다.

---

## 방향

`차 한 잔의 시간`의 가장 중요한 일은 **차를 마시는 것**이다. 기능을 늘리는 것보다 차 한 잔을 방해하지 않는 것을 우선한다.

로드맵에서 기능을 추가할 때는 다음 기준을 지킨다.

- 핵심 타이머 경험을 훼손하지 않는다.
- 아무것도 몰라도 차를 시작할 수 있게 한다.
- 기록은 항상 선택 사항이다. 기록하지 않은 차도 온전한 차 한 잔이다.
- 사용자가 이미 한 행동에서 얻은 정보는 다시 입력시키지 않는다.
- 사용 경험이 충분히 쌓였을 때 의미가 생기는 기능은 처음부터 노출하지 않는다.
- 서버·계정·중앙 DB 없이 유지 가능한 구조를 우선한다.
- 전문 관리 기능은 다른 앱이나 파일 기반 도구로 넘길 수 있게 하고, 이 앱이 모두 구현하려 하지 않는다.
- 새 기능은 "실제로 차를 마시며 반복해서 겪는 작은 장벽을 해결하는가?"를 먼저 묻는다.

### 기능 판단 질문

새 기능을 추가하기 전에 확인한다.

1. 차 한 잔을 더 즐겁게 만드는가?
2. 실제 차를 마시는 행동에서 나온 요구인가?
3. 초보자의 진입 장벽을 높이지 않는가?
4. 기록을 의무나 목표로 만들지 않는가?
5. 앱보다 사용자의 경험을 앞세우는가?
6. 현재 화면의 조용함과 시각 언어를 해치지 않는가?
7. 혼자 장기 유지할 수 있는 복잡도인가?

여러 항목에 `아니오`라면 구현하지 않는다.

---

## Phase A — 핵심 타이머 완성

현재 타이머의 가장 독특한 가치인 **준비시키는 타이머**를 먼저 안정화한다.

핵심 흐름:

```text
관찰
김 · 찻잎 · 탕색 · 저대비 링
   ↓
준비
예고음 · 금빛 강조 · 동심원 · 3~5초 카운트다운
   ↓
행동
0초 · 완료음 · 플래시 · 실제 출탕/분리
```

### 우선순위

| 우선순위 | 항목 | 난이도 | 설명 |
|---:|---|---|---|
| 1 | 백그라운드 완료 신호 예약 | 중간 | 절대 종료 시각 보정과 iOS 오디오 컨텍스트 복구는 완료. 백그라운드에서도 완료 신호를 놓치지 않게 WebAudio clock 예약 또는 Worker tick 검토 |
| 2 | 우림 중 즉시 출탕 | 낮음 | 차 상태를 보고 일찍 따르고 싶을 때 바로 실제 완료 행동으로 전환할 수 있게 한다. 숨은 long-press에만 의존하지 않는다 |
| 3 | 차 캐러셀 스와이프 | 낮음 | 이름보다 잎 모양으로 고르는 흐름 강화. 버튼 접근성은 유지 |
| 4 | 개완 뚜껑 열림/출탕 표현 | 중간 | 실제 개완 완료 행동과 화면 상태를 더 가깝게 맞춘다 |
| 5 | 폰트 셀프호스팅 | 중간 | Gowun Batang, Space Mono를 로컬 자산으로 두어 완전 오프라인에 가까워지게 한다 |
| 6 | 회차별 탕색 점감 | 중간 | 포가 진행되며 차가 변해가는 느낌을 시각적으로 이어준다 |
| 7 | 도구 전환 전이 | 낮음 | 단순 화면 교체보다 같은 차 자리에서 다구가 바뀌는 느낌을 유지한다 |
| 8 | `[이번 차의 느낌]` 기록 MVP | 중간 | 타이머가 안정된 뒤 선택적으로 여는 가장 얇은 기록의 문. 상세 설계는 `docs/design-tea-feeling.md` |

### 다구/우림법 확장 원칙

새 다구는 단순히 모양이 다르다는 이유만으로 추가하지 않는다.

**우림 행동 또는 찻잎과 찻물의 분리 방식이 다른가**를 우선 본다.

현재/향후 개념 예:

```text
pour   → 개완 / 다호 / 티팟: 찻물을 따라 분리

drain  → 표일배: 찻물을 아래로 배수해 분리

lift   → 티백 / 바스켓 스트레이너: 찻잎 쪽을 들어 올려 분리

none   → 잎을 그대로 두는 머그: 별도 분리 동작 없음
```

머그에 거름망을 쓰는 방식처럼 하나의 다구가 여러 brewing/separation method를 가질 수 있다. 구현 시 `vessel`과 `brewing method`를 무조건 같은 개념으로 묶지 않는다.

---

## Phase B — 기록 기반

타이머 사용 과정에서 자연스럽게 생긴 정보를 사용자 자산으로 전환한다.

### B1. 내부 데이터 정리

- 차·다구·감각 태그의 canonical ID와 표시 문자열 분리
- 다국어 전제를 고려한 데이터 구조
- Tea Session 데이터 모델 초안
- 앱이 관찰한 값 / 사용자가 입력한 값 / 추천값 구분

### B2. IndexedDB 영속 저장

장기 사용자 자산은 IndexedDB를 기본 후보로 한다.

- sessions
- vocabulary
- recipes (later)

설정값은 기존 localStorage와 분리한다.

### B3. `[이번 차의 느낌]`

앱이 이미 아는 것은 자동으로 넣는다.

- 차
- 다구
- 날짜
- 포수
- 포별 실제 우림 시간

사용자가 추가하는 것은 앱이 알 수 없는 감각이다.

- 기본 감각 태그
- 자유 메모
- 선택: 찻잎 g
- 선택: 물 ml
- 선택: 실제 수온

타이머를 썼다는 이유만으로 모든 세션을 장기 저장하지 않는다.

> 타이머 사용 ≠ 기록 의사

느낌 입력을 시작했다면 작성 중 draft는 자동 보존해 실수로 날아가지 않게 하고, 장기 기록은 사용자의 명시적 의사로 확정하는 방향을 우선 검토한다.

---

## Phase C — 데이터 이동권

상세 원칙은 `docs/data-portability.md`를 따른다.

- 전체 JSON 백업/복원
- Tea Session JSON / Markdown export
- 개인 감각 단어장 JSON / Markdown export
- 공개 Tea Session Schema v1 초안
- 공개 Sensory Vocabulary Schema v1 초안
- 사용자 데이터와 앱 기본 데이터 분리

스키마는 앱 내부 UI 상태가 아니라 차 한 잔의 경험을 표현해야 한다.

실제 JSON Schema 파일은 데이터 모델이 충분히 안정된 뒤 만든다.

---

## Phase D — 다국어

목표 언어:

- 한국어
- 영어
- 중국어 번체
- 중국어 간체
- 일본어

### 원칙

- 핵심 우림 흐름은 언어를 읽지 않아도 이해 가능하게 유지한다.
- 번역해야 하는 텍스트 자체를 줄인다.
- 내부 canonical ID와 표시 문자열을 분리한다.
- 차 문화 용어는 단순 직역보다 실제 사용 표현을 우선한다.
- 개인 단어와 메모는 사용자가 쓴 원문을 보존한다.

다국어는 새로운 디자인 철학이 아니라, 처음부터 지향했던 **말이 많이 필요 없는 타이머**의 자연스러운 확장으로 본다.

---

## Phase E — 향과 맛의 단어장 📗

상세 설계는 `docs/sensory-vocabulary.md`에서 관리한다.

핵심 구조:

```text
공식 풍미륜의 큰 갈래
        ↓
생활 속 구체적인 예
        ↓
개발자의 취향 언어 몇 개
        ↓
사용자의 자기 표현
        ↓
나의 단어장
```

### 목표

- `뭐라고 표현하지?`라는 첫 장벽을 낮춘다.
- 전문적인 정답을 맞혀야 한다는 압박을 줄인다.
- 사과, 복숭아, 우유, 미역, 표고버섯, 약수처럼 실제 감각 기억을 불러온다.
- `아세로라껌`, `자판기 우유`, `드봉 비누`, `더블비얀코 아래쪽 흰 부분` 같은 소량의 생활 언어로 "이렇게 표현해도 된다"는 규칙을 보여준다.
- 사용자가 결국 단어장에 없는 자기 표현을 만들게 한다.

단어장은 차 지식 백과가 아니다. 다음 차와 일상의 음식·꽃·풀·나무·흙을 조금 더 자세히 보고 싶게 만드는 작은 감각 책을 지향한다.

---

## Phase F — 차 열 잔의 기록 🍵

열 잔은 목표가 아니다.

기록이 어느새 열 잔 쌓였을 때 처음으로 회고가 의미를 갖기 시작하는 **발견 시점**이다.

### 노출 방식

- 첫 기록이 저장되면 아이콘 메뉴바에 작은 찻잔 아이콘과 숫자 배지를 보여줄 수 있다.
- `7/10`, 진행률 바, streak, "3잔 남았어요" 같은 목표 표현은 쓰지 않는다.
- 10번째 기록이 생긴 순간 조용히 `차 열 잔의 기록`이 열린다.

예:

> 어느새 열 잔의 기록이 모였어요.

### 보여줄 것

- 자주 선택한 감각어
- 자주 사용한 다구
- 첫 기록과 최근 기록의 표현
- 사용자가 남긴 기억에 남는 문장
- 새로 등장한 개인 단어

### 보여주지 않을 것

- 취향 점수
- 사용자 등급
- AI 성격/취향 판정
- "당신은 Floral Lover입니다" 같은 단정

해석 대신 사실을 보여준다.

> `열 잔 중 여섯 잔에서 꽃향을 골랐어요.`

사용자가 스스로 자기 취향과 변화를 발견하게 한다.

---

## Later / Experiment — 나의 레시피북 📖

충분히 차를 마신 사용자는 앱의 권장값보다 자기 방식이 더 중요해진다.

이때 필요한 고급 기능은 전문 관리가 아니라 **반복되는 자기 방식을 덜 귀찮게 불러오는 것**이다.

예:

```text
문산포종 · 개완
1泡 20초
2泡 25초
3泡 35초
4泡 45초

[이 우림 기억하기]
```

다음 세션:

```text
지난번 저장한 우림
20 / 25 / 35 / 45초

[이대로 우리기]
```

`50잔` 같은 해금 기준은 현재 제품 가설일 뿐 확정하지 않는다. 실제 사용 경험을 보고 결정한다.

레시피북의 목적은 전문적인 차 관리가 아니다.

> 지난번에 맛있게 마신 나를 복원한다.

---

## 명시적 비목표

이 프로젝트는 다음 영역으로 커지지 않는 것을 기본값으로 한다.

- 필수 계정
- 클라우드 동기화를 기본 책임으로 삼기
- 차 재고 관리
- 구매처·가격 관리
- 전문 시음 데이터베이스
- 커뮤니티/SNS
- streak, 목표, 레벨, 업적
- AI 취향 판정
- 사용자를 앱 안에 붙잡기 위한 푸시 알림
- 거대한 차 백과사전

전문적으로 관리하고 싶은 사용자는 더 적합한 앱이나 노트 도구로 이동할 수 있어야 한다. 데이터 이동권을 제공하는 이유도 여기에 있다.

`차 한 잔의 시간`은 **작아서 부족한 앱이 아니라, 작아야 완성되는 앱**을 지향한다.

---

## 완료된 핵심 항목

| 완료일 | 항목 | 구현 내역 |
|---|---|---|
| 2026-08-01 | 모바일 발열 억제 | 상태 기반 단일 스케줄러, 진행 30fps/완료 대기 12fps 예산, idle 정지, 다구 이미지 on-demand 연결 |
| 2026-07-17 | iOS 오디오 인터럽션 복구 | `AudioContext`의 non-running 상태 복구, `closed` 재생성, 앱 복귀·다음 포·재생 직전 복구 |
| 2026-07-12 | `[이번 차의 느낌]` 선행 진입점 | 메인 푸터 우측 연필 라인 아이콘과 `준비 중` 모달 추가, 제작자 반응은 도움말로 이동 |
| 2026-07-12 | 반복 사용 상태 영속 | 차·다구·차별 권장 대비 시간 보정·음소거 저장/복원, 권장 지점 눈금 |
| 2026-06-28 | 우림 시각 표현 보강 | 차종별 젖은 잎 형태, 실제 다구 구조에 맞춘 김 배출, 유리 다구 idle 찻잎 표시 |
| 2026-06-28 | 표일배 물리 구조 교정 | 윗 챔버 거름망 안에서 우러나고 아래 잔으로 배수되는 위치 관계 보정 |

---

## 점검 메모

- 차 캐러셀 스와이프는 버튼 접근성과 충돌하지 않아야 한다.
- 백그라운드 완료 신호는 실제 iOS Safari/PWA에서 앱 전환·화면 잠금 조건을 검증한다.
- 우림 중 즉시 출탕은 숨은 제스처에만 의존하지 않는다.
- 새 다구를 추가할 때 모양보다 실제 우림/분리 행동을 먼저 모델링한다.
- `[이번 차의 느낌]`은 실제 완료 행동보다 먼저 튀어나오지 않는다. 먼저 따르고 마신 뒤, 원하면 기록한다.
- 단어장과 기록 기능이 커져도 첫 화면의 핵심 타이머 밀도를 높이지 않는다.
- 사용자 자산 스키마를 바꿀 때는 export/import 마이그레이션을 먼저 고려한다.
