# 차 한 잔의 시간 · 변경 이력

> 정리일: 2026-08-31
> 구현 기준: TW-01–03·TW-05 + [TW-05 한국어 기반·UI 마감](#tw-05-close)

이 문서는 당시 변경 내용과 구현 범위를 보존하는 개발 이력입니다. 현재 구현 상태와 현재 스펙은 [ROADMAP.md](ROADMAP.md) 및 [DESIGN.md](DESIGN.md)를 기준으로 하며, ROADMAP에는 이 문서의 해당 항목 링크만 둡니다. 상세 설계는 [design-tea-feeling.md](design-tea-feeling.md), [sensory-vocabulary.md](sensory-vocabulary.md), [data-portability.md](data-portability.md)에서 관리합니다.

기존 로드맵의 주요 완료 내역을 옮긴 문서이며 전체 Git 이력을 나열한 것은 아닙니다. 완료일은 기존 로드맵 기준으로 보존하므로 관련 커밋의 작성일과 다를 수 있습니다.

## 기능 변경

<a id="tw-05-close"></a>

### 2026-08-31 — TW-05 한국어 공통 기반·UI 마감

사용자가 한국어 실제 화면 검토와 추가 수정을 마친 뒤 TW-05 마감을 확인했다. 공통 번역·언어/태그 호환, 언어 선택·메뉴·공지 읽기, 기본 15개와 설정 배치, 공통 UI 기준, 이용 안내·여운 명칭까지 기능 범위를 완료했다. 개별 구현·검증 이력은 아래에 보존한다. 기능 브랜치 `codex/tw-05-localization-foundation`의 `7c7cf5b`부터 `0837981`까지 포함하며, 별도 작업에서 확정한 감각 단어 조사 문서 `9b20bb8`도 출처 그대로 유지한다.

- 기능 브랜치에서 Node 37개, 현지화 49개, 공지 11개, 공통 UI 60개를 통과했다. 이전 접근성·초안·비동기 세션 회귀와 실제 PNG·글꼴 사전 렌더·로컬 캐시 검증 기록도 아래에 있다.
- 통합 상태: 완료 문서를 기능 브랜치에서 정리했으며 `codex/tw-readiness` 병합과 통합 후 회귀는 아직 수행 전이다. 실제 결과 확인 후 이 항목을 갱신한다.
- TW-04는 보류, 다음 항목 TW-06은 미착수다. 번체 전체 문구는 아직 한국어 폴백이며 TW-07·08의 긴 혼합 입력·실기기 글꼴·실제 공유·오디오/PWA·기존 설치 캐시 갱신·오프라인·첫 배포 공지 검증은 남는다. 네이티브 라디오 방향키·details Enter/Space, OS IME·스크린리더 실기기 확인도 최종 접근성 검증에 남긴다. `main` 반영·원격 푸시·배포는 하지 않는다.

### 2026-08-31 — 이용 안내·여운 명칭 정리

상위 메뉴와 `help.html`의 페이지/문서 제목을 **이용 안내**로 바꾸고, 하위 접힘 항목 **사용법**은 유지했다. 설정의 링크 접근성 이름과 안내 본문의 목적지 이름도 맞췄다. 기본 15개 중 **긴 여운 → 여운**은 길이 판단을 미리 요구하지 않으려는 사용자 결정이며 `finish.lingering` ID와 저장 목록은 유지한다. 직접 입력한 `긴 여운`·legacy 원문은 치환하지 않는다. [메뉴 기준](design-navigation.md#2-전역-메뉴와-맥락-진입)과 [단어장 기준](sensory-vocabulary.md#default-vocabulary)에 반영하고 과거 조사·변경 이력은 보존했다.

- Node 기존 검증 37개, 공통 UI 검증 320×660·375×660·768×900의 60개 통과. builtin 새 이름과 custom 원문 유지·상하위 제목 구분을 확인했다. 정적 번역 키 189개·JS 구문·diff를 확인하고 메뉴/이용 안내/단어장 실제 375px 스크린샷을 남겼다.
- URL·번역 키·저장 스키마·스타일은 변경하지 않았다. 기존 shell 자산만 수정하므로 배포 전 준비 중인 SW v62를 유지한다. 실제 모바일/PWA·오프라인·PNG 출력은 이번 명칭 수정에서 재검증하지 않았고 통합·원격 푸시·배포는 수행하지 않았다.

### 2026-08-31 — 공통 UI 기준과 화면 간 메뉴 일관성

[공통 UI 기준](ui-guidelines.md)과 `styles/ui.css`를 추가했다. 메인·사용법·설정의 상하단 조작을 .94rem/21px, 아이콘 19px, 최소 44px 영역으로 통일하고 460px 프레임 안의 상단·좌우·하단 기준선을 공유한다. 하위 페이지 제목·구역 제목·본문·보조 문구·각주를 역할별 토큰으로 정의하고 사용법/설정의 중복 스타일을 추출했다. 접힘 제목의 문구 자체가 가운데 오도록 화살표 공간을 양쪽에 예약했다. 넓은 화면의 박수 버튼도 프레임 안쪽에 둔다. 타이머 숫자·차/다구·시음 카드 PNG와 13.6px 단어장 칩/32px 배경은 별도 기준으로 유지했다.

- `tests/ui-consistency.html`: 320×660·375×660·768×900에서 세 화면의 글자/행 높이/아이콘/44px 영역·메뉴 기준선·제목 중심·계층·가로 넘침·기존 설정 배치 60개 통과. 320/375 실제 화면 스크린샷도 확인했다.
- `tests/localization.html` 49개, `tests/announcements.html` 11개, `node --test tests/*.test.cjs` 37개 통과. 타이머·초안·언어·공유 경로·공지 동작을 재확인했다. JS 구문·정적 번역 키 189개·전체 diff를 확인했다.
- 새 CSS를 SW shell 자산에 포함했고 `tests/offline-assets.html`에서 CSS를 포함한 11개 캐시 응답을 확인했다. 배포 전 준비 중인 v62를 유지한다. 이번에는 서버 중단 오프라인 재실행·실제 모바일/PWA·스크린리더를 재검증하지 않았다. 박수 전송·통합·원격 푸시·배포는 수행하지 않았다.

### 2026-08-31 — 단어장 칩 음영 축소·텍스트 출력 방침 기록

설정 단어장의 보이는 음영 배경을 삭제 버튼의 터치 영역과 분리했다. 한 줄 칩의 배경 높이는 52→32px, 전체 행 높이는 52→44px로 줄였고 `#식물향` 배경 폭은 약 117→94px다. 글자 13.6px·음영 색은 유지하며 삭제 버튼은 44×44px다. 긴 원문은 라벨 안에서 줄바꿈하고 배경도 높이에 맞춰 늘어난다. `#긴 여운`처럼 공백을 유지하는 일반 텍스트 복사와 ID/사용자 원문 저장의 구분을 [출력 방침](design-tea-feeling.md#52-두-가지-출력)에 기록했으며, 텍스트·PNG 출력이나 저장 스키마는 변경하지 않았다.

320×660·375×660 실제 렌더/스크린샷과 DOM에서 음영 크기·44px 삭제 영역·가로 넘침 없음을 확인했다. 전용 8134에서 공백 있는 사용자 단어의 추가·내 단어 표시·삭제·삭제 후 포커스 복귀를 확인하고 추가한 검증 단어는 제거했다. JS 구문·정적 번역 키 189개·diff 확인을 통과했다. 작은 스타일 수정으로 새 자동 테스트는 추가하지 않았으며 실제 모바일/PWA는 미검증이다. 기능 브랜치 로컬 변경이며 배포하지 않았다.

### 2026-08-31 — 설정 배치·언어 라디오·기본 감각 단어 15종

주의사항을 접히지 않는 첫 구역으로 옮기고, 바로 다음 언어 구역은 기본 펼침으로 바꿨다. 두 언어는 가로형 네이티브 라디오와 44px 라벨 영역으로 선택하며 기존 카드 여백을 줄였다. 차 느낌 단어장은 [확정 목록과 전환 계약](sensory-vocabulary.md#default-vocabulary)의 기본 15개를 제공한다. 이전 14개 ID·이름도 지원하며 저장된 builtin/custom/legacy/빈 목록은 교체하지 않는다. 태그 필드가 없거나 확인 후 기본 복원을 실행한 경우만 새 목록을 사용한다. 단어장 탐색·번체 번역·배포는 이번 범위가 아니다.

설정의 조합 이벤트를 `addEventListener`로 연결해 브라우저에서 유예가 실제 작동하도록 수정했다. 조합 중 라디오 선택은 기존 언어 체크를 유지하고 종료 후 적용한다. 저장 실패 시 이번 화면의 언어·라디오와 실패 안내를 유지하고 재진입 시 마지막 저장 언어로 돌아간다.

- `node --test tests/tags.test.cjs tests/preferences.test.cjs tests/i18n.test.cjs tests/announcements.test.cjs`: 37개 통과. 새 기본 목록·실제 한국어 카탈로그·이전 ID/라벨·목록 원문·빈 배열·명시 복원·저장 실패를 검증했다.
- 전용 8134의 `tests/localization.html`: 49개 통과. 설정 순서·기본 펼침·단일 선택·재진입·합성 IME·저장 실패와 기존 타이머/초안/공유 회귀를 확인했다. 320×660·375×660 실제 렌더와 스크린샷에서 언어 수평 정렬·44px 라벨·가로 넘침 없음·기본 15개 표시를 확인했다. 언어 구역은 기본 한국어 상태에서 높이 88px이다.
- JS 구문·정적 번역 키 189개·전체 diff를 확인했다. OS IME·모바일/PWA·스크린리더는 미검증이다. 설정의 네이티브 방향키 선택은 자동 키 주입으로 확정하지 못했으며 실제 키보드 확인을 남긴다. 회귀 fixture의 방향키 통과 항목은 메인 언어 popover 검증이다. 새 자산은 없고 배포 전 준비한 SW v62를 유지한다. 로컬 기능 브랜치 변경이며 통합·배포하지 않았다.

### 2026-08-31 — 지난 업데이트 조건부 노출·배포 묶음 자동 분류

`지난 업데이트`를 업데이트·예정된 업데이트와 같은 단계/금색 제목/접는 불릿 목록으로 추가했다. 과거 하위 항목이 없으면 제목과 목록 모두 생성하지 않는다. 정적 공지의 `releases`를 최신 배포 묶음 순서로 관리하고 첫 유효 묶음은 현재, 나머지는 과거로 분류한다. 새 묶음을 앞에 추가하면 기존 현재 항목이 자동으로 과거에 표시되며 날짜·예정 문구 수정이나 빈 배포 초안만으로는 이동하지 않는다. 현재 실제 데이터는 예정 공지만 유지했고 예시 배포는 게시하지 않았다.

`node --test tests/announcements.test.cjs tests/i18n.test.cjs` 8개 통과. 전용 8134의 `tests/announcements.html`은 실제 앱에 검증용 데이터만 주입해 배포 없음→첫 배포→다음 배포, 과거 제목 숨김, 현재/과거 분리, 동일 단계·스타일, 과거 항목 펼침/접힘, 예정 안내 미노출 11개를 통과했다. 375×660 iframe 렌더와 실제 8135 기본 공지의 빈 과거 섹션 숨김, JS 구문·번역 키·diff를 확인했다. 검증용 데이터는 브라우저 설정이나 실제 공지 파일에 저장하지 않았다. 기능 브랜치 로컬 변경이며 배포하지 않았다.

### 2026-08-31 — 공지 소제목·접는 불릿 목록

사용자 요청으로 `업데이트`·`예정된 업데이트`를 페이지 제목(1.18rem)보다 작은 금색 소제목(1.04rem)으로 바꿨다. 항목은 실제 ul/li 불릿 목록과 details/summary로 구성하고, 제목·본문을 같은 .9rem 본문색으로 왼쪽 정렬했다. 항목은 44px 이상 조작 영역과 펼침/접힘 표시를 유지하며 모달 Tab 순환에 포함했다. 예정 상태 안내는 예정 항목에만 표시한다. 375×660·320×660 렌더에서 색·글자 크기·줄바꿈·가로 넘침 없음, 클릭 펼침/접기와 Tab 진입, JS 구문·번역 키·diff를 확인했다. 네이티브 Enter/Space 활성화는 자동 키 주입으로 확정하지 못했으며 실제 키보드 확인은 남긴다. 배포하지 않았다.

### 2026-08-31 — 공지 창 제목에 갱신일 통합

사용자 요청으로 공지 창 제목을 `공지사항 (2026.08.31)` 형식으로 바꾸고 아래의 중복된 `공지 업데이트` 줄을 없앴다. 날짜는 기존 `updatedOn`에서 읽고 번역 키의 치환 변수로 표시하며, 유효하지 않으면 제목만 표시한다. 메뉴의 MMDD 위첨자·전체 날짜 접근성 설명은 유지한다. 320×660·375×660 렌더에서 한 줄 제목, 닫기 버튼과 겹침 없음, 이전 날짜 줄 제거·dialog 접근성 이름과 JS 구문·번역 키·diff를 확인했다. 배포하지 않았다.

### 2026-08-31 — 상단 세 버튼의 글자 높이 통일

사용자 화면 검토에서 우림 끝·언어 선택·메뉴의 수직 정렬을 확인했다. 버튼 영역은 모두 높이 44px·중심 Y 32.390625px로 같았지만 우림 끝의 글꼴만 15.68px(글자 영역 23px)로 다른 버튼의 15.04px(21px)보다 컸다. 세 버튼을 `.94rem`·줄 높이 21px로 통일하고 우측 도구 묶음의 가운데 정렬을 명시했다. 375×660·320×660 실제 렌더에서 세 글자의 중심과 버튼/아이콘 중심 일치, 44px 영역·가로 넘침 없음을 확인했다. 타이머·저장·언어 동작은 변경하지 않았고 TW-05 검토 브랜치에만 적용했다.

<a id="tw-05-localization"></a>

### 2026-08-31 — TW-05 한국어 번역 기반·언어/메뉴/공지 검토본

기능 브랜치 `codex/tw-05-localization-foundation`에서 한국어 공통 기반과 실제 UI를 구현·로컬 검증했다. 사용자와 합의한 **한국어 실제 화면 검토 → TW-06 번체 문구/화면 검토** 순서를 유지한다. 한국어 화면 승인 전이므로 TW-05 전체 완료·통합 병합·번체 지원 완료·배포 준비 완료로 표시하지 않는다.

메인 우상단에 언어 선택과 공지사항·사용법·설정 메뉴를 연결했다. 공지는 같은 문서에서 읽고, 읽음 여부와 무관한 갱신일 `0831` 및 전체 날짜를 표시한다. 첫 내용은 배포 완료가 아닌 예정 초안이다. 사용법·설정의 접힘 제목을 테두리 없이 가운데 정렬했고, 메인 이미지·중앙 타이머·푸터의 역할을 유지했다. 조작 아이콘은 선형 SVG, 임시저장은 문구 중심으로 맞췄다.

`TeaI18n` 공통 조회와 한국어 카탈로그를 정적/동적 UI·접근성·텍스트/PNG 공유에 적용했다. 사용자 원문과 차·다구 ID는 유지한다. 직접 언어 전환은 우림을 유지하고 IME/공유 작업 중에는 적용을 유예하며, 외부 탭 변경은 안전 idle까지 기다린다. 검토에서 발견한 마지막 노트·태그 해제 후 대기 언어 미적용과, 다른 탭의 시간 전용 저장이 IME/PNG 대기 중 직접 선택한 언어를 덮는 문제를 수정했다. 공유는 시작 시의 언어·입력·기록을 복사해 비동기 생성 중 전환으로 섞이지 않게 했다.

기본 태그 14종은 ID, 기존 문자열과 새 사용자 단어는 custom 원문으로 구분한다. 같은 표시의 builtin/custom을 독립 선택·삭제하며, 손상된 tagItems는 관련 없는 저장으로 덮어쓰지 않고 명시 복원만 허용한다. SW `v62`에 공용 JS·카탈로그·공지를 포함했다. 상세 계약은 [다국어 설계](design-localization.md)에 둔다.

검증:

- `node --test tests/preferences.test.cjs tests/tags.test.cjs tests/i18n.test.cjs`: 29개 통과. 기존/신규/빈/손상 태그, 원문/미지 필드 보존, 언어 patch, 한국어 폴백·치환·선택 알림을 확인했다.
- 전용 서버 `python3 -m http.server 8134 --bind 127.0.0.1`의 `tests/localization.html`: 38개 통과. 합성 번체 카탈로그로 우림 중 명시 전환, 다른 탭 유예·초안 해제/종료 후 적용, IME 대기, radio 키보드·Escape·공지 modal/inert, 이동 보호, 느린 실제 PNG 생성과 태그 복원을 확인했다. 테스트 대역은 실제 번체 번역·OS 입력/공유 검증이 아니다.
- 기존 `tests/accessibility.html` 35개, `tests/draft-boundaries.html` 66개, `tests/session-async.html` 9개 통과. 실제 앱 5초 우림·단일 완료→출탕→다음 포, 초안·포커스 보존과 플랫폼 지연 대역 회귀를 확인했다.
- `tests/font-preflight.html`: 한글·번체·emoji 혼합 제목/태그/메모로 정상 및 외부 스타일/글꼴 차단 두 모드의 실제 680×900 PNG를 생성·시각 확인했다. 새 원격 글꼴 의존성은 없다. 긴 입력 조판·기기별 폴백은 TW-07에 남긴다.
- 375×660·320×660 메인/언어/메뉴/설정/사용법을 렌더해 가로 넘침과 상단·주요 버튼의 44px 영역을 확인했다. 원본 PNG, DOM 측정과 보조 로컬 이미지 리뷰를 함께 사용하며 보조 모델의 OCR·상태 추정은 사실 판정에 쓰지 않는다.
- `tests/offline-assets.html`: v62 셸의 문서3개·JS7개 응답 확인. 8134 서버를 실제 중지한 뒤 캐시된 메인→설정(언어 변경)→사용법에 재진입했다. 외부 인터넷 전체 차단이나 기존 설치 SW 업그레이드 검증은 아니다. 인라인/공용 JS 구문, 카탈로그 키, 문서 링크·diff도 확인했다.

로그·스크린샷·원본 PNG는 Git 외부 `/Users/kioku/.codex/visualizations/2026/08/31/01a05600-b3f5-75d1-bdac-77e2d1d2c5c1/tw05`에 보관했다. 사용자 검토용 주소는 테스트 데이터와 분리한 `http://127.0.0.1:8135/`다. TW-06 번체 전체 문구, TW-07 긴 입력·실기기 글꼴, TW-08 모바일 브라우저/PWA의 오디오·실제 공유·캐시 업데이트/오프라인과 첫 배포 공지 일치는 미완료다. 원격 푸시·main 반영·배포는 하지 않았다.

<a id="tw-03-draft-boundaries"></a>

### 2026-08-31 — TW-03 초안 보호·세션 종료·완료 0포

기능 브랜치 `codex/tw-03-draft-boundaries`의 구현·검증·설계 커밋 `b561579`를 `232b208`에서 `codex/tw-readiness`에 병합했다. 통합 후 초안 회귀 66개·접근성 회귀 35개·설정 테스트 16개를 다시 통과했으며 5초 우림→완료→출탕, 초안 유지·명시적 종료, 실제 PNG 출력에 오류가 없었다.

첫 우림 전부터 제목·태그·메모 중 내용이 있으면 차·다구 변경을 막는다. 변경 없는 제목과 공백만 있는 메모는 잠그지 않고 원문은 보존한다. 다구 자산을 비동기로 준비하는 동안 새 초안이 생겨도 완료 직전에 다시 검사해 기존 선택을 유지한다. [초안·종료 계약](design-tea-feeling.md#draft-policy)을 구현했다.

`우림 끝`은 초안이 있으면 취소에 기본 포커스를 둔 삭제 확인을 연다. 확인 중 타이머는 계속되고 취소·Escape·배경 클릭은 현재 상태를 유지한다. 승인하면 초안·완료 기록만 지우고 저장 설정은 유지한다. 초안이 없으면 즉시 종료한다. `임시저장`은 입력·IME 확정 뒤 시트를 닫으며 세션 한정 보관 안내를 추가했다. 현재 문서를 떠나는 사용법·설정 링크는 안전한 idle에서만 허용하고 새 탭 이동은 유지한다. 문구는 ‘-습니다’체로 통일했으며 도움말도 현재 동작에 맞췄다.

시트·텍스트·카드·PNG는 완료 전 `완료된 우림 없음`을 사용하며, 열린 시트/카드도 완료 시 입력을 건드리지 않고 기록을 갱신한다. 종료 시 출탕 콜백을 지우고 완료/출탕 상태를 재검사한다. 오디오와 Wake Lock의 지연 요청은 세대값으로 무효화해 종료 뒤 늦은 소리나 화면 잠금 유지가 살아나지 않도록 했다. SW 캐시는 `v61`로 준비했다.

검증:

- `node --test tests/preferences.test.cjs`: 기존 16개 통과. 변경된 HTML의 인라인 JS·SW 구문, 문서 링크·diff 확인.
- 전용 `http://127.0.0.1:8134`에서 `tests/draft-boundaries.html`: 66개 통과. 제목/태그/메모 단독 보호, 취소/중복 승인, 설정 원문 보존, 임시저장과 조합 확정, 이동 허용/차단/새 탭, 5초 우림과 확인창 중 완료, 전·중·후 텍스트/DOM/실제 680×900 PNG, 공유 취소·다운로드 폴백을 확인했다.
- `tests/session-async.html`: 9개 통과. 실제 앱 HTML에 테스트용 AudioContext·Wake Lock·Image 지연기를 주입해 종료 뒤 늦은 음원/화면 잠금 요청, 자산 준비 중 세 종류의 단독 초안, 출탕 중 종료를 확인했다. 초기 fixture의 오래된 0포 기대 문구를 실제 확정 문구로 바로잡은 뒤 재통과했다.
- `tests/accessibility.html`: 기존 35개 재통과. 별도 앱 탭에서 실제 키 입력의 Tab/Escape 복귀, 초안 선택/페이지 이동 차단, 375×660·320×660 확인창과 44px 버튼을 확인했다. 0포·진행 중·1포 PNG 원본과 렌더를 확인했으며 콘솔 오류는 없었다.

산출물은 Git 외부 `/Users/kioku/.codex/visualizations/2026/08/31/01a055b0-8e12-73e2-ba94-91ae80e61456`의 `tw03-*`에 보관했다. 테스트의 합성 IME·플랫폼 API 대역·파일 공유 대역과 실제 OS IME·모바일 오디오/Wake Lock·공유 시트·스크린리더·설치형 PWA는 구분하며 후자는 미검증이다. 초안의 새로고침 복원·장기 저장, 즉시 출탕(TW-04), 번체 글꼴/긴 내용 PNG 조판(TW-07)은 추가하지 않았다. 원격 푸시·main 반영·배포는 하지 않았다.

<a id="tw-02-accessibility"></a>

### 2026-08-31 — TW-02 차 느낌 시트·카드 접근성

기능 브랜치 `codex/tw-02-accessibility`의 구현·검증·설계 커밋 `9fae2fa`를 `6be8fd4`에서 `codex/tw-readiness`에 병합했다. 통합 후에도 실제 앱 회귀 35개와 설정 테스트 16개를 재통과했으며 5초 우림→완료→출탕·중첩 창 복귀·초안 유지·PNG 생성에 오류가 없었다.

시트와 카드에 이름 있는 dialog, 최상위 창의 Tab/Shift+Tab 순환, Escape 한 단계 닫기와 실제 진입 버튼 포커스 복귀를 적용했다. 닫힌 레이어는 hidden/inert, 열린 창 아래의 시트와 타이머는 inert로 조작을 막는다. 카드의 닫기 버튼을 공유·저장 버튼에서 분리했다. [접근성·입력 보존 계약](design-tea-feeling.md#modal-accessibility)을 기준으로 구현했다.

제목 편집의 Enter/blur 중복 확정을 막고 원문 공백·이모지를 보존한다. 조합 중 Enter/Escape는 IME에 맡기며 닫기·카드 열기는 compositionend의 최종 입력 뒤 처리한다. 닫기·편집·태그·공유의 최소 조작 크기를 44×44px로 보강하고 태그 aria-pressed, 입력 이름과 포커스 표시, 긴 내용의 내부 스크롤을 추가했다. 타이머는 모달 아래에서도 계속되며 완료가 입력 포커스를 빼앗지 않는다. 창 안의 상태 안내와 타이머 복귀 버튼으로 출탕 조작에 돌아갈 수 있다. SW 캐시는 `v60`으로 준비했다.

검증:

- `node --test tests/preferences.test.cjs`: 기존 설정 보존 16개 통과. 인라인 JavaScript·SW 구문, 문서 링크·diff 확인.
- 전용 서버 `python3 -m http.server 8134 --bind 127.0.0.1`에서 `tests/accessibility.html`: 실제 앱 iframe 회귀 35개 통과. 닫힌 창 접근 차단, 중첩 포커스·Escape·배경 닫기, 제목 원문·조합 최종 입력, 메모·태그 보존, 5초 우림→완료→복귀→출탕, 실제 680×900 PNG 생성·공유 취소 후 보존, 320px 긴 제목·메모·태그와 하단 조작 접근을 확인했다. 키와 IME는 합성 이벤트 테스트다.
- 별도 실제 앱 탭에서 브라우저 키 입력으로 제목 편집 후 Tab 이동, Tab/Shift+Tab 순환과 연속 Escape 복귀를 확인했다. 375×660 시트·카드 렌더, 44px 버튼, 완료 시 메모 포커스 유지와 출탕 복귀, 브라우저 오류 없음도 확인했다.
- 스크린샷·회귀 로그·PNG 원본은 Git 외부 작업 폴더 `/Users/kioku/.codex/visualizations/2026/08/31/01a055b0-8e12-73e2-ba94-91ae80e61456`의 `tw02-*`에 보관했다.

TW-02는 앞서 반영한 [공통 알림 C](#common-toast)와 함께 구현·로컬 검증을 마쳤다. 실제 모바일 브라우저/설치형 PWA, OS 한글 IME, VoiceOver/TalkBack, 오프라인·오디오 조합은 미검증이며 최종 실사용 검증에 남긴다. `임시 저장`의 현재 동작·초안 삭제 확인·선택 잠금·완료 0포 표현은 TW-03, 번체/긴 내용의 PNG 조판은 TW-07 범위다. 원격 푸시·main 반영·배포는 하지 않았다.

<a id="common-toast"></a>

### 2026-08-31 — 공통 알림 C 말풍선·문구 통일

구현 `763fd5a`를 `9348198`에서 `codex/tw-readiness`에 병합했다. 통합 상태에서도 5초 우림 완료→우림 끝→새 마감 알림과 시작 상태 복귀를 재확인했다.

사용자가 선택한 C 시안을 `index.html`의 공통 토스트에 반영했다. 9px 모서리·작은 상단 꼬리·얇은 갈색 금빛 테두리·짙은 면과 낮은 그림자를 사용하고, 상단 조작 아래 콘텐츠 왼쪽에 배치한다. 긴 문구는 앱 폭 안에서 왼쪽 정렬로 줄바꿈한다. [공통 알림 정책](design-navigation.md#common-toast)에 기준을 기록했다.

세션 마감은 `이번 차를 마무리했습니다.`로 바꿨다. 임시 저장·복사·공유·다운로드·실패·선택 보호의 모든 토스트 문구를 ‘-습니다’체로 통일하고 이모지는 제거했다. 발생 조건·표시 시간·세션/초안 동작과 `role="status"`·포커스 비이동은 유지했다. 설정의 지속 오류 안내나 확인창은 토스트로 바꾸지 않았다. 배포용 SW 캐시는 `v59`로 준비했다.

실제 로컬 앱의 5초 우림→완료→차 선택 보호 안내→우림 끝→1포 5초 복귀를 확인했다. 375×660 세션 마감 말풍선과 320px 다중 행 안내에 가로 넘침이 없고, 9px 모서리·8px 꼬리·16px 왼쪽 여백을 확인했다. 알림이 표시돼도 클릭한 버튼에 포커스가 남고 조작을 가로채지 않는다. 느낌 메모 입력→임시 저장에서도 초안과 포커스가 유지된다. 원본 캡처는 Git 외부 `notification-studies`와 같은 작업 폴더의 `toast-implementation`에 보관했다. 인라인 JS 구문·기존 설정 테스트 16개·문서 링크·diff도 확인했다.

TW-02의 공통 알림 정리만 선반영했으며 시트·카드 접근성 전체는 완료하지 않았다. 실제 모바일 기기·스크린리더 검증과 원격 푸시·배포는 하지 않았다.

<a id="tw-01-preferences"></a>

### 2026-08-31 — TW-01 커스텀 태그·설정 보존

구현 커밋: `9425751` (`fix: preserve preferences and legacy tea adjustments`). 기능 브랜치 `codex/tw-01-preferences`의 구현 `9425751`·검증/문서 `294320d`를 `d0c7ee9`에서 `codex/tw-readiness`에 병합했다. 통합 후 Node 16개·브라우저 6개 시나리오를 다시 통과했고, 6초 우림→기록 1포 6초→출탕→다음 포 11초 흐름도 재확인했다.

메인과 설정이 `scripts/preferences.js`의 v1 읽기·필드 패치를 공유한다. 저장 직전 최신 raw를 읽고 편집한 필드/차의 보정만 바꿔 태그·다른 차 보정·미지의 `locale`/`tagItems` 등을 보존한다. 기본 태그의 화면 표시와 저장을 분리하고 `customTags: []`를 존중한다. 농향 우롱·생보이·숙보이의 과거 설정 ID를 호환 읽기하며 canonical 우선·손상 원본 보존·개별 기본값 복원 시 별칭 삭제를 적용했다. 상세 계약은 [데이터 보존](data-portability.md#preferences-contract)에 둔다.

잘못된 JSON·루트·지원하지 않는 버전은 명시적 전체 초기화 전까지 쓰기를 막는다. 읽기/쓰기 예외에도 타이머·현재 메모리 편집은 유지하고 저장 실패를 안내한다. 설정의 전체 초기화·기본 태그 복원에 취소 가능한 확인을 추가했다. 설정 시간 범위는 5–120초이며 시간 조절·태그 삭제 버튼에 44px 영역·접근성 이름·편집 후 포커스 유지를 적용했다. 새 공통 JS를 SW 앱 셸에 추가하고 캐시를 `v58`로 갱신했다.

검증:

- `node --test tests/preferences.test.cjs`: 16개 통과. 빈/기존/중복 유니코드 태그, 세 ID 별칭·충돌·손상 canonical, 잘못된 payload, 중첩 필드의 순차 교차 편집, 미래 필드, 읽기·쓰기·삭제 예외를 확인했다.
- 실제 두 HTML을 여는 로컬 브라우저 harness: 6개 시나리오 통과. 태그 편집→메인의 차·다구·시간·음소거 저장→오래 열린 설정의 다른 차 시간 저장, 태그 누락/빈 배열·재진입, 별칭과 시간 범위, 잘못된 raw 보호, 읽기/쓰기 실패 시 메모리 값·안내, 초기화/태그 복원 취소·삭제 실패·명시 초기화 후 복구를 확인했다. 예외와 확인 응답은 검증 서버에서만 주입하며 제품 코드에 테스트 분기는 없다.
- 설정 375×660·320×660 실제 렌더: 가로 넘침 없음, 설정 버튼 모두 44px 이상. 시간 조절 뒤 같은 버튼의 키보드 포커스 유지 확인. 작은 화면과 변경된 안내의 스크린샷을 남겼다.
- 메인 5초 정상 우림→완료 기록 1포 5초→출탕→다음 포 10초, 저장 델타 -15 유지 확인.
- 캐시 원본에서 `chahanjan-shell-v58`의 공통 JS와 모든 앱 셸 자산을 확인했다. 로컬 HTTP 서버를 중지한 채 설정 진입→시간 +1 저장→메인 재진입 6초 표시를 확인했다. 이는 앱 서버 자산의 오프라인 검증이며 외부 글꼴 차단 테스트는 아니다.
- 두 HTML의 인라인 JS·공통 JS 구문, 브라우저 런타임 오류, 문서 링크·`git diff --check` 확인.

브라우저 harness·주입 서버·원본 캡처·검증 로그는 Git 외부의 `/Users/kioku/.codex/visualizations/2026/08/31/01a055b0-8e12-73e2-ba94-91ae80e61456/tw-01/`에 보관한다. 화면/저장 검증은 데스크톱 브라우저의 결과이며 실제 iOS/Android·설치형 PWA·오디오 출력·스크린리더는 미검증이다. 기존 설치의 v57→새 버전 업데이트와 외부 글꼴 오프라인 조합은 TW-07·08에서 다시 확인한다. 다중 탭 동일 필드의 동시 쓰기와 구버전 앱의 쓰기를 무손실 병합한다고 보장하지 않는다.

원격 푸시·main 반영·배포는 하지 않았다. TW-02–08·장기 기록·언어 UI는 이번 구현에 포함하지 않는다.

## 문서 변경

### 2026-08-31 — 한국어 기본 감각 15개와 선택 확장 확정

[후기 조사 §7](research-korean-tea-review-vocabulary.md#confirmed-vocabulary)에 확정 목록과 대만 풍미륜·TAGs·NIH/NIDCD·후기 원문의 근거 범위를 정리했다. 구운 향은 향으로 분류하며, 입안에서 코로 전달되는 향과 일상적인 맛 표현의 관계를 설명했다. 텁텁함 채택은 제품 결정으로 기록하고 교체 전 깔깔함 조사 결과를 검증 근거로 바꾸지 않았다.

단어장·차 느낌·제품 설계와 로드맵에 한국어 명칭·확장 방식 확정을 반영했다. 원전과 개인 표현의 출처, 구운맛·구수함 등 사용자 원문, 기존 ID·기록의 보존 경계를 유지한다. 최종 목록 15개·출처·문서 링크·앵커·diff를 확인했으며 번체 문구·세부 해설·이관·Phase E 구현은 미완료다. 앱 코드·저장값·SW 변경이나 배포는 하지 않았다.

### 2026-08-31 — 입안 감각 문구를 깔깔함에서 텁텁함으로 교체

사용자 결정에 따라 [기본 문구안](sensory-vocabulary.md#default-vocabulary)의 입안 감각을 `깔끔함 · 텁텁함 · 떫음 · 묵직함 · 긴 여운`으로 정리했다. 텁텁함의 생활어 설명은 초안으로 두고, 공식 세치도와의 대응·떫음 및 깔끔함과의 의미 구분은 검증된 사실로 표시하지 않았다. 기존 후기 조사는 교체 전 결과로 보존하고 후속 결정 안내를 추가했다. 문서 링크·내용·diff를 확인했으며 앱 코드·기존 태그 ID·저장값은 이 작업에서 변경하지 않았다.

### 2026-08-31 — 한국어 차 후기와 감각 후보 15개 비교

[후기 어휘 조사](research-korean-tea-review-vocabulary.md)에 공개 후기·개인 시음기 13개 페이지의 본문과 후보 15개를 대조했다. 판매자·서포터즈·가향차·본문 미확인 자료를 분리하고, 단어의 직접 사용과 의미 대응, 감각의 부재·비교 문맥을 구분했다. 추가 검색으로 짭짤함·시큼함의 직접 시음 용례도 확인했으며, 표본의 대표성이나 사용 빈도는 주장하지 않는다.

깔깔함의 정확한 용례는 확보하지 못했고, 까끌함·분말의 거침을 공식 세치도와 등치할 수 없어 거친 질감을 대안으로 남겼다. 단어장 설계에 최신 검토 후보와 조사 링크를 연결했다. 인용·출처·본문 대응, 문서 링크·diff를 확인했으며, 문구 확정·사용자 실험·앱 코드·저장값·SW 변경은 하지 않았다.

### 2026-08-31 — 입안 감각의 공식 정의와 한국어 표현 조사

[풍미륜 조사 §3.3–3.4](research-taiwan-tea-flavor-wheel.md#mouthfeel-definitions)에 공식 TAGs 차품 가이드의 문장형 설명과 차개장 영문 안내를 대조해 다섯 감각의 뜻·한계를 기록했다. 공식 PDF 10·29쪽의 축 구조·플랫폼 안내도 렌더로 확인했다. 한국어 후보는 국립국어원 사전의 의미 범위를 참고한 편집 제안으로 구분하며, 기본 15개 방향은 유지하되 입안 감각의 선택 문구는 미확정으로 남겼다.

공식 설명에 있는 분자 크기 서술을 검증된 물리적 인과로 확대하지 않고, 고운 감촉·매끄러움·농도감·여운의 혼동 위험을 정리했다. 문서 링크·앵커·diff를 확인했다. 한국어 사용자 실험이나 앱 구현은 하지 않았으며, 기존 코드·ID·사용자 저장값은 이 작업에서 변경하지 않았다.

### 2026-08-31 — 기본 감각 15개와 단어장 선택 확장 방향

사용자와 [향 5개·맛 5개·입안 감각 5개를 기본 제공](sensory-vocabulary.md#default-vocabulary)하고 세부 표현은 단어장을 참고해 추가하는 방향을 정했다. 기본 제공과 이번 차의 감각 선택을 구분하고, 입안 감각 다섯 축의 실제 태그 문구는 구현 전에 다듬도록 했다. 공식 항목의 출처와 개인 원문을 보존하며 기존 14개·사용자 목록을 자동 삭제하거나 재분류하지 않는 전환 조건을 기록했다.

설계·로드맵·차 느낌 문서에 연결하고 조사 당시의 14개 유지 제안은 새 결정으로 대체됐음을 표시했다. 문서 링크·앵커·diff를 확인했으며 Phase E는 미구현으로 유지한다. 이 작업은 문서만 변경했고 코드·저장값·SW 변경, 커밋·병합·푸시·배포는 하지 않았다.

### 2026-08-31 — 대만 풍미륜과 기본 감각 태그 비교 조사

[대만 특색차 풍미륜 조사](research-taiwan-tea-flavor-wheel.md)에 2.0의 향·맛·입안 감각 구조와 기본 14개 태그 전수 대응표를 작성했다. 번체·영어·한국어 원본을 대조하고, 2.0에서 빠진 나무·흙은 1.0에서 확인했다. 직접 대응·부분 대응·버전별 미수록을 구분하고, 공통 단어·앱의 설명·개인 표현의 경계와 원전 이용 조건을 검토 사항으로 남겼다.

14개 이름·ID 및 판정 집계, 원전의 분류별 항목 수, 문서 링크·diff를 확인했다. 단어장 설계와 로드맵에 조사 링크를 연결했으며 Phase E는 미구현 상태를 유지한다. 앱 코드·사용자 데이터·SW 변경은 없고, 커밋·병합·원격 푸시·배포는 하지 않았다. 진행 중인 TW-05 작업 트리의 기존 변경은 이 조사에 포함하지 않는다.

<a id="tw-04-deferred"></a>

### 2026-08-31 — TW-04 보류·다음 착수 TW-05로 조정

사용자가 실제로 일찍 따라내는 일이 거의 없다는 경험을 확인해 TW-04 즉시 출탕을 보류했습니다. [결정 근거와 재검토 조건](DESIGN.md#early-pour-policy)을 기록하고 번호·이전 설계·검증안은 보존했습니다. 이번 TW 묶음의 구현·번역·필수 검증·배포 준비 조건에서 조기 출탕을 제외하며, 축소판 기능도 추가하지 않습니다.

ROADMAP·실행 계획·AGENTS의 다음 착수를 TW-05로 맞추고 차 느낌·다국어·배치 설계의 참조 범위를 정리했습니다. TW-08은 정상 완료·출탕·다음 포·세션 종료의 중복 처리 방지 검증을 유지합니다. 문서 변경만으로 런타임 기능 완료를 추가하지 않으며, 기존 TW-01–03의 구현·검증 이력은 그대로 보존합니다.

검증: `git diff --check`, 변경된 Markdown 8개 파일의 로컬 링크·앵커 168개 검사, 이전 착수 순서·조기 출탕 필수 검증 표현 검색과 전체 diff 검토를 통과했습니다. 런타임·자산·SW 변경이 없어 앱 테스트는 재실행하지 않았으며, 원격 푸시·main 반영·배포는 하지 않았습니다.

### 2026-08-31 — 이전 차 느낌 목업을 Git 외부로 이동

독립 검토용 `mockup-tea-feeling.html`을 저장소에서 제거하고 작업 전용 미리보기 폴더의 `legacy-tea-feeling/`에 원본 보관했습니다. 제목 편집·태그·카드 미리보기 등 과거 검토 내용을 남기기 위해 HTML을 수정하지 않고 상대 경로의 개완 이미지도 함께 복사했습니다. 현행 배치 미리보기는 계속 별도로 관리합니다.

HTML·이미지의 SHA-256 일치와 상대 경로 자산 존재, 앱·문서·서비스워커의 기존 참조 없음, 실제 앱 코드·자산·SW 불변을 확인했습니다. AGENTS에 목업의 Git 외부 관리 원칙을 추가하고 루트의 `mockup-*.html`은 ignore하도록 했습니다. 과거 Git 이력은 수정하지 않았으며 배포는 하지 않았습니다.

### 2026-08-31 — 공지 갱신일 위첨자

[공지사항 설계](design-navigation.md#announcements)에 메뉴명 바로 오른쪽의 `MMDD` 위첨자를 추가했습니다. 앱 배포일이나 예정일이 아닌 공지 묶음의 마지막 내용 갱신일을 사용하며, 열람 후에도 유지합니다. 공지 화면과 접근성 설명에는 연도를 포함한 전체 날짜를 제공하고 별도 읽음 상태는 저장하지 않습니다. 미리보기의 `0831`은 예시 날짜입니다.

미리보기의 한국어 375px 렌더, 번체 320px의 가로 넘침 없음·44px 메뉴 높이, 전체 날짜 안내, 열람·언어 변경 후 날짜 유지와 JavaScript 구문·브라우저 오류 로그·문서 diff를 확인했습니다. 실제 앱 코드·공지 게시·배포는 변경하지 않았습니다.

### 2026-08-31 — 공지사항 메뉴와 업데이트 안내 설계

사용자 요청으로 메뉴의 사용법 위에 스피커 아이콘+공지사항을 추가하는 [설계](design-navigation.md#announcements)를 반영했습니다. 실제 배포된 업데이트와 예정된 업데이트를 구분하고, 제목은 테두리 없이 가운데 정렬·본문은 왼쪽 정렬로 읽습니다. 자동 팝업·푸시 없이 같은 문서에서 읽으며 타이머·초안을 보존하는 방향입니다. 첫 공지에는 UI 개편 뒤 기능 위치와 기존 이미지 유지 여부를 실제 배포 내용에 맞춰 안내합니다.

TW-05에 정적 공지 진입·읽기, TW-06에 번체 공지, TW-08에 배포 내용 일치·기존 설치 업데이트·오프라인 검증을 연결했습니다. 공지 데이터는 앱과 함께 배포하며 별도 서버·CMS를 추가하지 않습니다. 기존 장기 기록·단어장·레시피의 Phase는 유지했습니다.

별도 미리보기에서 공지 메뉴·본문 펼침·타이머 복귀, 한국어 375px 렌더, 번체 320px의 제목 정렬·44px 이상 조작 영역·가로 넘침 없음, TW 단계의 공지사항/사용법/설정 노출, 공지 읽기 후 메모와 음소거 상태 유지를 확인했습니다. JavaScript 구문·브라우저 오류 로그·문서 링크·diff도 확인했습니다. 예시 공지와 설계만 변경했으며 실제 공지 게시·앱 런타임·자산·SW·배포 변경은 없습니다.

### 2026-08-31 — 느낌 시트 저장 버튼 문구

사용자 요청에 따라 장기 기록 버튼을 `모바일 기기 아이콘 + 브라우저 기록으로 남기기`, 초안 유지·닫기 버튼을 `임시저장`으로 정했습니다. [차 느낌](design-tea-feeling.md#draft-policy)·[배치 설계](design-navigation.md)의 문구와 아이콘 기준, DESIGN·ROADMAP·데이터 보존 문서의 연결 표현을 맞췄습니다. 임시저장은 현재 세션 메모리에 유지하는 기존 의미이며, 버튼 근처에 `임시저장은 이번 우림 동안만 유지됩니다.`를 표시합니다. 브라우저 기록 저장은 방문 기록·동기화가 아닌 앱의 로컬 기록이며 Phase B 구현 대기입니다.

별도 미리보기의 375px 렌더에서 문구·스마트폰 아이콘을 확인하고, 320px에서 가로 넘침 없음·44px 이상 버튼 높이·임시저장 후 입력 유지·기록 저장 예시 후 기록함 링크를 확인했습니다. JavaScript 구문·브라우저 오류 로그·문서 diff를 확인했습니다. 실제 앱의 저장 기능·코드·이미지 자산·SW는 변경하지 않았습니다.

### 2026-08-31 — 2단계 메뉴의 테두리 제거·가운데 정렬

사용자 검토를 반영해 [배치 설계 §2.1](design-navigation.md)에 사용법·설정 등 하위 메뉴 행의 테두리를 없애고 메뉴명을 가운데 정렬하는 기준을 추가했습니다. 아이콘 유무와 관계없이 텍스트 중심을 맞추고, 전역 메뉴·입력창·주요 실행 버튼은 기존 형태를 유지합니다. 터치 영역과 키보드 포커스 표시는 제거하지 않습니다.

별도 미리보기의 한국어 375px 렌더와 번체 320px 사용법·설정 행을 확인했습니다. 메뉴 행은 테두리 0px·가운데 정렬·높이 44px이며, 사용법 텍스트 중심 오차와 가로 넘침이 없었습니다. 브라우저 오류 로그·JavaScript 구문·문서 diff를 확인했으며 실제 앱 런타임 변경은 없습니다.

### 2026-08-31 — 후속 기능 배치와 아이콘·문구 기준

[화면 배치와 메뉴 표현](design-navigation.md)을 추가하고 DESIGN·ROADMAP·다국어·차 느낌·단어장·TW 실행 계획을 연결했습니다. 메인 우상단의 언어 선택+메뉴, 기록함·단어장·레시피의 단계별 메뉴 진입, 느낌 작성 중 단어장, 기록함 안 회고, 설정의 전체 백업·복원을 배치했습니다. 목적지는 아이콘+이름, 결과가 중요한 행동은 문구, 맥락이 분명한 작은 조작은 아이콘만 쓰는 기준도 정했습니다.

기존 설정 전용 언어 선택은 메인의 명시적 전환으로 확장했습니다. 같은 문서의 전환은 우림 상태를 보존하고, 별도 도움말·설정 문서로 나가는 동작은 계속 보호합니다. 첫 저장 후에만 기록 진입점을 노출하거나 홈에 개수 배지를 추가하는 제안은 채택하지 않았습니다. 기능 도입 후에는 빈 기록함도 메뉴에서 접근하며 첫 저장 성공 뒤 맥락 링크를 제공합니다.

별도 배치 미리보기에서 TW/후속 단계 메뉴 노출, 320px·375px 폭, 한국어·번체 상단 조작의 44px 높이와 가로 넘침, 느낌→단어장→느낌의 메모·포커스 보존, 언어 전환 뒤 메모·태그 유지, 명시적 저장 뒤 기록함 링크, 설정→백업 진입, Escape 닫기·포커스 복귀를 확인했습니다. 미리보기의 JavaScript 구문 검사와 브라우저 오류 로그, 문서 로컬 링크·앵커 및 diff를 확인했습니다. 화면 캡처는 초기 한국어 배치를 확인했으며, 일부 상호작용 후 캡처가 빈 이미지로 반환되어 그 상태의 시각 검증은 DOM 경계 확인과 구분합니다.

이는 배치 예시와 문서 검증이며 실제 타이머·영구 저장·전체 번역·스크린리더·모바일 PWA의 통과 결과가 아닙니다. TW-05에는 언어 선택·기본 메뉴만 포함하고 후속 기능의 Phase는 유지합니다. 런타임·SW·푸시·배포 변경은 없습니다.

### 2026-08-31 — TW 착수 전 동작 계약과 개발 흐름

[AGENTS.md](../AGENTS.md)에 통합 브랜치 `codex/tw-readiness` → TW별 기능 브랜치 → 설계·구현·검증 → 순차 병합 흐름과 문서 역할을 정리했습니다. 기존 대만 우선순위 문서와 [실행 계획](tw-implementation-plan.md)을 공통 기준점으로 함께 관리합니다.

[DESIGN §6.5](DESIGN.md), [차 느낌](design-tea-feeling.md#draft-policy), [설정 보존](data-portability.md#preferences-contract), 새 [다국어 설계](design-localization.md)에 구현 계약을 반영했습니다. 상시 느낌 진입·초안이 있을 때 선택 잠금·명시적 종료, 완료 0포 표현, 조기 출탕 시간과 중복 완료 방지, v1 부분 저장·ID 별칭·빈 태그 보존, 한국어 기본값과 대만 번체 선택·안전한 페이지 이동, 기존 제품명 유지가 대상입니다.

검증은 현재 코드 대조와 문서 일관성·로컬 링크·diff 확인입니다. 데이터 fixture와 타이머 경계·언어·기기 시나리오는 실행할 완료 조건으로 정의했으며 아직 통과 결과가 아닙니다. TW-01–08은 모두 구현 대기이고 런타임·SW 캐시·배포 변경은 없습니다. 다음 구현 항목은 TW-01입니다.

### 2026-08-31 — 대만 실사용 우선 처리 묶음 추가

대만에서 약 3개월간 중국어를 공부하고 사람들과 차를 마시는 실사용을 위해, [현재 우선 처리](ROADMAP.md#taiwan-priority)를 추가했습니다. Phase A의 태그 보존·시트 접근성·초안 경계·즉시 출탕과 Phase D-1의 번체 중국어 지원을 TW-01–08로 묶고, 주분류·완료 확인·선행 결정·제외 범위를 적었습니다.

기존 A 전체→B→C→D 순서에서 번체 지원만 A 핵심 안정화와 함께 앞당깁니다. 영어·간체·일본어는 D-2로 남기고, 백그라운드 완료 신호 예약은 별도 검증합니다. 감각 연출·스와이프·전체 폰트 셀프호스팅과 장기 기록·백업은 이번 묶음의 선행 조건이 아닙니다. 카테고리 정의와 기존 완료 내역은 유지했으며, 기능 구현·완료·배포를 뜻하는 변경은 아닙니다.

### 2026-08-31 — 개발 카테고리 정의와 완료 내역 분리

기존 날짜순 표의 완료 항목 12개에 주분류를 부여해 6개 주분류(우림 감각·다구 표현, 조작·세션 흐름, UI·화면 구조·내비게이션, 차 느낌·감각 확장, 성능·PWA·오디오, 도움말·설정·언어)로 색인했습니다. 데이터 보존·이동성은 독립 완료 항목이 아직 없어 완료 색인에서는 생략했습니다.

분류 기준은 다음과 같이 정리했습니다. `우림 상태의 감각 표현`·`차 선택의 시각 단서`는 우림 감각·다구 표현으로, `짧은 조작의 편의`는 조작·세션 흐름으로 옮겼습니다(제스처·배치는 UI 책임). `접근성과 피드백 중복성`은 UI 기준이자 모든 카테고리에 공통인 품질 기준으로 두고, 신호 표현은 우림 감각에 둡니다. `설치형 앱 완성도`는 성능·PWA·오디오로, `한 잔의 감각 기록`은 차 느낌·감각 확장과 데이터 보존·이동성으로 분리했습니다. 도움말·설정·언어는 기존 완료 내역과 다국어 계획의 공통 책임으로, UI는 아이콘·탭·메인 역할 등 확장 전 화면 구조 설계의 책임으로 명시했습니다.

변경 이유는 앱 철학을 책임 범위와 연결하고, 기능 확장 때 생기는 UI·데이터·기록의 경계 혼동을 줄이며, 로드맵에서 전체 진행 상황을 쉽게 훑을 수 있게 하기 위해서입니다. Phase·완료일·구현 상태와 기존 12개 항목의 구현 범위는 유지했습니다. 런타임 변경은 없습니다.

## 2026-08-01

<a id="asset-loading"></a>

### 다구 이미지 on-demand와 오디오 잔여음 정리

분류: 성능·PWA·오디오

평상시 선택 다구의 SVG `href`만 유지하고 전환 대상은 `Image.decode()`와 실제 SVG load 뒤 교체했습니다. 새 화면 페인트 후 이전 `href`를 제거했습니다. 폰트를 기다리지 않고 시작 자산과 UI를 초기화하며 음소거·초기화 시 활성 oscillator를 정지했습니다. SW 캐시 v57을 적용했습니다.

관련 커밋: [`89a5043`](https://github.com/winterrainlee/tea-timer/commit/89a5043) (`Load vessel artwork on demand`)

<a id="rendering-cache"></a>

### 모바일 렌더·PWA 캐시 최적화

분류: 성능·PWA·오디오

상태 기반 단일 스케줄러, 진행 중 30fps·완료 대기 12fps, idle·배수 완료 정지, reduced-motion 정적 렌더, 김 노드 재사용을 적용했습니다. 문서 network-first와 정적 자산 cache-first를 shell/static 캐시로 분리했습니다.

관련 커밋: [`aefbfa4`](https://github.com/winterrainlee/tea-timer/commit/aefbfa4) (`Reduce mobile rendering and cache overhead`)

## 2026-07-28

<a id="small-screen-safari"></a>

### 작은 화면·Safari PWA 안정화

분류: UI·화면 구조·내비게이션

iPhone Mini급 레이아웃 붕괴와 설정 입력 폭을 교정하고, 카드 PNG는 Web Share 파일 공유를 우선한 뒤 다운로드로 폴백하도록 보강했습니다.

관련 커밋: [`733c8ca`](https://github.com/winterrainlee/tea-timer/commit/733c8ca) (`fix: improve PWA layout and settings guidance`), [`1745c51`](https://github.com/winterrainlee/tea-timer/commit/1745c51) (`fix: share card images in Safari`), [`48ca7b6`](https://github.com/winterrainlee/tea-timer/commit/48ca7b6) (iPhone 13 mini 여백 붕괴 수정), [`e67b5e0`](https://github.com/winterrainlee/tea-timer/commit/e67b5e0) (설정 입력 폭·터치 영역 수정)

<a id="feeling-mvp"></a>

### `[이번 차의 느낌]` MVP

분류: 차 느낌·감각 확장

현재 세션 요약, 기본·커스텀 태그, 제목·자유 문장, 세션 초안, 텍스트 복사, 680×900 Canvas 카드 공유·다운로드를 구현했습니다. `help.html`과 `settings.html` 역할을 분리했습니다. 영구 기록 저장은 별도 미완료 범위입니다.

관련 커밋: [`6bbaa93`](https://github.com/winterrainlee/tea-timer/commit/6bbaa93) (`feat: [차 느낌] 시음 카드 바텀시트 모달 및 help.html 3대 섹션 개인설정 통합 구현`), [`78e6174`](https://github.com/winterrainlee/tea-timer/commit/78e6174) (사용법·개인설정 페이지 분리)

<a id="session-draining"></a>

### 세션 보호와 모든 다구 출탕 단계

분류: 조작·세션 흐름

1포 이상 완료 후 차·다구 변경을 `우림 끝`까지 잠그고, 일반 다구와 표일배 모두 우림 완료 뒤 실제 따라내기·배수 단계를 거쳐 다음 포로 이동하도록 했습니다.

관련 커밋: [`97f005d`](https://github.com/winterrainlee/tea-timer/commit/97f005d) (`feat: 우림 세션 보호(Session Lock) 구현`), [`33f5fd6`](https://github.com/winterrainlee/tea-timer/commit/33f5fd6) (`feat: 모든 다구 우림 완료 후 따라내기(出湯) 단계 및 기물별 시각화 구현`)

## 2026-07-17

<a id="ios-audio"></a>

### iOS 오디오 인터럽션 복구

분류: 성능·PWA·오디오

`AudioContext`가 `suspended`뿐 아니라 `interrupted` 등 모든 non-running 상태일 때 재개하고, `closed`면 재생성하도록 했습니다. 앱 복귀·다음 포 시작·매 재생 직전에 복구하며, `sw.js` 캐시를 v49로 갱신했습니다. 백그라운드 완료 신호 선예약은 별도 미완료 항목입니다.

관련 커밋: [`516146b`](https://github.com/winterrainlee/tea-timer/commit/516146b) (`Restore tea timer audio after iOS interruptions`)

## 2026-07-12

<a id="feeling-entry"></a>

### `[이번 차의 느낌]` 선행 정리

분류: UI·화면 구조·내비게이션

메인 화면 푸터의 응원 박수(👏)·모달·제작자 반응 전송 코드를 제거하고 도움말의 박수만 유지했습니다. 그 자리에 음소거 버튼과 같은 실루엣의 연필 라인 아이콘 노트 버튼과 당시 `준비 중` 안내 모달을 추가했습니다. 상세 설계는 [design-tea-feeling.md](design-tea-feeling.md) §7을 따릅니다.

관련 커밋: [`edce834`](https://github.com/winterrainlee/tea-timer/commit/edce834) (`Extract tea-feeling design doc and seed footer note button`)

<a id="help-tone"></a>

### 도움말 톤 정리

분류: 도움말·설정·언어

`help.html` 전체를 정중한 합니다체로 통일했습니다(해요체는 과하게 친근해 배제, 요청문만 `-해 주세요`). 앱 소개 섹션을 추가하고, 사용법에 권장 눈금·泡 설명을 반영했습니다. 포크/오픈소스 문구를 사용자 관점(`자유롭게 쓰세요`)으로 재작성하고, 기억한 설정에 "이 기기에만 저장"을 명시했으며, 박수 모달 `싫어요`→`괜찮습니다` 등 문구를 순화했습니다.

관련 커밋: [`1a4783f`](https://github.com/winterrainlee/tea-timer/commit/1a4783f) (`Switch help copy from haeyo to hamnida register`), [`18b1ddb`](https://github.com/winterrainlee/tea-timer/commit/18b1ddb) (`Soften help page tone for tea beginners`)

<a id="preferences"></a>

### 반복 사용 상태 영속

분류: 조작·세션 흐름

`teaTimer.preferences.v1`에 차·다구·차별 권장 대비 보정값(델타)·음소거를 저장·복원했습니다. 슬라이더 권장 지점 눈금(금색 채움 위에서도 식별되도록 어두운 테두리), `aria-valuetext`로 권장 시간 병기, 도움말 `기억한 설정 지우기`, 우림 중 `−`/`+` 비활성화를 적용했습니다. 상세 스펙은 [DESIGN.md](DESIGN.md) §6.2를 따릅니다.

관련 커밋: [`8b25b4b`](https://github.com/winterrainlee/tea-timer/commit/8b25b4b) (`Persist repeated-use preferences as per-tea deltas`)

## 2026-06-28

<a id="tea-visuals"></a>

### 우림 시각 표현 보강

분류: 우림 감각·다구 표현

개완·표일배의 김 배출 위치를 뚜껑과 림 사이 틈으로 교정하고, 7종 차의 젖은 잎 형태를 분리했으며, 유리 다구의 찻잎을 idle부터 표시했습니다.

관련 커밋: [`3c9508c`](https://github.com/winterrainlee/tea-timer/commit/3c9508c) (`Fix steam emission positions for gaiwan and piaoyibei`), [`684428e`](https://github.com/winterrainlee/tea-timer/commit/684428e) (`Differentiate wet leaf shapes per tea type`), [`892541e`](https://github.com/winterrainlee/tea-timer/commit/892541e) (`Show tea leaves from the start of brewing`)

<a id="separator-liquid"></a>

### 표일배 액체·찻잎 위치 교정

분류: 우림 감각·다구 표현

윗 챔버의 배수구 위로 찻잎과 액체 하단을 올려 거름망 안에서 우러나는 물리적 구조에 맞췄습니다.

관련 커밋: [`b82ef6d`](https://github.com/winterrainlee/tea-timer/commit/b82ef6d) (`Fix piaoyibei leaf and liquid positions above drain spout`)

## 2026-06-18

<a id="reactions"></a>

### 도움말 제작자 반응 집계

분류: 도움말·설정·언어

`help.html`의 박수/괜찮습니다 모달에서 앱·반응값만 Cloudflare Worker로 보내고 D1에 집계했습니다. 허용 Origin과 입력값 검증, Bearer 토큰 기반 비공개 합계 API, 실패 시 사용자 흐름을 막지 않는 전송 방식을 적용했습니다.

관련 커밋: [`82a0260`](https://github.com/winterrainlee/tea-timer/commit/82a0260) (`Add creator reaction tracking`), [`9be2e88`](https://github.com/winterrainlee/tea-timer/commit/9be2e88) (`Document applause privacy note`), [`4c8bb07`](https://github.com/winterrainlee/tea-timer/commit/4c8bb07) (`Add applause button to help page`)
