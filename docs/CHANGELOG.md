# 차 한 잔의 시간 · 변경 이력

> 정리일: 2026-08-31
> 구현 기준: TW-01–03·TW-05·TW-06 + [TW-07 A안 구현·로컬 검증](#tw-07-balanced-card), 실기기 확인 대기

이 문서는 당시 변경 내용과 구현 범위를 보존하는 개발 이력입니다. 현재 구현 상태와 현재 스펙은 [ROADMAP.md](ROADMAP.md) 및 [DESIGN.md](DESIGN.md)를 기준으로 하며, ROADMAP에는 이 문서의 해당 항목 링크만 둡니다. 상세 설계는 [design-tea-feeling.md](design-tea-feeling.md), [sensory-vocabulary.md](sensory-vocabulary.md), [data-portability.md](data-portability.md)에서 관리합니다.

기존 로드맵의 주요 완료 내역을 옮긴 문서이며 전체 Git 이력을 나열한 것은 아닙니다. 완료일은 기존 로드맵 기준으로 보존하므로 관련 커밋의 작성일과 다를 수 있습니다.

## 기능 변경

<a id="offline-usage-copy"></a>

### 2026-08-31 — 공지·이용 안내에 오프라인 사용 조건 추가

사용자가 공개 앱의 비행기 모드 실행 성공을 알려 주었고 휴대폰에서 보낸 테스트 의견도 비공개 조회로 확인했다. 확인한 언어·Safari/설치형 구분과 모든 기능 조합까지 특정된 결과는 아니므로 TW-07/08 전체 통과로 확대하지 않는다.

승인받은 오프라인 안내를 기존 첫 공지 묶음의 다섯 번째 항목과 이용 안내의 사용법에 추가했다. 한국어 원문을 그대로 쓰고 번체·간체도 적용했다. 첫 온라인 접속과 화면 로드, 오프라인 타이머·느낌 카드·이미지 저장, 메시지/박수 전송의 인터넷 필요, 사이트 데이터 삭제 후 다시 접속해야 하는 조건을 안내한다. 기존 네 공지·예정 안내와 캐시·저장 기능은 유지하며 문구 갱신용 SW 버전은 v84다.

공지·번역 카탈로그 Node 16개, v84 캐시 응답 20개를 통과했다. 격리된 8154 origin에서 한국어·번체·간체의 375px·320px 공지/이용 안내 12개 화면을 렌더하고 문구·가로 넘침 없음·스크롤을 확인했다. 화면과 DOM 기록은 Git 외부 `tw-release-readiness/offline-usage/`에 보존한다.

<a id="feedback-reader-production"></a>

### 2026-08-31 — 의견 조회 도구의 운영 연결 확인

운영 Worker의 같은 요청에 시스템 curl은 성공했지만 Python urllib의 기본 User-Agent는 health부터 HTTP 403 / Cloudflare 1010으로 거부됐다. 조회 도구가 실제 이름 `TeaTimerFeedbackReader/1.0`을 명시하도록 수정했다. 브라우저를 사칭하지 않으며 Bearer 인증·TLS 검증·허용 endpoint·리디렉션 차단·로컬 저장 경계는 유지한다.

조회 도구 테스트 12개와 실제 `fetch_page(after=0, limit=3)`로 기존 검증 행 1·2·3을 확인했다. 비밀키·본문 출력과 추가 메시지 생성은 하지 않았다. 프런트엔드 자산과 Worker는 변경하지 않았으므로 SW v83과 운영 Worker 버전을 유지한다. 정기 조회 자동화는 아직 등록하지 않았다.

<a id="tw-feedback-production-ready"></a>

### 2026-08-31 — 운영 의견함 활성화·배포 전 준비 완료

사용자 승인 후 운영 Worker에 `FEEDBACK_READ_TOKEN`·`FEEDBACK_RATE_SECRET`을 등록하고 `FEEDBACK_ENABLED=true`로 배포했다. 비밀키 값은 노출·기록하지 않았으며 기존 `ADMIN_TOKEN`은 유지했다. 운영 D1의 `0001`·`0002` migration은 앞서 적용된 상태임을 확인했다. 현재 운영 Worker 버전은 `06cc86c5-7b80-44af-9b07-f6f2682bb686`이다.

운영 curl 검증에서 health는 200, 무인증 관리자 조회는 401이며 CORS 헤더가 없었다. 한국어·번체·간체 테스트 메시지 ID 1·2·3의 생성, 같은 UUID 재시도 시 같은 행 유지, 해당 행의 인증 조회 성공과 `Cache-Control: no-store`·CORS 없음까지 확인했다. 실제 박수 POST는 보내지 않았다. 일일 `0 4 * * *` 삭제 스케줄 등록도 확인했지만 실제 만료 행 삭제는 운영에서 아직 검증하지 않았다.

운영 검증은 시스템 `curl` 결과 기준이다. `query_feedback.py`의 urllib 요청은 Cloudflare에서 health와 함께 HTTP 403/error 1010을 받아 운영 조회 도구의 실제 접속은 아직 통과로 기록하지 않으며, 도구 수정은 별도 작업으로 남긴다.

코드 공지 네 항목은 `releases`의 첫 묶음 `tw-readiness-2026-08-31`으로 전환하고 `draft`를 비운 SW v83으로 배포했다. 최신 로컬 자동 검증은 Node 77개·Worker 7개·조회 도구 12개(총 96개)와 migration 원문·ID·sequence 보존을 통과했다. 격리 브라우저 8154에서 캐시 v83 갱신, 공지 11개·캐시 20개·언어 전환/타이머 49개·카드 PNG 138개(총 218개)도 통과했다. 공개 브라우저의 기본 흐름은 [Pages 배포 기록](#tw-pages-deployed)에 기록했으며, iPhone 13 mini 한국어 화면은 사용자가 확인했지만 세 언어 설치형 PWA 완료로 확대하지 않는다. 공개 HTTPS 실기기/PWA, 기존 설치 업데이트, 오프라인·오디오 복귀, 글꼴·OS IME·실제 공유, 현지인 문구 검수와 정기 조회 자동화는 남아 있다.

운영 Worker와 앱 배포를 구분해 기록하며, 이전의 승인 대기 상태는 [단계별 역사 기록](#tw-worker-staged-deployment)으로 보존한다.

<a id="tw-pages-deployed"></a>

### 2026-08-31 — GitHub Pages 첫 배포 완료, 필수 실기기 검증 대기

런타임 배포 커밋 [`7de9ada1ad488f9f1efbbfdc5bdd93f5fe661096`](https://github.com/winterrainlee/tea-timer/commit/7de9ada1ad488f9f1efbbfdc5bdd93f5fe661096)이 `main` 및 `origin/main`에 반영됐고 GitHub Pages 상태 `built`를 확인했다. 생성 시각은 `2026-08-31T13:24:40Z`, 갱신 시각은 `2026-08-31T13:25:24Z`다. 공개 [앱 주소](https://winterrainlee.github.io/tea-timer/)에서 캐시 자산 34개와 `sw.js`를 포함한 35개 GET 응답의 바이트 일치를 확인했다.

운영 의견함은 공식 Origin의 CORS preflight 204와 `Content-Type`을 포함한 POST 경로를 확인했다. 기존 브라우저의 첫 진입에서 오래된 v57 화면이 보였으나 사용자 데이터를 삭제하지 않고 두 번째 진입에서 새 v83 화면으로 갱신됐다. 이는 공개 캐시 갱신 확인이며 실제 iPhone 설치 업데이트 검증은 아직이다. 실제 박수 POST는 집계 오염 방지를 위해 의도적으로 보내지 않았다.

공개 브라우저에서 실제 20초 우림 중 `ko`→`zh-TW`→`zh-CN` 전환(20→17→14→10초 지속), 공지를 연 채 완료→닫기→출탕→다음 포 준비→테스트 세션 종료를 확인했다. 한국어·음소거 원래값 복원과 이용 안내→제작자 소개→메시지 폼 진입, 빈 본문 전송 비활성도 확인했으며 추가 메시지는 전송하지 않았다. 이 검증은 실제 iOS/PWA 설치, 소리, OS 공유를 포함하지 않는다.

공개 HTTPS의 세 언어 모바일 브라우저·설치형 PWA, 기존 iPhone 설치 업데이트, 오프라인·오디오 복귀, 글꼴·OS IME·실제 공유와 현지인 문구 검수, 만료 행 삭제 및 정기 조회 자동화는 남아 있다. TW-07·TW-08 전체 완료로 표시하지 않는다. 앞선 런타임 개발·로컬 검증은 [최종 준비 기록](#tw-release-readiness)과 [운영 의견함 기록](#tw-feedback-production-ready)에 둔다.

<a id="tw-worker-staged-deployment"></a>

### 2026-08-31 — 운영 D1·접수 비활성 Worker 반영, 비밀키 등록 승인 대기 (이전 상태)

사용자가 Cloudflare 재로그인과 운영 Worker 배포·D1 변경을 명시적으로 승인했다. 필요한 account/user 읽기·Workers/스크립트 쓰기·D1 쓰기 권한으로 인증을 갱신하고 운영 계정·대상 DB·Worker를 확인했다. 계정 내 Worker는 하나이며 신규 요청 제한 namespace와의 충돌은 없었다. 초기 운영 Worker 버전과 D1 Time Travel bookmark를 Git에서 제외한 `.local/deployment/2026-08-31/`에 기록했다. Wrangler의 `migrations list`는 migration 관리 테이블을 생성하므로 순수 조회만 수행한 것으로 기록하지 않는다. 사용자 박수 데이터는 변경하지 않았다.

Wrangler migration runner로 운영 D1의 `0001_creator_feedback.sql`·`0002_messages_zh_cn.sql`을 적용했다. 전후 박수 행 수·최대 ID가 같고 신규 메시지 테이블이 비어 있음을 확인했다. 이어 `FEEDBACK_ENABLED=false`로 Worker `cb7132d0-974b-4aaf-95f4-7f168a6608a3`을 배포했다. 기존 `ADMIN_TOKEN`과 DB 바인딩을 유지하고 `FEEDBACK_LIMITER`(60초당 3회)·일일 만료 삭제 스케줄을 추가했다. 공개 health는 200, 키가 없는 비공개 조회는 503·CORS 없음·`no-store`를 확인했다. Python urllib의 최초 health 요청은 403이었고 기본 curl로 200을 확인했으며 인증 조회 성공으로 해석하지 않는다. 만료 삭제 스케줄 등록과 실제 운영 만료 행 삭제 검증도 구분한다.

새 `FEEDBACK_READ_TOKEN`·`FEEDBACK_RATE_SECRET`은 Git에서 제외한 로컬 파일에 권한 0600으로 생성했지만, 운영 Worker에 등록하는 전송은 자동 안전 검토가 별도 명시적 승인을 요구해 차단했다. 키 값은 출력·커밋·원격 전송하지 않았다. 운영에는 기존 `ADMIN_TOKEN`만 있으며 의견 접수는 비활성 상태다. 운영 메시지·박수 검증 요청은 보내지 않았다. 키 등록 승인 뒤 접수 활성화·세 언어 검증 메시지·인증 조회를 확인하고 정식 공지와 GitHub Pages 배포를 이어간다. 프런트엔드 v82·공지 초안·main·원격 Git·Pages는 그대로다.

<a id="tw-release-readiness"></a>

### 2026-08-31 — 세 언어 최종 회귀·배포 준비, Cloudflare 재인증 대기

UI 가이드라인 문서 변경을 `03bf9f0`, 세 언어 배포·실기기 검증 계획을 `50f1539`로 통합 브랜치에 커밋했다. 이어 `codex/tw-08-release-readiness`에서 [운영 반영 순서·기준점·장애 대응](tw-implementation-plan.md#release-execution)을 준비했다. GitHub Pages API와 공개 SW에서 기존 배포 `2bfc01e`/v57, `main` 루트·HTTPS·빌드 성공을 확인했으며 원격 main은 통합 브랜치의 조상이다. Worker dry run은 12.02 KiB 번들을 생성했다. Wrangler 인증은 만료 상태이고 재로그인은 계정 인증 세션 변경에 대한 명시적 승인이 필요하다는 자동 안전 검토로 차단됐다. 운영 Worker·DB·main·원격·Pages는 변경하지 않았다.

현재 런타임 v82의 Node **77개**, Worker **7개**, 의견 조회 도구 **12개**, DB 이관의 행·ID·sequence 보존 검사를 통과했다. 브라우저는 사용자 미리보기 저장값을 보호하기 위해 앱 자산을 바이트 그대로 복사한 별도 8154 origin에서 실행했다. 복사한 harness의 주소 가드만 8134→8154로 바꿨다. 공통 UI 60·접근성 36·현지화 49·초안 66·단어 추가 37·번체 21·비동기 세션 9·간체 15·의견함 38·카드 138·공지 11·캐시 20, 총 **500개**를 통과했다. 간체 설정이 남은 상태에서 공지 harness가 한국어 ‘지난 업데이트’만 찾던 오류를 발견해 기존 `data-i18n` 식별자로 검사하도록 수정했다. 앱 공지 분류는 정상이며 런타임 변경은 필요하지 않았다.

공지 네 항목을 정식 배포 묶음으로 옮기고 SW를 v83으로 올리는 패치를 **Git 외부의 로컬 후보**로 준비했다. 저장소에는 적용하지 않았으며 운영 의견함 확인 후 실제 게시일·캐시 버전을 재확인해야 한다. 후보의 Node 77개·공지 11개·캐시 20개·실제 5초 우림을 포함한 현지화 49개를 통과했고 세 언어의 정식 공지 제목·네 항목·준비 중 문구를 확인했다. 후보 서버를 중지한 상태에서 캐시된 앱 재실행→5초 우림 완료→출탕·세션 종료→간체 전환·이용 안내 탐색도 확인했다. 이는 앱 서버 중단 검증이며 실제 iPhone 비행기 모드·PWA·구버전 업데이트 통과를 뜻하지 않는다.

전체 JS 구문과 앱 자산 경로, 문서 링크·diff를 확인했다. 로그·초기 공지 검사 실패 및 수정 후 결과·11개 실제 PNG와 전체 텍스트·공지 후보 스크린샷·게시 전환 패치는 Git 외부 `…/01a05671-a428-7392-940b-09ee1a7816ad/tw-release-readiness/`에 보존한다. 저장소 앱 자산은 점검 전 v82와 해시가 같고, 실제 공지는 초안·의견함 설정은 비활성 상태를 유지한다. 실제 OS 공유·VoiceOver·IME·오디오·세 언어 Safari/PWA·현지인 검수는 기존 합의대로 별도 확인한다.

준비 커밋 `6814657`을 `c241c48`에서 통합했다. 통합 후 Node 77개와 공지 11개·현지화 49개·v82 캐시 20개를 다시 통과했다. 격리 서버에 후보 v83 캐시가 남아 있던 최초 캐시 검사는 실패했으며, 사용자 캐시를 지우지 않고 격리 Worker를 v82로 명시 갱신한 뒤 공지·현지화·캐시를 재검증했다. 앱 런타임이나 원격 배포 상태는 바꾸지 않았다.

### 2026-08-31 — 사용자 검토 기준을 UI 가이드라인으로 정리 (문서만)

지금까지의 구현·디자인 수정에서 사용자가 강조한 여백과 균형, 중복 안내 제거, 정중한 말투, 조판과 원문 보존, 지역별 현지화, 참여와 고지, 실물 중심의 단계적 검토를 [디자인 판단 원칙](ui-guidelines.md#design-principles)에 정리했다. 기능별 상세 계약은 기존 설계에 연결하고, UI 문서에 남아 있던 우하단 고정 박수 버튼 설명과 글꼴 설명을 현재 기준에 맞췄다. 문서 내용·상대 링크·diff를 확인했으며 런타임·SW·배포 상태는 바꾸지 않았고 런타임 테스트는 실행하지 않았다.

### 2026-08-31 — 네 항목 공지 초안을 로컬 화면에 연결

사용자 요청으로 카탈로그에 준비한 메인 UI·감각 단어 15개·중국어 번체/간체·제작자 메시지의 네 항목을 공지 창에 넣었다. 한국어·번체·간체 모두 `업데이트 초안`/`更新草稿`으로 구분하며 배포 이력 `releases`는 비워 둔다. 예정 목록은 비워 두어 준비 중 문구만 표시한다. 본문은 앞서 다듬은 배포용 초안을 그대로 사용했고 새 기능을 공개 배포했다고 기록하지 않는다. 실제 운영 준비 후 첫 배포 묶음으로 옮기는 경계는 [공지 설계](design-navigation.md#announcements)에 기록했다. SW는 v82다.

Node 77개·v82 캐시 20개 검사를 통과했다. 기존 공지 검사의 현재 상태 기대값만 초안 네 항목으로 갱신하고 배포 이력 분류 회귀는 유지했다. 375/320px 세 언어에서 네 제목·본문·예정 안내와 가로 넘침 없음, 창 내부 스크롤로 마지막 본문까지 읽을 수 있음을 실제 렌더로 확인했다. 로그·스크린샷·임시 렌더 fixture는 Git 외부 `…/01a05671-a428-7392-940b-09ee1a7816ad/announcement-draft-preview/`에 보존한다. 원격 푸시·운영 서버/DB 반영·배포는 하지 않았다.

### 2026-08-31 — 언어 선택·메뉴 드롭다운을 내용 폭에 맞춤

사용자 요청으로 두 popover의 고정 252px 폭을 없앴다. 각 항목을 블록 flex 행으로 두어 가장 긴 항목의 글자·아이콘·선택 표시·날짜와 좌우 여백만큼 폭이 잡히게 했다. 화면 가용 폭 상한, 오른쪽 정렬, 44px 조작 높이와 기존 포커스·닫기 동작은 유지한다. [공통 UI 기준](ui-guidelines.md)에 기록했고 SW는 v81로 갱신했다.

한국어·번체·간체의 375/320px 실제 앱 렌더에서 언어 선택 약 117–119px, 메뉴 약 126–127px로 표시되고 글자·선택 표시 잘림과 가로 넘침이 없음을 확인했다(기기 글꼴에 따라 폭은 달라짐). 기존 공통 UI 60개와 캐시 20개 검사를 통과했다. 새 영구 테스트는 추가하지 않았다. 측정·스크린샷·임시 렌더 fixture는 Git 외부 `…/01a05671-a428-7392-940b-09ee1a7816ad/content-sized-popovers/`에 보존했다. 원격 푸시·배포는 하지 않았다.

### 2026-08-31 — 번체 언어 선택지를 네 글자로 통일

사용자 요청으로 메인과 설정의 `繁體中文（台灣）`를 `繁體中文`으로 줄여 `简体中文`과 같은 네 글자로 맞췄다. 세 언어 설정 카탈로그·HTML 폴백·설계를 함께 갱신했다. 대만 번체 표현 기준과 `zh-TW` 저장값, 상단 버튼의 짧은 `繁體` 표시는 유지한다. SW는 v79로 갱신했다.

기존 i18n 12개·공통 UI 60개(320/375/768px)·캐시 20개 검사를 통과했고 메인 선택지와 설정의 실제 렌더를 확인했다. 새 테스트는 추가하지 않았다. 로그·스크린샷은 Git 외부 `…/01a05671-a428-7392-940b-09ee1a7816ad/locale-labels/`에 보존한다. 원격 푸시·배포는 하지 않았다.

<a id="simplified-chinese-local"></a>

### 2026-08-31 — 중국 대륙 간체 지원·세 언어 로컬 검증

사용자 승인으로 `zh-CN`을 추가했다. 메인·설정에서 한국어/繁體中文（台灣）/简体中文을 선택하고, 메인·도움말·설정·공지 초안·느낌 카드·공유·접근성·의견함의 간체 카탈로그를 제공한다. 번체 카탈로그를 바탕으로 간체 글자와 중국 대륙 UI 표현을 정리했고, 풍미륜을 간체로 옮긴 15개를 `cn.*` 독립 ID로 제공한다. 기존 단어·제목·메모·저장값은 언어 전환으로 번역하거나 교체하지 않는다. [확정 계약](design-localization.md#simplified-chinese-review)을 따른다.

간체 화면과 실제 PNG는 SC 시스템 글꼴을 우선하며 한국어·번체 스택은 유지했다. 좁은 설정 화면의 세 언어 라디오는 44px 영역을 유지하며 필요할 때 줄바꿈한다. SW는 v78이며 간체 카탈로그 3개를 오프라인 셸에 포함했다. 공지의 언어 추가 문구를 번체·간체 지원으로 맞췄으나 실제 `releases`는 게시하지 않았다.

의견함 Worker의 locale 허용 목록과 신규 DB 스키마를 확장하고 `0002_messages_zh_cn.sql`을 준비했다. 기존 본문·ID·UUID·접수 시각·AUTOINCREMENT 최고값을 보존한다. 격리한 실제 Wrangler local D1에서 0002 성공 적용, 간체 삽입과 잘못된 locale 거부를 확인했다. 별도 빈 DB에 의도적 SQL 오류를 넣은 migration은 원래 스키마와 0001만 남도록 롤백됐다. 운영 DB나 기존 사용자의 로컬 메시지 DB는 변경하지 않았다.

검증 결과:

- `node --test tests/*.test.cjs`: **77개 통과**. 세 카탈로그 키/치환 변수, 독립 기본 목록, 기존·손상 raw 보존, 언어별 글꼴 계약 포함.
- 전용 8134 브라우저: 공통 UI **60개**, 기존 현지화/실제 5초 우림 **49개**, 번체 **21개**, 간체 신규 **15개**, 의견함/박수 **38개**, v78 캐시 **20개**, 세 언어 카드 조판 **138개** — 합계 **341개 통과**. 간체 3방향 키보드 선택·우림 중 전환·IME 유예·실패 재시도와 320/375px 화면을 포함한다.
- 실제 짧은/긴 간체·혼합 PNG와 원문 텍스트, 글꼴 다운로드 차단 PNG, 미리보기/공유 픽셀 일치·다운로드 폴백·2/2/3/5 표시 한도를 확인했다. 375px 프레임의 메인/설정/도움말과 320px 메시지 창을 직접 렌더했다. 데스크톱 스크롤바가 있는 읽기 페이지의 실제 본문 폭은 프레임보다 15px 작다.
- Worker `npm test` **7개**, `npm run test:migration` 보존/빈 DB/전부 삭제 경계 통과. `python3 -m unittest discover -s tools/feedback -p 'test_*.py'` **12개 통과**. 실제 local D1의 스키마·행·migration 기록도 메인 에이전트가 다시 읽어 확인했다.

스크린샷·실제 PNG·검증 로그·D1 조회 증거는 Git 외부 `…/01a05671-a428-7392-940b-09ee1a7816ad/zh-cn-localization/`에 보존한다. 실제 iPhone/Android의 간체 글꼴·OS IME·공유/PWA와 현지인 표현 검수는 미검증이며 기존 방침대로 배포 후 확인한다. 기능 커밋 `4a0ec05`를 `d987fd6`에서 `codex/tw-readiness`에 병합했다. 통합 상태에서 Node 77개와 실제 5초 우림을 포함한 간체 15개 검사를 다시 통과했다. `main` 반영·원격 푸시·Worker/DB·GitHub Pages 배포·실제 의견/박수 전송·정기 자동화 등록은 하지 않았다.

### 2026-08-31 — 간체 지역 기준과 동시 지원 범위 검토 (문서만)

사용자의 ‘표준 북경어’ 방향을 중국 대륙의 표준 중국어 문어체·간체(`zh-CN`)로 정리했다. 번체 카탈로그·기본 태그·언어 설정·의견함 Worker/DB의 실제 제약을 확인하고 [간체 검토안](design-localization.md#simplified-chinese-review)을 기록했다. 지역 기준과 이번 배포 포함 여부를 구분했으며 후자는 아직 검토 중이다. 문서 링크·diff만 확인했고 런타임·데이터·캐시 변경과 테스트 실행은 하지 않았다.

<a id="future-language-line-breaking"></a>

### 2026-08-31 — 후속 지원 언어의 줄바꿈 기준 사전 정리 (문서만)

사용자 요청으로 [공통 UI 가이드라인](ui-guidelines.md#locale-line-breaking)에 한국어·번체·간체·일본어·영어의 줄바꿈 기준을 모았다. 어절/글자 단위 줄바꿈, 문장부호·일본어 금칙, 긴 주소의 예외 분리, 영어의 자동 하이픈 비활성 기본값, 혼합 언어·원문 보존·실제 PNG 검증 범위를 기록했다. 간체와 번체의 공통 처리와 지역별 검토를 구분하고 로드맵·현지화 설계에서 연결했다. W3C 자료와 문서 링크·diff를 확인했으며 런타임·지원 locale·SW는 변경하지 않았다. 런타임 테스트는 실행하지 않았다.

<a id="help-text-wrapping"></a>

### 2026-08-31 — 이용 안내 왼쪽 정렬과 언어별 줄바꿈

[UI 기준](ui-guidelines.md)에 따라 이용 안내 본문·접힘 제목·메시지/박수 창을 왼쪽 정렬했다. 한국어는 `keep-all`로 어절을 보존하고, 번체는 `normal`과 `line-break: strict`로 글자 사이 줄바꿈과 문장부호 금칙을 적용한다. 한 줄보다 긴 이메일 등은 `overflow-wrap: anywhere`로 넘침을 방지한다. 페이지 맨 위 제목·하단 제품명·버튼 배치와 설정 페이지는 유지했다. 문구나 사용자 원문은 수정하지 않았다.

Node 67개·공통 UI 60개(320/375/768px)·의견함/박수 36개·v77 캐시 17개 검사를 통과했다. 기존 공통 UI 검사의 도움말 제목 정렬 기대값만 갱신했다. 375×660 한국어·번체 본문과 메시지 창을 실제 스크린샷으로 확인했고, 번체 메시지 창은 320px에서도 가로 넘침이 없었다. 로그·스크린샷은 Git 외부 `…/01a05671-a428-7392-940b-09ee1a7816ad/help-text-wrapping/`에 보존한다. 실제 iPhone Safari의 이번 줄바꿈 변경은 별도 미검증이며 `main` 반영·원격 푸시·배포는 하지 않았다.

<a id="korean-request-tone"></a>

### 2026-08-31 — 한국어 요청 표현을 ‘-주세요’로 통일

사이트 문구 전체를 검색해 의견함에 남아 있던 ‘-십시오’를 ‘-주세요’ 형태로 바꿨다. 개인정보 안내는 ‘적지 말아 주세요’, 나머지 입력·오류 안내는 ‘적어 주세요·줄여 주세요·보내 주세요·열어 주세요’ 등으로 정리했다. 한국어 카탈로그와 HTML 폴백을 함께 수정했고, 설명·상태 문장의 ‘-습니다’ 및 번체·사용자 원문·전송 정책은 유지했다. [말투 기준](design-localization.md)에 결정과 범위를 기록하고 SW를 v76으로 갱신했다.

Node 67개, 기존 의견함/박수 36개, v76 캐시 17개 검사를 통과했다. 전체 런타임 검색으로 ‘-십시오’ 잔여가 없고 변경 전후 ‘-습니다’ 문장이 같은지 확인했으며, 실제 메시지 창 렌더도 확인했다. 새 테스트는 추가하지 않았다. 로그·스크린샷은 Git 외부 `…/01a05671-a428-7392-940b-09ee1a7816ad/korean-request-tone/`에 보존한다. `main` 반영·원격 푸시·운영 배포는 하지 않았다.

<a id="help-intro-copy"></a>

### 2026-08-31 — 이용 안내 소개·제작자 문구와 순서 정리

사용자 제공 한국어 문구를 그대로 반영했다. 마지막 제목을 ‘제작자 소개 / 의견 보내기’로 바꾸고, 제작자 이름 → 트위터·GitHub·메일 → 익명 비공개 메시지/90일 서버 보관/이메일 답장/박수 안내 → 두 보내기 버튼 순서로 정리했다. 앱의 시작 취지와 개발·다언어 지원·디자인의 AI 활용 설명은 제목 아래·사용법 앞에 항상 보이도록 옮겼다. 기존 사용법 도입문과 제작자 구역의 중복 소개·AI 설명은 제거하고 번체에도 같은 구성을 적용했다. 메시지 작성 창의 상세 개인정보·AI 검토·정기 삭제 안내와 서버/전송 정책은 변경하지 않았다.

Node 67개, 의견함/박수 36개·공통 UI 60개·현지화/실제 5초 우림 49개·v75 캐시 17개 검사를 통과했다. 기존 AI 고지 검사의 위치를 갱신했고, 번체 소개에 고정 한국어 제품명이 들어가는 경우 그 이름만 미번역 검사에서 제외해 나머지 본문 검사는 유지했다. 375×660 한국어·번체 상단 소개·하단 연락처 순서와 버튼 열기/닫기·포커스를 실제 렌더로 확인했다. 로그·스크린샷은 Git 외부 `…/01a05671-a428-7392-940b-09ee1a7816ad/help-intro-copy/`에 보존했다. 사용자 데이터·타이머·전송 로직은 그대로이며 `main` 반영·원격 푸시·운영 배포는 하지 않았다.

<a id="creator-contact-actions"></a>

### 2026-08-31 — 제작자 소개에 메시지·박수 버튼 나란히 배치

사용자 요청으로 제작자 소개의 메시지 버튼 옆에 박수 버튼을 두었다. 동일 너비·높이의 `메시지 아이콘 + 보내기`, `박수 아이콘 + 보내기` 구성으로 한국어·번체를 제공하고, 접근성 이름은 각 기능을 구분한다. 우하단 고정 박수 버튼과 전용 하단 여백을 제거했으며 제작자 소개의 문구도 아래 박수 버튼을 가리키도록 변경했다. 기존 연락처·AI 고지·메시지 저장 계약은 유지한다.

박수 버튼은 기존 확인 창을 열고 선택한 `clap`/`down`만 기존 API로 전송한다. 창을 열 때 배경 비활성화·스크롤 잠금, 창 안 Tab 순환과 닫기 후 박수 버튼 복귀를 맞췄다. 메시지/박수 창을 열거나 X·Escape로 닫는 것만으로 전송하지 않는다. 서버·운영 데이터는 변경하지 않았다. 관련 CSS·문구 갱신을 위해 SW를 v74로 올렸다.

Node 67개, 의견함/박수 36개·공통 UI 60개·현지화/실제 5초 우림 49개·캐시 17개 검사를 통과했다. 기존 검사에서 고정 버튼 전제를 인라인 버튼으로 바꾸고, 박수는 가짜 전송 응답으로 열기/취소/선택/포커스를 검증했다. 운영 박수를 실제 전송하지 않았다. 320/375/768px에서 동일 행·같은 너비·44px 영역을, 375×660 한국어·번체 실제 렌더와 닫기 후 포커스를 확인했다. 로그·스크린샷은 Git 외부 `…/01a05671-a428-7392-940b-09ee1a7816ad/creator-contact-actions/`에 보존했다. 실제 iPhone 재확인과 운영 배포는 별도이며 `main` 반영·원격 푸시·배포는 하지 않았다.

<a id="feedback-creator-section"></a>

### 2026-08-31 — 의견함을 제작자 소개에 통합·안내 문구 정리

사용자가 의견함 동작이 잘 됨을 확인한 뒤 요청한 후속 수정이다. 별도의 의견 보내기 구역을 제거하고 이용 안내 마지막 ‘제작자 소개’에 메시지 버튼·비공개 의견 안내·기존 이메일을 모았다. 제작자 구역의 접기/펼치기, 박수·기존 연락처와 AI 제작 고지는 유지한다. 작성 창에서는 서버 보관을 개인정보 입력 주의와 연결하고, 90일 후 정기 삭제와 답장이 필요한 경우 이메일로 연락하라는 안내를 한국어·번체에 반영했다. AI 검토·비공개 검토 사본 안내는 유지하며, 의견함 자체에 연락처 입력·답장 기능을 추가하지 않았다.

기존 Node 67개, 의견함 31개·공통 UI 60개·현지화/실제 5초 우림 49개·v73 캐시 17개 검사를 통과했다. 기존 의견함 검사의 진입 경로·문구 전제만 갱신했으며 새 테스트를 추가하지 않았다. 375×660 한국어·번체의 실제 렌더, 마지막 구역의 버튼/메일 링크, 모달 닫기 후 포커스 복귀를 확인했다. 전송·서버·사용자 데이터 로직은 변경하지 않았다. 근거는 Git 외부 `…/01a05671-a428-7392-940b-09ee1a7816ad/feedback-creator-section/`에 보존했다. `main`·원격 푸시·운영 배포는 수행하지 않았다.

<a id="creator-feedback-local"></a>

### 2026-08-31 — 비공개 의견함·Codex 조회 도구·AI 활용 고지

사용자 요청으로 이름·연락처 없는 의견함과 이메일 대신 비공개 조회를 구현했다. [의견함 계약](creator-feedback.md)에 전송/저장/인증/보관 경계를 기록하고, 이용 안내에 한국어·번체 작성 창과 웹앱 개발·번역·디자인의 AI 활용 고지를 추가했다. 전송 전 개인정보 주의, 개별 답장 없음, Cloudflare 보관·90일 조회 후 정기 삭제, Codex 등 AI 검토와 검토 사본 가능성을 안내한다. 기존 박수·연락처·타이머·차 느낌 저장값은 유지한다.

- 최대 1,000 Unicode 코드 포인트와 요청 8KiB, 원문 보존, 창 닫기/실패/시간 초과 시 입력 유지, 중복 제출 차단, 동일 UUID 재시도, 성공 확인 뒤 비우기, IME 중 제출 차단·포커스 순환을 구현했다. Worker는 증가하는 정수 커서, 별도 읽기 토큰, 일일 삭제와 날짜별 HMAC IP 키의 최소 빈도 제한을 사용한다.
- Python 조회 도구는 토큰을 URL·출력에 넣지 않고 모든 리디렉션을 거부한다. 페이지/시각 검증, 파일 잠금·원자 저장, 로컬 만료 사본 정리, 미보고 목록과 명시적 보고 확인을 제공한다. 의견 내용은 실행 지시가 아닌 외부 데이터로 다룬다. 비밀 설정과 로컬 사본은 Git에서 제외한다.
- `tools/preview_server.py`는 앱 공개 자산만 제공하고 `/api/messages`만 로컬 Worker로 중계한다. 운영 origin 이외에서는 운영 의견함으로 전송하지 않는다. 사용자용 8138 서버를 이 도구로 교체했고 현재 로컬 8787 Worker와 연결했다. 사용자 페이지 강제 새로고침·저장값 변경은 하지 않았다.
- 공지 네 항목의 다듬은 한국어·번체 문구를 카탈로그에 준비했다. 제목은 명사형, 한국어 본문은 ‘-습니다’체로 쓰고 카드 조판·임시저장 내용은 넣지 않았다. `planned`는 비우고 준비 중 문구를 표시한다. 운영 서버 준비 전이므로 `releases`는 비어 있으며 실제 배포 묶음 연결·공개는 남아 있다. 새 런타임 자산을 포함해 SW를 v71로 갱신했다.
- **검증:** 기존 Node 67개, Worker 6개, Python 조회 도구 12개 통과. 실제 브라우저 의견함 31개와 기존 회귀 137개(현지화/5초 우림 49, UI 60, 공지 분류 11, 캐시 17) 통과. 한글·번체 실제 UI 전송 → 로컬 프록시 → 실제 D1 저장 → 관리자 API·Python 조회를 확인했다. 실제 로컬 D1 만료 행의 조회 제외·scheduled 삭제, 잘못된 읽기 토큰 거부, 프록시 비공개 경로/외부 Origin 차단과 현재 자산 응답을 확인했다. 테스트에서 운영 API에 의견·박수를 보내지 않았다.
- 375×660 한국어·번체 실제 렌더와 44px 조작을 확인했다. 합성 IME·브라우저 검증을 실제 iPhone 키보드·VoiceOver 결과로 확대하지 않는다. 로그·스크린샷은 Git 외부 `…/01a05671-a428-7392-940b-09ee1a7816ad/creator-feedback/`에 보존한다.

**남은 단계:** iPhone 의견함 입력·키보드 확인, 운영 DB migration·비밀키·Worker 활성화/배포, 서버 확인 후 공지 게시·GitHub Pages 배포와 HTTPS Safari/PWA 검증. 운영 설정은 `FEEDBACK_ENABLED=false`로 유지했다. `main` 반영·원격 푸시·운영 배포·정기 자동화 등록은 수행하지 않았다.

서버·조회 도구 `fb40a8d`와 UI·설계·검증 `f469c14`를 `e44609e`에서 `codex/tw-readiness`에 병합했다. 통합 후 Node 67개·Worker 6개·Python 12개, 실제 5초 우림을 포함한 현지화 49개·의견함 31개를 다시 통과했다. 로컬 조회 도구가 새 번체 의견 한 건만 가져오고 다음 실행에서는 0건을 가져오면서 미보고 4건을 유지하는 것도 확인했다.

<a id="help-reduced-motion"></a>

### 2026-08-31 — 이용 안내에 기기의 동작 줄이기 고지

사용자가 iPhone에서 ‘동작 줄이기’가 켜져 있음을 확인하고 기존 연출 생략 동작을 수용했다. 이용 안내의 기본 펼침 ‘사용법’ 아래에 한국어·번체 안내를 추가했다. 물 붓기·완료 직전 동심원·완료 플래시 등의 생략, 남은 시간·안내 문구 유지, 앱의 소리 설정에 따른 알림을 설명한다. 기기 설정 변경을 요구하지 않으며 타이머·모션·저장 로직은 변경하지 않았다. 캐시된 도움말 카탈로그도 갱신되도록 SW는 v70으로 올렸다.

- Node 67개, 기존 현지화 회귀 49개(실제 5초 우림·출탕·다음 포 포함), v70 캐시 응답 15개를 통과했다. 새 테스트는 추가하지 않았다.
- 375×660 브라우저에서 한국어·번체 안내의 실제 렌더와 기본 펼침·가로 넘침 없음을 확인했다. 스크린샷·DOM·검증 로그는 Git 외부 `…/01a05671-a428-7392-940b-09ee1a7816ad/help-motion/`에 보존했다. 이 결과를 실제 iPhone 안내 재검토나 번체 현지인 검수로 대신하지 않는다.

기능 커밋 `1d91d23`을 `829559b`에서 `codex/tw-readiness`에 병합했다. 사용자용 8138의 도움말·두 언어 도움말 카탈로그·SW 응답이 현재 파일과 일치함을 확인했다. 사용자 페이지를 강제 새로고침하지 않았으며 `main` 반영·원격 푸시·배포는 하지 않았다.

<a id="duplicate-timer-notices"></a>

### 2026-08-31 — 공지·메뉴·언어·종료 확인의 중복 우림 안내 제거

사용자가 공지에서 남아 있는 우림 상태 안내를 확인하고 나머지 중복 안내의 제거를 요청했다. [메뉴·공지 계약](design-navigation.md#announcements)과 [창 닫기·초안 계약](design-tea-feeling.md#modal-accessibility)에 따라 공지·언어 선택·메뉴의 시각 안내와 `타이머로 돌아가기` 버튼을 제거하고, 종료 확인은 삭제 경고·취소·종료 버튼만 남겼다. 타이머 본체의 실제 완료/출탕 안내와 진행 동작·사용자 데이터는 바꾸지 않았다.

- 공지는 X·Escape·배경 닫기와 메뉴 버튼으로 포커스 복귀를 유지한다. 공지·느낌·카드는 최상위 창의 숨긴 짧은 상태 알림만 갱신하며 완료가 창을 닫거나 초점을 이동시키지 않는다. 메뉴·언어 선택의 기존 닫기/포커스와 종료 취소의 초안 보존은 유지한다. 안내용 CSS·이벤트·양 언어 미사용 키 5개를 제거하고 SW를 v69로 갱신했다.
- `node --test tests/*.test.cjs` 67개와 기존 브라우저 회귀 226개(현지화 49·접근성 36·초안/종료 66·UI 60·캐시 15)를 통과했다. 기존 검사의 복귀 버튼/종료 상태 문구 전제를 X·삭제 경고·포커스·초안 유지 기준으로 갱신했고, 삭제된 문구를 찾던 초안 검사의 최초 실패와 수정 후 통과 로그를 구분해 보존했다. 새 테스트 파일은 추가하지 않았다.
- 375×660에서 실제 5초 우림 뒤 한국어/번체 공지, 메뉴·언어 선택, 번체 종료 확인의 렌더를 확인했다. 종료 취소 뒤 번체 원문 입력도 유지됐다. 로그·스크린샷은 Git 외부 `…/01a05671-a428-7392-940b-09ee1a7816ad/notice-cleanup/`에 보관한다. 사용자 8138 페이지는 강제 새로고침하지 않았으며 다음 로드부터 변경을 제공한다. 실제 iPhone 재확인·배포 후 HTTPS 검증은 별도다.

기능 커밋 `d987352`를 `d0695cd`에서 `codex/tw-readiness`에 병합했다. 통합 후 Node 67개·현지화/실제 5초 우림 49개를 다시 통과했고, 아이폰용 8138 서버의 메인·두 언어 카탈로그·SW 응답이 현재 파일과 일치함을 확인했다. `main` 반영·원격 푸시·배포는 하지 않았다.

<a id="tw-08-local-final-check"></a>

### 2026-08-31 — TW-08 배포 전 로컬 최종 점검

사용자 요청으로 통합 `3750749`(런타임 SW v68)의 최종 점검을 수행했다. 사용자용 LAN HTTP 8138과 분리된 `127.0.0.1:8134`에서 검사했고 사용자 미리보기의 저장값·페이지는 조작하지 않았다. 배포 후 HTTPS 실기기 검증 방침은 유지한다.

- `node --test tests/*.test.cjs` **67/67 통과**. 실제 브라우저의 기존 harness **10개·404개 검사 통과**: 접근성 36, 현지화 49, 초안/종료 66, 인라인 단어 37, 번체 21, 비동기 세션 9, 카드 100, 공통 UI 60, 공지 분류 11, 캐시 15. 실제 5초 우림·출탕·다음 포, 언어 전환·공유 지연·저장 실패·기존 설정/사용자 원문 보존·320/375/768px 화면을 확인했다. 공유/오디오/IME 대역을 실제 기기 결과로 확대하지 않는다.
- 카드 8종(두 언어의 짧은/긴 내용, 혼합, 빈 기록, 두 언어의 외부 글꼴 차단) 원본 PNG·전체 텍스트를 보존했다. PNG 680×900 크기, 미리보기와 공유 PNG 동일성, 2/2/3/5 한도·생략·원문 불변을 통과했고 한국어/번체 짧은 카드·긴 번체·한국어 폴백 PNG를 직접 확인했다.
- 세 HTML의 로컬 참조·인라인 JS, 런타임 JS 12개 구문, precache 29개 항목의 파일 존재·`/tea-timer/` 하위 경로 해석, manifest start_url/scope, HTTPS 참조를 점검했다. 별도 정적 검토와 직접 검사에서 해당 범위의 코드상 배포 차단 오류는 발견하지 못했다. 실제 Pages 설정·배포 응답은 아직 확인하지 않았다.
- 테스트 서버 8134만 중단해 캐시에서 한국어 앱 재실행→5초 우림 완료→출탕/다음 포 버튼→번체 전환·기록 보존→혼합 카드 미리보기→번체 설정·이용 안내 탐색을 확인했다. 이후 같은 명령으로 테스트 서버를 재시작했고 8138은 계속 유지했다. **앱 서버 중단 시 캐시 복구 검증**이며 외부 폰트 네트워크까지 끊은 iPhone 비행기 모드·설치형 PWA 검증은 아니다. 별도 카드 fixture의 글꼴 차단 결과와 구분한다.
- **배포 준비 잔여:** `releases: []`와 ‘언어 선택과 메뉴를 준비 중’인 예정 공지는 이번 배포 내용으로 교체해야 한다. 한국어/번체 공지 초안을 Git 외부 `tw08-final/release-notice-draft.md`에 준비했고, 아직 앱 공지에 적용·게시하지 않았다. 실제 날짜·배포 묶음·카탈로그·SW 갱신과 관련 회귀는 배포 준비 작업으로 남긴다.
- 로그·정적 검사 스크립트·8종 PNG/텍스트·서버 중단 화면·`summary.json`은 Git 외부 `…/01a05671-a428-7392-940b-09ee1a7816ad/tw08-final/`에 보관한다. 사용자에게 iPhone 13 mini의 실제 터치/키보드·완료음·단어/초안 유지와 iOS 버전 확인을 요청했으며 결과는 대기다. 기기 OS 공유·VoiceOver·PWA 설치·완전 오프라인·기존 설치 업데이트는 미검증이다. 런타임 코드는 수정하지 않았고 `main` 반영·원격 푸시·배포도 하지 않았다.

<a id="tw-08-pages-validation"></a>

### 2026-08-31 — HTTPS 실기기 검증을 GitHub Pages 배포 후로 변경

사용자가 구현을 마친 뒤 GitHub Pages에 배포하고 그 HTTPS 환경에서 검증하는 순서를 선택했다. [검증 시점 계약](design-localization.md#pages-device-validation)에 따라 임시 인증서 설치·신뢰는 진행하지 않는다. 로컬 회귀·공지/버전·배포 준비를 먼저 수행하고, 실제 Safari/PWA의 공유·오프라인·업데이트·오디오 등은 배포 후 필수 확인으로 남긴다. 배포 성공만으로 TW-07/08 또는 전체 묶음을 완료 처리하지 않는다. 기존 공개 설치 업데이트를 검사할 수 있도록 배포 전에 구버전 설치·캐시·테스트 설정을 보존한다.

이전에 시작한 임시 HTTPS 8838·인증서 다운로드 8139 프로세스를 종료하고 서버 개인 키를 제거했다. 공개 인증서·서버 설정·검증 로그와 `decommissioned.json`은 Git 외부 작업 폴더에 보존한다. 기존 HTTP 8138 미리보기는 계속 실행하며, 맥/아이폰의 인증서 신뢰 설정·사용자 데이터는 바꾸지 않았다.

AGENTS·로드맵·실행 계획·다국어 설계의 검증 시점을 맞췄다. 저장소는 문서만 변경했으며 런타임·SW v68은 그대로다. 로컬 문서 링크 대상 171개·결정 앵커·전체 diff·`git diff --check`와 임시 서버 종료·개인 키 제거를 확인했다. 런타임 테스트는 반복하지 않았다. 이 단계에서 `main` 반영·원격 푸시·GitHub Pages 배포는 하지 않았다.

<a id="tw-08-https-preparation"></a>

### 2026-08-31 — TW-08 LAN HTTPS 환경 준비

카드 배치 최종 승인 뒤 다음 단계 진행 요청에 따라 [실기기 검증 환경·절차](design-localization.md#device-validation-scope)를 준비했다. 앱 전용 HTTPS 8838과 공개 인증서 한 파일만 제공하는 HTTP 8139를 맥의 LAN 주소에 바인딩했다. 기존 HTTP 8138은 유지한다. 테스트 CA/서버 인증서는 7일간 유효하며 발급용 CA 개인 키는 단일 서버 인증서 발급 뒤 제거했다. 개인 키·문서·테스트·Git 경로는 제공하지 않는다. 인증서·서버·로그는 Git 외부 `…/01a05671-a428-7392-940b-09ee1a7816ad/tw08-https/`에 둔다.

- `python3 …/tw08-https/verify.py`: 54개 검사 통과. 이 프로세스에만 테스트 CA를 지정하여 TLS/IP를 검증했고, 앱 30개 경로의 현재 파일 일치, 주요 3개 MIME, 양쪽 서버의 비공개 경로 16개 차단, 공개 DER 인증서 일치, 기본 신뢰의 인증서 거부, 발급용 개인 키 제거·서버 키/디렉터리 권한을 확인했다. `verification.txt`와 `certificate-info.json`에 근거를 보관했다. 시스템 인증서 신뢰는 바꾸지 않았다.
- HTTPS는 기존 HTTP·공개 배포와 다른 origin이다. 기존 사용자 데이터를 삭제·이관하지 않는다. Safari/PWA 두 언어의 입력·실제 PNG/OS 공유·오디오 복귀·오프라인·구버전 업데이트 확인 순서를 기록했다. 공유 취소는 자동 다운로드가 아니라 취소 유지이며, 명시적 세션 종료·재실행의 초안 소멸은 기존 계약대로 검사한다.
- 런타임 기준은 기존 통합 상태 `e5197ad`(SW v68) 그대로다. 저장소에서는 문서만 변경했으므로 SW 버전을 올리거나 기존 런타임 검증을 반복하지 않았다. 로컬 문서 링크 대상 155개와 전체 diff·`git diff --check`를 확인했다.
- 기기 인증서 설치·신뢰는 사용자 확인 대기다. 실제 Safari HTTPS 접속·PWA 설치·공유/저장·오프라인·오디오·이전 버전 업데이트는 아직 미검증이다. 공개 배포·터널·방화벽 변경·`main` 반영·원격 푸시는 하지 않았다.

<a id="card-spacing-centered"></a>

### 2026-08-31 — 카드 여백 ③ 선택·컴포넌트 간격 조정

사용자가 짧은 카드 처리에서 **③ 같은 크기 안에서 내용 모으기**를 선택하고, 컴포넌트별 수직 간격 및 구분선 위아래의 조금 더 넓은 여백을 요청했다. [확정 설계](design-tea-feeling.md#card-height-options)에 따라 680×900·글자 크기·표시 한도·원문은 유지하고 본문을 실제 줄 수에 맞춰 묶어 가운데 배치한다. 메모 바탕과 왼쪽 선도 실제 메모 줄 수만큼만 차지한다. 기본 간격은 22–24px, 구분선 바로 위·아래는 28px이며 포별 기록이나 단어가 없으면 빈 영역을 예약하지 않는다. 하단 날짜·제품명 위치는 유지한다. 가장 긴 2/2/3/5 구성에서도 본문은 y64–791에 들어간다.

`codex/tw-07-card-spacing`에서 공통 Canvas 배치만 수정하고 SW를 v68로 갱신했다. 기존 Node 67개·카드 100개·접근성/실제 5초 우림 36개·캐시 15개를 통과했다. 두 언어·혼합 원문·빈 기록·외부 글꼴 차단의 실제 PNG, 미리보기와 PNG 일치 및 텍스트/저장 원문 보존을 재확인했다. 새 테스트를 추가하지 않고 기존 검증을 사용했다. Git 외부 `…/01a05671-a428-7392-940b-09ee1a7816ad/card-spacing/`에 PNG·스크린샷·로그를 보관한다. 기존 배치의 iPhone 사용자 확인과 이번 여백 조정의 로컬 검증은 구분하며 이번 변경의 실기기 재확인은 남아 있다. 원격 푸시·배포는 하지 않았다.

기능 커밋 `edb706c`를 `0ac81d2`에서 `codex/tw-readiness`에 병합했다. 통합 상태에서 Node 67개와 접근성/실제 5초 우림 36개를 다시 통과했다.

2026-08-31 사용자가 여백 보완을 포함한 현재 카드 배치를 최종 승인했다. 디자인 검토 완료를 기록했으며, 실기기 기능·공유·PWA 검증이나 배포 완료를 뜻하지 않는다.

<a id="card-height-review"></a>

### 2026-08-31 — iPhone 카드 표시 확인·짧은 카드 여백 시안

사용자가 카드 미리보기를 확인한 뒤 iPhone 13 mini에서도 잘 표시된다고 보고했다. [검증 환경 기록](design-localization.md#device-validation-scope)에 사용자 확인 범위와 미확인 항목을 구분했다. 짧은 기록의 여백을 줄이는 [세 후보](design-tea-feeling.md#card-height-options)를 Git 외부 비교 시안으로 보존한다. 아직 선택되지 않은 검토안이며 앱 코드·PNG 크기·저장값·서비스워커는 바꾸지 않았다. 시안의 두 언어×세 길이×세 후보 18개 조합에서 폭 680·높이 상한 900·메모/하단 비중첩을 확인했고 375px 화면에서도 가로 넘침 없이 배치됨을 확인했다. 비교 화면과 점검 로그는 Git 외부 `card-height-review/`에 보관한다.

<a id="tw-07-balanced-card"></a>

### 2026-08-31 — TW-07 A안 카드 조판 구현·로컬 검증

앞선 작성 시트·단어 칩·이용 안내/설정 이동 정리는 사용자 승인을 받았다. 이어 승인된 [A안](design-tea-feeling.md#card-layout-draft)을 `codex/tw-07-balanced-card`에서 구현했다. TW-07의 실제 iPhone 글꼴·화면 확인과 TW-08은 별도 대기로 유지한다.

- 680×900 고정 카드의 제목 2줄·포별 시간 2줄·단어 3행·메모 5줄, 하단 좌측 날짜/우측 제품명을 적용했다. 단어는 실제 글자 폭으로 배치하고 생략 개수도 행 안에 넣는다. 한글·번체·결합 문자·emoji의 글자 경계를 보존하며 긴 단어 하나는 칩 안에서만 생략한다. A안 목업대로 자동 장식 emoji는 제목에 덧붙이지 않고 사용자가 적은 emoji는 유지한다.
- `scripts/card-layout.js`의 공통 조판으로 DOM 안 Canvas 미리보기를 그리고, 같은 표시 결과를 접근성 텍스트로 제공한다. 공유 시 미리보기 픽셀과 메타데이터를 즉시 복제하여 늦은 글꼴 로딩·우림 완료·언어 변경과 분리한다. 열린 카드에서 우림이 완료되면 포 수와 시간도 함께 갱신한다. 제목/단어/메모·설정 저장·전체 텍스트 내보내기 원문은 건드리지 않았다.
- 일반·굵은 글꼴 준비와 폴백 후 재렌더를 적용했다. 새 런타임 모듈을 SW v67에 포함하고 앱 전용 8138 서버의 허용 자산 목록도 재시작으로 갱신했다. 제품명 `차 한 잔의 시간`과 `TIME FOR TEA` 표식은 고정 제품명 계약을 유지한다.
- Node `node --test tests/*.test.cjs` 67개, 전용 카드 harness 100개(한국어/번체 짧은·긴 기록, 혼합 입력, 빈 기록, 두 언어 외부 글꼴 차단), 번체 회귀 21개, 초안/실제 5초 우림 회귀 66개, 접근성 36개, 언어 전환/지연 PNG 회귀 49개, SW 캐시 15개를 통과했다. 공유 파일은 미리보기와 PNG 데이터 URL이 동일한지 검증했고 다운로드 폴백·전체 텍스트·원문/설정 불변·320/375px 조작도 확인했다. 기존 Canvas 캡처 검사는 미리보기 렌더 시점에 맞추고 제목의 자동 emoji 가정은 제거했다.
- Git 외부 `…/01a05671-a428-7392-940b-09ee1a7816ad/tw07/`에 두 언어 375×660 실제 앱 예시 스크린샷, 680×900 원본 PNG, 전체 텍스트·로그를 보관한다. 로컬 Gemma E4B의 메모 줄 수 의심은 직접 이미지·5개 기준선·harness 결과로 반증했고 26B 텍스트 검토도 이를 확인했다. 모델 판단만으로 통과 처리하지 않았다.
- 기능 커밋 `a2564b0`을 `1579238`에서 `codex/tw-readiness`에 병합했다. 통합 상태에서 Node 67개·카드 100개·접근성/실제 5초 우림 36개를 다시 통과했고 8138의 새 모듈 응답과 저장소 파일이 일치함을 확인했다.
- Mac 데스크톱 브라우저 검증과 iPhone 13 mini Safari/설치형 PWA를 구분한다. 실제 모바일 글꼴·OS 공유 시트·VoiceOver·오프라인 실행/기존 설치 업데이트는 미검증이다. 원격 푸시·`main` 반영·배포·현지인 문구 검수는 하지 않았다.

<a id="subpage-navigation-cleanup"></a>

### 2026-08-31 — 이용 안내·설정의 상호 이동 아이콘 제거

사용자 요청으로 이용 안내 우상단의 설정 아이콘과 설정 우상단의 이용 안내 아이콘을 제거했다. 각 화면에는 좌상단 `← 처음으로`만 남기고, 메인 메뉴의 이용 안내·설정 진입은 유지한다. 관련 미사용 번역 키와 아이콘 전용 스타일도 제거했다. [이동 설계](design-navigation.md#2-전역-메뉴와-맥락-진입)를 갱신했으며 저장값·타이머·초안 동작은 바꾸지 않았다.

Node 61개, 공통 UI 320/375/768px 60개, SW v66 캐시 응답 14개를 확인했다. 두 페이지의 375×660 실제 렌더와 상단 링크가 `index.html` 하나뿐임을 확인했으며, Git 외부 `…/01a05671-a428-7392-940b-09ee1a7816ad/subpage-navigation/`에 스크린샷·로그를 보관했다. 기존 UI 검사의 우측 기준선은 제거된 버튼 대신 상단 컨테이너를 측정하도록 맞췄고 새 테스트는 추가하지 않았다. 기능 브랜치는 `codex/subpage-navigation-cleanup`이며 `main` 반영·원격 푸시·배포는 하지 않았다.

<a id="feeling-chip-spacing"></a>

### 2026-08-31 — 차 느낌 단어 칩의 배경·행 간격 정리

설정 단어장과 같은 32px 배경·.85rem/20px 글자·좌우 8px 여백을 차 느낌의 선택 칩에도 적용했다. 실제 조작 영역 44px 안에서 배경만 위아래 6px씩 줄이고, 추가 세로 gap은 5.6→0px로 바꿨다. 배경 축소 후 같은 줄 간격을 그대로 두면 생길 17.6px 공백을 12px로 제한하고 터치 영역은 겹치지 않게 유지한다. `+ 단어 추가`의 점선도 같은 크기로 맞췄다. 기존 선택색·글로우·글자 굵기·키보드 포커스, 긴 원문의 줄바꿈은 유지한다. 입력창의 추가/취소 버튼·설정 화면·카드 PNG 배치는 바꾸지 않았다. 상세는 [칩 여백 계약](design-tea-feeling.md#42-오늘-떠오른-단어-감각-태그)을 따른다.

375×660 한국어 기본 목록에서 단어 영역 높이는 약 193→176px로 줄었다. 한국어 선택/해제, 배경 바깥 투명 영역의 적중 대상, 버튼의 최소 44px와 영역 간 비중첩을 확인했다. 번체 375px·320px 실제 렌더에서 가로 넘침 없이 배경·행 간격이 유지됐다. 스크린샷·로그는 Git 외부 `…/01a05671-a428-7392-940b-09ee1a7816ad/feeling-chips/`에 보존한다. 런타임 변경에 맞춰 SW v65를 준비했다. 실기기 터치/키보드·PWA 업데이트·배포 완료를 뜻하지 않는다.

검증: `node --test tests/*.test.cjs` 61개, 전용 8134의 `inline-tags.html` 37개·`accessibility.html` 36개·v65 캐시 응답 14개 통과. 실제 5초 우림·초안/IME·단어 추가/설정 저장·긴 단어·44px·PNG를 기존 회귀로 확인했다. 순수 스타일 변경에 새 자동 테스트는 추가하지 않았으며, 구문·문서 링크·전체 diff를 확인했다.

기능 커밋 `62f6a3a`를 `fa645bf`에서 `codex/tw-readiness`에 병합했다. 통합 상태에서 Node 61개·인라인 단어/실제 우림 회귀 37개를 다시 통과했다. `main` 반영·원격 푸시·배포는 하지 않았다.

<a id="feeling-sheet-spacing"></a>

### 2026-08-31 — 차 느낌의 중복 안내 제거·상단 여백 축소

사용자 화면 검토에 따라 차 느낌 작성 시트와 카드 미리보기에서 타이머 상태 안내 문단과 `타이머로 돌아가기` 버튼을 제거했다. 기존 안내는 창 뒤에서 계속되는 우림을 인지하게 하려는 TW-02 장치였으나, 완료 기록·X와 중복되어 작성 화면을 늘렸다. X·Escape의 단계별 복귀, 초안·타이머·포커스 보존은 유지하고 최상위 창의 스크린리더 상태 알림만 시각적으로 숨겨 제공한다. 공지·종료 확인 안내는 유지한다.

상단은 드래그가 구현되지 않은 장식 손잡이를 제거하고 패딩 20→12px, 제목 행 아래 간격 17.6→8px로 줄였다. 시트의 최대 높이도 88dvh에서 화면 상단 안전 간격을 제외한 높이로 늘렸다. 실제 1280×720 번체 화면에서 시트 상단부터 제목까지 약 50→22px, 제목의 화면 y좌표는 약 136→80px로 줄었고 하단 안내까지 내부 스크롤 없이 보였다. 짧은 내용의 높이는 강제로 채우지 않는다. 상세 정책은 [차 느낌 설계](design-tea-feeling.md#modal-accessibility)를 따른다. PNG 배치와 TW-07 A안은 수정하지 않았다.

- 검증: `node --test tests/*.test.cjs` 61개. 전용 8134에서 접근성/X 복귀 36개, 현지화 49개, 번체 21개, 초안/종료 66개, v64 캐시 응답 14개 통과. 실제 5초 우림 완료·출탕·다음 포, 중첩 창의 X/Escape·포커스·초안, 기존 PNG·언어/입력 보호를 확인했다. 공지의 기존 복귀 경로와 종료 확인도 회귀했다.
- 1280×720·375×660·320×660 렌더를 확인했다. 375px 한국어·번체에서 상단 여백 16px, 제목 행 시작 29px, 320px에서 가로 넘침 없음을 확인했다. 스크린샷과 로그는 Git 외부 `…/01a05671-a428-7392-940b-09ee1a7816ad/feeling-spacing/`에 보존한다. 모바일 실제 키보드·스크린리더·PWA/설치 캐시 업데이트는 미검증이다. 런타임 변경에 맞춰 SW v64를 준비했으며 공개 배포하지 않았다.
- 기능 커밋 `ab12fff`를 `6c65d36`에서 `codex/tw-readiness`에 충돌 없이 병합했다. 통합 뒤 Node 61개와 접근성/X·타이머·PNG 회귀 36개를 다시 통과했다. 문서 링크·구문·diff도 확인했다. `main` 반영·원격 푸시·배포는 하지 않았다.

<a id="tw-06-traditional-chinese"></a>

### 2026-08-31 — TW-06 번체 문구와 언어별 독립 기본 단어

승인한 초안을 메인·설정·이용 안내·공지·차 느낌·접근성·텍스트/PNG 공유에 적용했다. 동양식 다호는 번체에서 `茶壺`로 표시하고 제품명은 유지한다. 초안 수명은 한 포가 아닌 이번 차 세션 전체로 설명한다. 현지인 문구 검수는 사용자 결정대로 배포 후 진행한다.

- 한국어와 번체는 각각 기본 15개를 제공한다. 기존 25개 한국어 builtin ID의 원문을 고정하고 번체에는 별도 `tw.` ID를 부여했다. 저장된 목록·빈 목록·legacy·custom·선택 단어는 언어 전환으로 바꾸지 않는다. 목록을 편집한 적이 없을 때만 현재 화면 언어의 기본 목록을 보여준다. 다른 탭의 언어 적용 유예나 언어 저장 실패 중에도 첫 단어 추가는 보이는 목록을 기준으로 저장한다. 상세 근거·15개 원문·복원 계약은 [다국어 설계 §7.4](design-localization.md#locale-default-vocabulary)를 따른다.
- 세 번체 카탈로그를 각 페이지와 SW 사전 캐시에 추가하고 v63으로 갱신했다. 이전의 “번체는 다음 단계” 안내를 제거했다. 동적 제목 입력의 접근성 이름도 언어 전환을 따른다. TW-07 A안 카드 조판은 이번 변경에 포함하지 않았다.
- Node: `node --test tests/*.test.cjs` **61개 통과**. 두 언어의 전체 248개 고유 키·치환 변수 일치, 의도한 제품명/기존 단어 외 한국어 혼용 없음, 기존 데이터·손상/빈 목록·저장 실패·현재 UI locale에 따른 추가/복원을 확인했다.
- 브라우저: 전용 `http://127.0.0.1:8134/tests/`에서 `traditional-chinese.html` **21개**, `localization.html` **49개**, `inline-tags.html` **37개**, `draft-boundaries.html` **66개**, `accessibility.html` **35개**, `session-async.html` **9개**, `announcements.html` **11개**, `ui-consistency.html` **60개**, `offline-assets.html` **14개** 통과. 번체 전환·원문·현재 UI 기본값·실제 5초 우림·완료/출탕/다음 포·IME/공유 유예·초안 보호·44px·작은 화면·캐시 응답을 확인했다. UI 검사는 합성 입력과 플랫폼 대역을 포함하며 실기기 결과와 구분한다.
- 산출물: 375×660 번체 작성 시트 실제 렌더, 번체 원문 텍스트 복사와 실제 680×900 PNG를 확인했다. `font-preflight.html`에서 한국어·번체·이모지 혼합 입력의 정상/외부 글꼴 차단 PNG 2종을 생성했다. Git 외부 `/Users/kioku/.codex/visualizations/2026/08/31/01a05671-a428-7392-940b-09ee1a7816ad/tw06/`에 로그·원본 PNG·스크린샷을 보관한다. 이미지 보조 검토는 E4B의 일부 OCR 오류·JSON 형식 실패를 기록하고 실제 이미지/Canvas 문자열과 대조했다. 26B는 그 텍스트 근거만 검토했으며 이미지 직접 판독으로 취급하지 않는다.
- 이번 검증에서는 실제 iPhone·OS IME·스크린리더·OS 공유 시트·오디오 출력·설치형 PWA·서버 중단 오프라인·기존 설치 캐시 업데이트를 완료하지 않았다. 긴 입력의 A안 조판·전체 기기 글꼴 보장은 TW-07·08에 남긴다. Mac/iPhone용 8138 서버는 앱 런타임 파일만 허용하고 문서·테스트·`.git`은 제공하지 않는다. LAN HTTP 확인과 HTTPS가 필요한 검증은 [기기 검증 경계](design-localization.md#device-validation-scope)를 따른다.
- 기능 커밋 `d204174`를 `12a2e81`에서 `codex/tw-readiness`에 충돌 없이 병합했다. 기능 브랜치와 동일한 런타임·테스트를 통합했으며, 통합 후 Node 61개·현지화 49개(실제 5초 우림 포함)·번체 원문/PNG/작은 화면 21개를 다시 통과했다. `codex/tw-06-traditional-chinese`는 이력용으로 보존한다. JS 24개 구문·정적 번역 속성 114개·로컬 문서 링크 192개·전체 diff도 확인했다. `main` 반영·원격 푸시·배포는 하지 않았다.

<a id="feeling-inline-tags"></a>

### 2026-08-31 — 차 느낌 작성 중 단어 추가

사용자 요청에 따라 `+ 단어 추가` 입력창을 차 느낌 시트에 넣었다. 추가하면 이번 카드에 즉시 선택하고 설정에도 저장하며 우림 종료 뒤에도 단어 목록에는 남는다. 중복 custom은 재사용하고 builtin과 같은 이름이어도 독립적으로 유지한다. 저장 실패 시 입력을 남기고, 미확정 입력은 초안으로 보호한다. 상세는 [입력 계약](design-tea-feeling.md#42-오늘-떠오른-단어-감각-태그)을 따른다.

- 메인·설정이 공통 `addTag`로 최신 저장값에 추가하고 기존 원문·빈 목록·알 수 없는 필드/태그 속성을 보존한다. 다른 탭의 목록 변경은 현재 선택·메모·조합 입력을 유지하며 반영한다. 설정의 삭제 실패는 해당 단어를 목록에 남겨 명시적으로 재시도하게 했다. 실패한 과거 목록으로 다른 탭의 추가를 덮어쓰는 경계를 제거했다.
- 검증: `node --test tests/*.test.cjs` 46개. 전용 8134 origin에서 `tests/inline-tags.html` 37개, 현지화 49개, 초안/종료 66개, 접근성 35개, 비동기 세션 9개, 공통 UI 60개, 캐시 자산 11개 통과. 단어 추가/설정 동기화·실패/재시도·IME 최종 입력·중복·손상/빈 목록·초안 보호·실제 5초 우림·텍스트 및 680×900 PNG를 확인했다. 375×660·320×660 화면에서 실제 Enter/Escape 조작과 렌더를 확인하고 입력창·선택 상태·PNG 원본을 보존했다. JS 구문·정적 번역 키·변경 설계 링크·diff도 확인했다.
- 산출물: Git 외부 `/Users/kioku/.codex/visualizations/2026/08/31/01a05671-a428-7392-940b-09ee1a7816ad/inline-tags/`. UI 검토 중 테스트 확인창에서 브라우저 제어가 지연되어 별도 테스트 탭으로 이어서 검증했다. 사용자 검토용 8135 origin은 변경하지 않았다.
- 기존 shell 자산만 변경해 배포 준비 중인 SW v62를 유지한다. 서버 중단 오프라인·기존 설치 캐시 갱신·실제 모바일 키보드/OS IME·스크린리더·공유 시트·PWA는 이번 검증에 포함하지 않았다. 테스트의 합성 IME·Web Share/clipboard 대역과 실제 기기 동작을 구분한다.
- 기능 커밋 `419172c`를 `74a3c00`에서 `codex/tw-readiness`에 병합했다. 충돌 없이 동일한 런타임·테스트를 통합했고 통합 상태에서 Node 46개, 인라인 작성·설정·5초 우림·PNG 회귀 37개를 다시 통과했다. `codex/feeling-inline-tags`는 이력용으로 보존한다. TW-06 전체 번체 번역·언어별 기본 단어 전환과 TW-07 카드 배치 초안은 이번 범위 밖이다. `main` 반영·원격 푸시·배포는 하지 않았다.

<a id="tw-05-close"></a>

### 2026-08-31 — TW-05 한국어 공통 기반·UI 마감

사용자가 한국어 실제 화면 검토와 추가 수정을 마친 뒤 TW-05 마감을 확인했다. 공통 번역·언어/태그 호환, 언어 선택·메뉴·공지 읽기, 기본 15개와 설정 배치, 공통 UI 기준, 이용 안내·여운 명칭까지 기능 범위를 완료했다. 개별 구현·검증 이력은 아래에 보존한다. 기능 브랜치 `codex/tw-05-localization-foundation`의 `7c7cf5b`부터 `0837981`까지 포함하며, 별도 작업에서 확정한 감각 단어 조사 문서 `9b20bb8`도 출처 그대로 유지한다.

- 기능 브랜치에서 Node 37개, 현지화 49개, 공지 11개, 공통 UI 60개를 통과했다. 이전 접근성·초안·비동기 세션 회귀와 실제 PNG·글꼴 사전 렌더·로컬 캐시 검증 기록도 아래에 있다.
- 통합 상태: 완료 문서 커밋 `3647e30`까지 포함해 `d2304c5`에서 `codex/tw-readiness`에 충돌 없이 병합했다. 통합 런타임·테스트는 기능 브랜치와 동일하며, 병합 후 아래 검증을 다시 통과했다. 기능 브랜치는 이력 확인용으로 보존한다.
- 통합 검증: `node --test tests/*.test.cjs` 37개. 전용 `http://127.0.0.1:8134`에서 `tests/localization.html` 49개, `tests/draft-boundaries.html` 66개, `tests/accessibility.html` 35개, `tests/session-async.html` 9개, `tests/announcements.html` 11개, `tests/ui-consistency.html` 60개, `tests/offline-assets.html` 11개 캐시 응답을 확인했다. 실제 5초 우림·정상 완료·출탕·다음 포, 초안/설정 원문 보존, 언어 적용 유예, 포커스·44px·320/375/768px 화면, 680×900 PNG 생성과 공유 대역을 재확인했다. JS 구문·정적 번역 키 189개·문서 링크 176개·전체 diff도 통과했다.
- 통합 로그와 전/중/후 우림 PNG는 기존 Git 외부 `tw05` 산출물 폴더에 `integrated-*` 이름으로 보관했다. 공유 UI·OS 입력·실제 오디오 출력은 테스트 대역과 구분한다. 이번 통합에서는 캐시 응답만 확인했으며 서버 중단 오프라인·글꼴 차단 사전 렌더를 재실행하지 않았다. 박수 전송과 사용자 검토용 8135 origin의 데이터 변경·탭 재로드는 하지 않았다.
- TW-04는 보류, 다음 항목 TW-06은 미착수다. 번체 전체 문구는 아직 한국어 폴백이며 TW-07·08의 긴 혼합 입력·실기기 글꼴·실제 공유·오디오/PWA·기존 설치 캐시 갱신·오프라인·첫 배포 공지 검증은 남는다. 네이티브 라디오 방향키·details Enter/Space, OS IME·스크린리더 실기기 확인도 최종 접근성 검증에 남긴다. `main` 반영·원격 푸시·배포는 하지 않는다.

### 2026-08-31 — 이용 안내·여운 명칭 정리

상위 메뉴와 `help.html`의 페이지/문서 제목을 **이용 안내**로 바꾸고, 하위 접힘 항목 **사용법**은 유지했다. 설정의 링크 접근성 이름과 안내 본문의 목적지 이름도 맞췄다. 기본 15개 중 **긴 여운 → 여운**은 길이 판단을 미리 요구하지 않으려는 사용자 결정이며 `finish.lingering` ID와 저장 목록은 유지한다. 직접 입력한 `긴 여운`·legacy 원문은 치환하지 않는다. [메뉴 기준](design-navigation.md#2-전역-메뉴와-맥락-진입)과 [단어장 기준](sensory-vocabulary.md#default-vocabulary)에 반영하고 과거 조사·변경 이력은 보존했다.

- Node 기존 검증 37개, 공통 UI 검증 320×660·375×660·768×900의 60개 통과. builtin 새 이름과 custom 원문 유지·상하위 제목 구분을 확인했다. 정적 번역 키 189개·JS 구문·diff를 확인하고 메뉴/이용 안내/단어장 실제 375px 스크린샷을 남겼다.
- URL·번역 키·저장 스키마·스타일은 변경하지 않았다. 기존 shell 자산만 수정하므로 배포 전 준비 중인 SW v62를 유지한다. 실제 모바일/PWA·오프라인·PNG 출력은 이번 명칭 수정에서 재검증하지 않았고 통합·원격 푸시·배포는 수행하지 않았다.

### 2026-08-31 — 공통 UI 기준과 화면 간 메뉴 일관성

[공통 UI 기준](ui-guidelines.md)과 `styles/ui.css`를 추가했다. 메인·사용법·설정의 상하단 조작을 .94rem/21px, 아이콘 19px, 최소 44px 영역으로 통일하고 460px 프레임 안의 상단·좌우·하단 기준선을 공유한다. 하위 페이지 제목·구역 제목·본문·보조 문구·각주를 역할별 토큰으로 정의하고 사용법/설정의 중복 스타일을 추출했다. 접힘 제목의 문구 자체가 가운데 오도록 화살표 공간을 양쪽에 예약했다. 넓은 화면의 박수 버튼도 프레임 안쪽에 둔다. 타이머 숫자·차/다구·시음 카드 PNG와 13.6px 단어장 칩/32px 배경은 별도 기준으로 유지했다.

- `tests/ui-consistency.html`: 320×660·375×660·768×900에서 세 화면의 글자/행 높이/아이콘/44px 영역·메뉴 기준선·제목 중심·계층·가로 넘침·기존 설정 배치 60개 통과. 320/375 실제 화면 스크린샷도 확인했다.
- `tests/localization.html` 49개, `tests/announcements.html` 11개, `node --test tests/*.test.cjs` 37개 통과. 타이머·초안·언어·공유 경로·공지 동작을 재확인했다. JS 구문·정적 번역 키 189개·전체 diff를 확인했다.
- 새 CSS를 SW shell 자산에 포함했고 `tests/offline-assets.html`에서 CSS를 포함한 11개 캐시 응답을 확인했다. 배포 전 준비 중인 v62를 유지한다. 이번에는 서버 중단 오프라인 재실행·실제 모바일/PWA·스크린리더를 재검증하지 않았다. 박수 전송·통합·원격 푸시·배포는 수행하지 않았다.

### 2026-08-31 — 단어장 칩 음영 축소·텍스트 출력 방침 기록

설정 단어장의 보이는 음영 배경을 삭제 버튼의 터치 영역과 분리했다. 한 줄 칩의 배경 높이는 52→32px, 전체 행 높이는 52→44px로 줄였고 `#식물향` 배경 폭은 약 117→94px다. 글자 13.6px·음영 색은 유지하며 삭제 버튼은 44×44px다. 긴 원문은 라벨 안에서 줄바꿈하고 배경도 높이에 맞춰 늘어난다. `#긴 여운`처럼 공백을 유지하는 일반 텍스트 복사와 ID/사용자 원문 저장의 구분을 [출력 방침](design-tea-feeling.md#52-두-가지-출력)에 기록했으며, 텍스트·PNG 출력이나 저장 스키마는 변경하지 않았다.

320×660·375×660 실제 렌더/스크린샷과 DOM에서 음영 크기·44px 삭제 영역·가로 넘침 없음을 확인했다. 전용 8134에서 공백 있는 사용자 단어의 추가·내 단어 표시·삭제·삭제 후 포커스 복귀를 확인하고 추가한 검증 단어는 제거했다. JS 구문·정적 번역 키 189개·diff 확인을 통과했다. 작은 스타일 수정으로 새 자동 테스트는 추가하지 않았으며 실제 모바일/PWA는 미검증이다. 기능 브랜치 로컬 변경이며 배포하지 않았다.

### 2026-08-31 — 설정 배치·언어 라디오·기본 감각 단어 15종

주의사항을 접히지 않는 첫 구역으로 옮기고, 바로 다음 언어 구역은 기본 펼침으로 바꿨다. 두 언어는 가로형 네이티브 라디오와 44px 라벨 영역으로 선택하며 기존 카드 여백을 줄였다. 차 느낌 단어장은 [확정 목록과 전환 계약](sensory-vocabulary.md#default-vocabulary)의 기본 15개를 제공한다. 이전 14개 ID·이름도 지원하며 저장된 builtin/custom/legacy/빈 목록은 교체하지 않는다. 태그 필드가 없거나 확인 후 기본 복원을 실행한 경우만 새 목록을 사용한다. 단어장 탐색·번체 번역·배포는 이번 범위가 아니다.

설정의 조합 이벤트를 `addEventListener`로 연결해 브라우저에서 유예가 실제 작동하도록 수정했다. 조합 중 라디오 선택은 기존 언어 체크를 유지하고 종료 후 적용한다. 저장 실패 시 이번 화면의 언어·라디오와 실패 안내를 유지하고 재진입 시 마지막 저장 언어로 돌아간다.

- `node --test tests/tags.test.cjs tests/preferences.test.cjs tests/i18n.test.cjs tests/announcements.test.cjs`: 37개 통과. 새 기본 목록·실제 한국어 카탈로그·이전 ID/라벨·목록 원문·빈 배열·명시 복원·저장 실패를 검증했다.
- 전용 8134의 `tests/localization.html`: 49개 통과. 설정 순서·기본 펼침·단일 선택·재진입·합성 IME·저장 실패와 기존 타이머/초안/공유 회귀를 확인했다. 320×660·375×660 실제 렌더와 스크린샷에서 언어 수평 정렬·44px 라벨·가로 넘침 없음·기본 15개 표시를 확인했다. 언어 구역은 기본 한국어 상태에서 높이 88px이다.
- JS 구문·정적 번역 키 189개·전체 diff를 확인했다. OS IME·모바일/PWA·스크린리더는 미검증이다. 설정의 네이티브 방향키 선택은 자동 키 주입으로 확정하지 못했으며 실제 키보드 확인을 남긴다. 회귀 fixture의 방향키 통과 항목은 메인 언어 popover 검증이다. 새 자산은 없고 배포 전 준비한 SW v62를 유지한다. 로컬 기능 브랜치 변경이며 통합·배포하지 않았다.

### 2026-08-31 — 지난 업데이트 조건부 노출·배포 묶음 자동 분류

`지난 업데이트`를 업데이트·예정된 업데이트와 같은 단계/금색 제목/접는 불릿 목록으로 추가했다. 과거 하위 항목이 없으면 제목과 목록 모두 생성하지 않는다. 정적 공지의 `releases`를 최신 배포 묶음 순서로 관리하고 첫 유효 묶음은 현재, 나머지는 과거로 분류한다. 새 묶음을 앞에 추가하면 기존 현재 항목이 자동으로 과거에 표시되며 날짜·예정 문구 수정이나 빈 배포 초안만으로는 이동하지 않는다. 현재 실제 데이터는 예정 공지만 유지했고 예시 배포는 게시하지 않았다.

`node --test tests/announcements.test.cjs tests/i18n.test.cjs` 8개 통과. 전용 8134의 `tests/announcements.html`은 실제 앱에 검증용 데이터만 주입해 배포 없음→첫 배포→다음 배포, 과거 제목 숨김, 현재/과거 분리, 동일 단계·스타일, 과거 항목 펼침/접힘, 예정 안내 미노출 11개를 통과했다. 375×660 iframe 렌더와 실제 8135 기본 공지의 빈 과거 섹션 숨김, JS 구문·번역 키·diff를 확인했다. 검증용 데이터는 브라우저 설정이나 실제 공지 파일에 저장하지 않았다. 기능 브랜치 로컬 변경이며 배포하지 않았다.

### 2026-08-31 — 공지 소제목·접는 불릿 목록

사용자 요청으로 `업데이트`·`예정된 업데이트`를 페이지 제목(1.18rem)보다 작은 금색 소제목(1.04rem)으로 바꿨다. 항목은 실제 ul/li 불릿 목록과 details/summary로 구성하고, 제목·본문을 같은 .9rem 본문색으로 왼쪽 정렬했다. 항목은 44px 이상 조작 영역과 펼침/접힘 표시를 유지하며 모달 Tab 순환에 포함했다. 예정 상태 안내는 예정 항목에만 표시한다. 375×660·320×660 렌더에서 색·글자 크기·줄바꿈·가로 넘침 없음, 클릭 펼침/접기와 Tab 진입, JS 구문·번역 키·diff를 확인했다. 네이티브 Enter/Space 활성화는 자동 키 주입으로 확정하지 못했으며 실제 키보드 확인은 남긴다. 배포하지 않았다.

### 2026-08-31 — 공지 창 제목에 갱신일 통합

사용자 요청으로 공지 창 제목을 `공지사항 (2026.08.31)` 형식으로 바꾸고 아래의 중복된 `공지 업데이트` 줄을 없앴다. 날짜는 기존 `updatedOn`에서 읽고 번역 키의 치환 변수로 표시하며, 유효하지 않으면 제목만 표시한다. 메뉴의 MMDD 위첨자·전체 날짜 접근성 설명은 유지한다. 320×660·375×660 렌더에서 한 줄 제목, 닫기 버튼과 겹침 없음, 이전 날짜 줄 제거·dialog 접근성 이름과 JS 구문·번역 키·diff를 확인했다. 배포하지 않았다.

### 2026-08-31 — 상단 세 버튼의 글자 높이 통일

사용자 화면 검토에서 우림 끝·언어 선택·메뉴의 수직 정렬을 확인했다. 버튼 영역은 모두 높이 44px·중심 Y 32.390625px로 같았지만 우림 끝의 글꼴만 15.68px(글자 영역 23px)로 다른 버튼의 15.04px(21px)보다 컸다. 세 버튼을 `.94rem`·줄 높이 21px로 통일하고 우측 도구 묶음의 가운데 정렬을 명시했다. 375×660·320×660 실제 렌더에서 세 글자의 중심과 버튼/아이콘 중심 일치, 44px 영역·가로 넘침 없음을 확인했다. 타이머·저장·언어 동작은 변경하지 않았고 TW-05 검토 브랜치에만 적용했다.

<a id="tw-05-localization"></a>

### 2026-08-31 — TW-05 한국어 번역 기반·언어/메뉴/공지 검토본

기능 브랜치 `codex/tw-05-localization-foundation`에서 한국어 공통 기반과 실제 UI를 구현·로컬 검증했다. 사용자와 합의한 **한국어 실제 화면 검토 → TW-06 번체 문구/화면 검토** 순서를 유지한다. 한국어 화면 승인 전이므로 TW-05 전체 완료·통합 병합·번체 지원 완료·배포 준비 완료로 표시하지 않는다.

메인 우상단에 언어 선택과 공지사항·사용법·설정 메뉴를 연결했다. 공지는 같은 문서에서 읽고, 읽음 여부와 무관한 갱신일 `0831` 및 전체 날짜를 표시한다. 첫 내용은 배포 완료가 아닌 예정 초안이다. 사용법·설정의 접힘 제목을 테두리 없이 가운데 정렬했고, 메인 이미지·중앙 타이머·푸터의 역할을 유지했다. 조작 아이콘은 선형 SVG, 임시저장은 문구 중심으로 맞췄다.

`TeaI18n` 공통 조회와 한국어 카탈로그를 정적/동적 UI·접근성·텍스트/PNG 공유에 적용했다. 사용자 원문과 차·다구 ID는 유지한다. 직접 언어 전환은 우림을 유지하고 IME/공유 작업 중에는 적용을 유예하며, 외부 탭 변경은 안전 idle까지 기다린다. 검토에서 발견한 마지막 노트·태그 해제 후 대기 언어 미적용과, 다른 탭의 시간 전용 저장이 IME/PNG 대기 중 직접 선택한 언어를 덮는 문제를 수정했다. 공유는 시작 시의 언어·입력·기록을 복사해 비동기 생성 중 전환으로 섞이지 않게 했다.

기본 태그 14종은 ID, 기존 문자열과 새 사용자 단어는 custom 원문으로 구분한다. 같은 표시의 builtin/custom을 독립 선택·삭제하며, 손상된 tagItems는 관련 없는 저장으로 덮어쓰지 않고 명시 복원만 허용한다. SW `v62`에 공용 JS·카탈로그·공지를 포함했다. 상세 계약은 [다국어 설계](design-localization.md)에 둔다.

검증:

- `node --test tests/preferences.test.cjs tests/tags.test.cjs tests/i18n.test.cjs`: 29개 통과. 기존/신규/빈/손상 태그, 원문/미지 필드 보존, 언어 patch, 한국어 폴백·치환·선택 알림을 확인했다.
- 전용 서버 `python3 -m http.server 8134 --bind 127.0.0.1`의 `tests/localization.html`: 38개 통과. 합성 번체 카탈로그로 우림 중 명시 전환, 다른 탭 유예·초안 해제/종료 후 적용, IME 대기, radio 키보드·Escape·공지 modal/inert, 이동 보호, 느린 실제 PNG 생성과 태그 복원을 확인했다. 테스트 대역은 실제 번체 번역·OS 입력/공유 검증이 아니다.
- 기존 `tests/accessibility.html` 35개, `tests/draft-boundaries.html` 66개, `tests/session-async.html` 9개 통과. 실제 앱 5초 우림·단일 완료→출탕→다음 포, 초안·포커스 보존과 플랫폼 지연 대역 회귀를 확인했다.
- `tests/font-preflight.html`: 한글·번체·emoji 혼합 제목/태그/메모로 정상 및 외부 스타일/글꼴 차단 두 모드의 실제 680×900 PNG를 생성·시각 확인했다. 새 원격 글꼴 의존성은 없다. 긴 입력 조판·기기별 폴백은 TW-07에 남긴다.
- 375×660·320×660 메인/언어/메뉴/설정/사용법을 렌더해 가로 넘침과 상단·주요 버튼의 44px 영역을 확인했다. 원본 PNG, DOM 측정과 보조 로컬 이미지 리뷰를 함께 사용하며 보조 모델의 OCR·상태 추정은 사실 판정에 쓰지 않는다.
- `tests/offline-assets.html`: v62 셸의 문서3개·JS7개 응답 확인. 8134 서버를 실제 중지한 뒤 캐시된 메인→설정(언어 변경)→사용법에 재진입했다. 외부 인터넷 전체 차단이나 기존 설치 SW 업그레이드 검증은 아니다. 인라인/공용 JS 구문, 카탈로그 키, 문서 링크·diff도 확인했다.

로그·스크린샷·원본 PNG는 Git 외부 `/Users/kioku/.codex/visualizations/2026/08/31/01a05600-b3f5-75d1-bdac-77e2d1d2c5c1/tw05`에 보관했다. 사용자 검토용 주소는 테스트 데이터와 분리한 `http://127.0.0.1:8135/`다. TW-06 번체 전체 문구, TW-07 긴 입력·실기기 글꼴, TW-08 모바일 브라우저/PWA의 오디오·실제 공유·캐시 업데이트/오프라인과 첫 배포 공지 일치는 미완료다. 원격 푸시·main 반영·배포는 하지 않았다.

<a id="tw-03-draft-boundaries"></a>

### 2026-08-31 — TW-03 초안 보호·세션 종료·완료 0포

기능 브랜치 `codex/tw-03-draft-boundaries`의 구현·검증·설계 커밋 `b561579`를 `232b208`에서 `codex/tw-readiness`에 병합했다. 통합 후 초안 회귀 66개·접근성 회귀 35개·설정 테스트 16개를 다시 통과했으며 5초 우림→완료→출탕, 초안 유지·명시적 종료, 실제 PNG 출력에 오류가 없었다.

첫 우림 전부터 제목·태그·메모 중 내용이 있으면 차·다구 변경을 막는다. 변경 없는 제목과 공백만 있는 메모는 잠그지 않고 원문은 보존한다. 다구 자산을 비동기로 준비하는 동안 새 초안이 생겨도 완료 직전에 다시 검사해 기존 선택을 유지한다. [초안·종료 계약](design-tea-feeling.md#draft-policy)을 구현했다.

`우림 끝`은 초안이 있으면 취소에 기본 포커스를 둔 삭제 확인을 연다. 확인 중 타이머는 계속되고 취소·Escape·배경 클릭은 현재 상태를 유지한다. 승인하면 초안·완료 기록만 지우고 저장 설정은 유지한다. 초안이 없으면 즉시 종료한다. `임시저장`은 입력·IME 확정 뒤 시트를 닫으며 세션 한정 보관 안내를 추가했다. 현재 문서를 떠나는 사용법·설정 링크는 안전한 idle에서만 허용하고 새 탭 이동은 유지한다. 문구는 ‘-습니다’체로 통일했으며 도움말도 현재 동작에 맞췄다.

시트·텍스트·카드·PNG는 완료 전 `완료된 우림 없음`을 사용하며, 열린 시트/카드도 완료 시 입력을 건드리지 않고 기록을 갱신한다. 종료 시 출탕 콜백을 지우고 완료/출탕 상태를 재검사한다. 오디오와 Wake Lock의 지연 요청은 세대값으로 무효화해 종료 뒤 늦은 소리나 화면 잠금 유지가 살아나지 않도록 했다. SW 캐시는 `v61`로 준비했다.

검증:

- `node --test tests/preferences.test.cjs`: 기존 16개 통과. 변경된 HTML의 인라인 JS·SW 구문, 문서 링크·diff 확인.
- 전용 `http://127.0.0.1:8134`에서 `tests/draft-boundaries.html`: 66개 통과. 제목/태그/메모 단독 보호, 취소/중복 승인, 설정 원문 보존, 임시저장과 조합 확정, 이동 허용/차단/새 탭, 5초 우림과 확인창 중 완료, 전·중·후 텍스트/DOM/실제 680×900 PNG, 공유 취소·다운로드 폴백을 확인했다.
- `tests/session-async.html`: 9개 통과. 실제 앱 HTML에 테스트용 AudioContext·Wake Lock·Image 지연기를 주입해 종료 뒤 늦은 음원/화면 잠금 요청, 자산 준비 중 세 종류의 단독 초안, 출탕 중 종료를 확인했다. 초기 fixture의 오래된 0포 기대 문구를 실제 확정 문구로 바로잡은 뒤 재통과했다.
- `tests/accessibility.html`: 기존 35개 재통과. 별도 앱 탭에서 실제 키 입력의 Tab/Escape 복귀, 초안 선택/페이지 이동 차단, 375×660·320×660 확인창과 44px 버튼을 확인했다. 0포·진행 중·1포 PNG 원본과 렌더를 확인했으며 콘솔 오류는 없었다.

산출물은 Git 외부 `/Users/kioku/.codex/visualizations/2026/08/31/01a055b0-8e12-73e2-ba94-91ae80e61456`의 `tw03-*`에 보관했다. 테스트의 합성 IME·플랫폼 API 대역·파일 공유 대역과 실제 OS IME·모바일 오디오/Wake Lock·공유 시트·스크린리더·설치형 PWA는 구분하며 후자는 미검증이다. 초안의 새로고침 복원·장기 저장, 즉시 출탕(TW-04), 번체 글꼴/긴 내용 PNG 조판(TW-07)은 추가하지 않았다. 원격 푸시·main 반영·배포는 하지 않았다.

<a id="tw-02-accessibility"></a>

### 2026-08-31 — TW-02 차 느낌 시트·카드 접근성

기능 브랜치 `codex/tw-02-accessibility`의 구현·검증·설계 커밋 `9fae2fa`를 `6be8fd4`에서 `codex/tw-readiness`에 병합했다. 통합 후에도 실제 앱 회귀 35개와 설정 테스트 16개를 재통과했으며 5초 우림→완료→출탕·중첩 창 복귀·초안 유지·PNG 생성에 오류가 없었다.

시트와 카드에 이름 있는 dialog, 최상위 창의 Tab/Shift+Tab 순환, Escape 한 단계 닫기와 실제 진입 버튼 포커스 복귀를 적용했다. 닫힌 레이어는 hidden/inert, 열린 창 아래의 시트와 타이머는 inert로 조작을 막는다. 카드의 닫기 버튼을 공유·저장 버튼에서 분리했다. [접근성·입력 보존 계약](design-tea-feeling.md#modal-accessibility)을 기준으로 구현했다.

제목 편집의 Enter/blur 중복 확정을 막고 원문 공백·이모지를 보존한다. 조합 중 Enter/Escape는 IME에 맡기며 닫기·카드 열기는 compositionend의 최종 입력 뒤 처리한다. 닫기·편집·태그·공유의 최소 조작 크기를 44×44px로 보강하고 태그 aria-pressed, 입력 이름과 포커스 표시, 긴 내용의 내부 스크롤을 추가했다. 타이머는 모달 아래에서도 계속되며 완료가 입력 포커스를 빼앗지 않는다. 창 안의 상태 안내와 타이머 복귀 버튼으로 출탕 조작에 돌아갈 수 있다. SW 캐시는 `v60`으로 준비했다.

검증:

- `node --test tests/preferences.test.cjs`: 기존 설정 보존 16개 통과. 인라인 JavaScript·SW 구문, 문서 링크·diff 확인.
- 전용 서버 `python3 -m http.server 8134 --bind 127.0.0.1`에서 `tests/accessibility.html`: 실제 앱 iframe 회귀 35개 통과. 닫힌 창 접근 차단, 중첩 포커스·Escape·배경 닫기, 제목 원문·조합 최종 입력, 메모·태그 보존, 5초 우림→완료→복귀→출탕, 실제 680×900 PNG 생성·공유 취소 후 보존, 320px 긴 제목·메모·태그와 하단 조작 접근을 확인했다. 키와 IME는 합성 이벤트 테스트다.
- 별도 실제 앱 탭에서 브라우저 키 입력으로 제목 편집 후 Tab 이동, Tab/Shift+Tab 순환과 연속 Escape 복귀를 확인했다. 375×660 시트·카드 렌더, 44px 버튼, 완료 시 메모 포커스 유지와 출탕 복귀, 브라우저 오류 없음도 확인했다.
- 스크린샷·회귀 로그·PNG 원본은 Git 외부 작업 폴더 `/Users/kioku/.codex/visualizations/2026/08/31/01a055b0-8e12-73e2-ba94-91ae80e61456`의 `tw02-*`에 보관했다.

TW-02는 앞서 반영한 [공통 알림 C](#common-toast)와 함께 구현·로컬 검증을 마쳤다. 실제 모바일 브라우저/설치형 PWA, OS 한글 IME, VoiceOver/TalkBack, 오프라인·오디오 조합은 미검증이며 최종 실사용 검증에 남긴다. `임시 저장`의 현재 동작·초안 삭제 확인·선택 잠금·완료 0포 표현은 TW-03, 번체/긴 내용의 PNG 조판은 TW-07 범위다. 원격 푸시·main 반영·배포는 하지 않았다.

<a id="common-toast"></a>

### 2026-08-31 — 공통 알림 C 말풍선·문구 통일

구현 `763fd5a`를 `9348198`에서 `codex/tw-readiness`에 병합했다. 통합 상태에서도 5초 우림 완료→우림 끝→새 마감 알림과 시작 상태 복귀를 재확인했다.

사용자가 선택한 C 시안을 `index.html`의 공통 토스트에 반영했다. 9px 모서리·작은 상단 꼬리·얇은 갈색 금빛 테두리·짙은 면과 낮은 그림자를 사용하고, 상단 조작 아래 콘텐츠 왼쪽에 배치한다. 긴 문구는 앱 폭 안에서 왼쪽 정렬로 줄바꿈한다. [공통 알림 정책](design-navigation.md#common-toast)에 기준을 기록했다.

세션 마감은 `이번 차를 마무리했습니다.`로 바꿨다. 임시 저장·복사·공유·다운로드·실패·선택 보호의 모든 토스트 문구를 ‘-습니다’체로 통일하고 이모지는 제거했다. 발생 조건·표시 시간·세션/초안 동작과 `role="status"`·포커스 비이동은 유지했다. 설정의 지속 오류 안내나 확인창은 토스트로 바꾸지 않았다. 배포용 SW 캐시는 `v59`로 준비했다.

실제 로컬 앱의 5초 우림→완료→차 선택 보호 안내→우림 끝→1포 5초 복귀를 확인했다. 375×660 세션 마감 말풍선과 320px 다중 행 안내에 가로 넘침이 없고, 9px 모서리·8px 꼬리·16px 왼쪽 여백을 확인했다. 알림이 표시돼도 클릭한 버튼에 포커스가 남고 조작을 가로채지 않는다. 느낌 메모 입력→임시 저장에서도 초안과 포커스가 유지된다. 원본 캡처는 Git 외부 `notification-studies`와 같은 작업 폴더의 `toast-implementation`에 보관했다. 인라인 JS 구문·기존 설정 테스트 16개·문서 링크·diff도 확인했다.

TW-02의 공통 알림 정리만 선반영했으며 시트·카드 접근성 전체는 완료하지 않았다. 실제 모바일 기기·스크린리더 검증과 원격 푸시·배포는 하지 않았다.

<a id="tw-01-preferences"></a>

### 2026-08-31 — TW-01 커스텀 태그·설정 보존

구현 커밋: `9425751` (`fix: preserve preferences and legacy tea adjustments`). 기능 브랜치 `codex/tw-01-preferences`의 구현 `9425751`·검증/문서 `294320d`를 `d0c7ee9`에서 `codex/tw-readiness`에 병합했다. 통합 후 Node 16개·브라우저 6개 시나리오를 다시 통과했고, 6초 우림→기록 1포 6초→출탕→다음 포 11초 흐름도 재확인했다.

메인과 설정이 `scripts/preferences.js`의 v1 읽기·필드 패치를 공유한다. 저장 직전 최신 raw를 읽고 편집한 필드/차의 보정만 바꿔 태그·다른 차 보정·미지의 `locale`/`tagItems` 등을 보존한다. 기본 태그의 화면 표시와 저장을 분리하고 `customTags: []`를 존중한다. 농향 우롱·생보이·숙보이의 과거 설정 ID를 호환 읽기하며 canonical 우선·손상 원본 보존·개별 기본값 복원 시 별칭 삭제를 적용했다. 상세 계약은 [데이터 보존](data-portability.md#preferences-contract)에 둔다.

잘못된 JSON·루트·지원하지 않는 버전은 명시적 전체 초기화 전까지 쓰기를 막는다. 읽기/쓰기 예외에도 타이머·현재 메모리 편집은 유지하고 저장 실패를 안내한다. 설정의 전체 초기화·기본 태그 복원에 취소 가능한 확인을 추가했다. 설정 시간 범위는 5–120초이며 시간 조절·태그 삭제 버튼에 44px 영역·접근성 이름·편집 후 포커스 유지를 적용했다. 새 공통 JS를 SW 앱 셸에 추가하고 캐시를 `v58`로 갱신했다.

검증:

- `node --test tests/preferences.test.cjs`: 16개 통과. 빈/기존/중복 유니코드 태그, 세 ID 별칭·충돌·손상 canonical, 잘못된 payload, 중첩 필드의 순차 교차 편집, 미래 필드, 읽기·쓰기·삭제 예외를 확인했다.
- 실제 두 HTML을 여는 로컬 브라우저 harness: 6개 시나리오 통과. 태그 편집→메인의 차·다구·시간·음소거 저장→오래 열린 설정의 다른 차 시간 저장, 태그 누락/빈 배열·재진입, 별칭과 시간 범위, 잘못된 raw 보호, 읽기/쓰기 실패 시 메모리 값·안내, 초기화/태그 복원 취소·삭제 실패·명시 초기화 후 복구를 확인했다. 예외와 확인 응답은 검증 서버에서만 주입하며 제품 코드에 테스트 분기는 없다.
- 설정 375×660·320×660 실제 렌더: 가로 넘침 없음, 설정 버튼 모두 44px 이상. 시간 조절 뒤 같은 버튼의 키보드 포커스 유지 확인. 작은 화면과 변경된 안내의 스크린샷을 남겼다.
- 메인 5초 정상 우림→완료 기록 1포 5초→출탕→다음 포 10초, 저장 델타 -15 유지 확인.
- 캐시 원본에서 `chahanjan-shell-v58`의 공통 JS와 모든 앱 셸 자산을 확인했다. 로컬 HTTP 서버를 중지한 채 설정 진입→시간 +1 저장→메인 재진입 6초 표시를 확인했다. 이는 앱 서버 자산의 오프라인 검증이며 외부 글꼴 차단 테스트는 아니다.
- 두 HTML의 인라인 JS·공통 JS 구문, 브라우저 런타임 오류, 문서 링크·`git diff --check` 확인.

브라우저 harness·주입 서버·원본 캡처·검증 로그는 Git 외부의 `/Users/kioku/.codex/visualizations/2026/08/31/01a055b0-8e12-73e2-ba94-91ae80e61456/tw-01/`에 보관한다. 화면/저장 검증은 데스크톱 브라우저의 결과이며 실제 iOS/Android·설치형 PWA·오디오 출력·스크린리더는 미검증이다. 기존 설치의 v57→새 버전 업데이트와 외부 글꼴 오프라인 조합은 TW-07·08에서 다시 확인한다. 다중 탭 동일 필드의 동시 쓰기와 구버전 앱의 쓰기를 무손실 병합한다고 보장하지 않는다.

원격 푸시·main 반영·배포는 하지 않았다. TW-02–08·장기 기록·언어 UI는 이번 구현에 포함하지 않는다.

## 문서 변경

### 2026-08-31 — 한국어 기본 감각 15개와 선택 확장 확정

[후기 조사 §7](research-korean-tea-review-vocabulary.md#confirmed-vocabulary)에 확정 목록과 대만 풍미륜·TAGs·NIH/NIDCD·후기 원문의 근거 범위를 정리했다. 구운 향은 향으로 분류하며, 입안에서 코로 전달되는 향과 일상적인 맛 표현의 관계를 설명했다. 텁텁함 채택은 제품 결정으로 기록하고 교체 전 깔깔함 조사 결과를 검증 근거로 바꾸지 않았다.

단어장·차 느낌·제품 설계와 로드맵에 한국어 명칭·확장 방식 확정을 반영했다. 원전과 개인 표현의 출처, 구운맛·구수함 등 사용자 원문, 기존 ID·기록의 보존 경계를 유지한다. 최종 목록 15개·출처·문서 링크·앵커·diff를 확인했으며 번체 문구·세부 해설·이관·Phase E 구현은 미완료다. 앱 코드·저장값·SW 변경이나 배포는 하지 않았다.

### 2026-08-31 — 입안 감각 문구를 깔깔함에서 텁텁함으로 교체

사용자 결정에 따라 [기본 문구안](sensory-vocabulary.md#default-vocabulary)의 입안 감각을 `깔끔함 · 텁텁함 · 떫음 · 묵직함 · 긴 여운`으로 정리했다. 텁텁함의 생활어 설명은 초안으로 두고, 공식 세치도와의 대응·떫음 및 깔끔함과의 의미 구분은 검증된 사실로 표시하지 않았다. 기존 후기 조사는 교체 전 결과로 보존하고 후속 결정 안내를 추가했다. 문서 링크·내용·diff를 확인했으며 앱 코드·기존 태그 ID·저장값은 이 작업에서 변경하지 않았다.

### 2026-08-31 — 한국어 차 후기와 감각 후보 15개 비교

[후기 어휘 조사](research-korean-tea-review-vocabulary.md)에 공개 후기·개인 시음기 13개 페이지의 본문과 후보 15개를 대조했다. 판매자·서포터즈·가향차·본문 미확인 자료를 분리하고, 단어의 직접 사용과 의미 대응, 감각의 부재·비교 문맥을 구분했다. 추가 검색으로 짭짤함·시큼함의 직접 시음 용례도 확인했으며, 표본의 대표성이나 사용 빈도는 주장하지 않는다.

깔깔함의 정확한 용례는 확보하지 못했고, 까끌함·분말의 거침을 공식 세치도와 등치할 수 없어 거친 질감을 대안으로 남겼다. 단어장 설계에 최신 검토 후보와 조사 링크를 연결했다. 인용·출처·본문 대응, 문서 링크·diff를 확인했으며, 문구 확정·사용자 실험·앱 코드·저장값·SW 변경은 하지 않았다.

### 2026-08-31 — 입안 감각의 공식 정의와 한국어 표현 조사

[풍미륜 조사 §3.3–3.4](research-taiwan-tea-flavor-wheel.md#mouthfeel-definitions)에 공식 TAGs 차품 가이드의 문장형 설명과 차개장 영문 안내를 대조해 다섯 감각의 뜻·한계를 기록했다. 공식 PDF 10·29쪽의 축 구조·플랫폼 안내도 렌더로 확인했다. 한국어 후보는 국립국어원 사전의 의미 범위를 참고한 편집 제안으로 구분하며, 기본 15개 방향은 유지하되 입안 감각의 선택 문구는 미확정으로 남겼다.

공식 설명에 있는 분자 크기 서술을 검증된 물리적 인과로 확대하지 않고, 고운 감촉·매끄러움·농도감·여운의 혼동 위험을 정리했다. 문서 링크·앵커·diff를 확인했다. 한국어 사용자 실험이나 앱 구현은 하지 않았으며, 기존 코드·ID·사용자 저장값은 이 작업에서 변경하지 않았다.

### 2026-08-31 — 기본 감각 15개와 단어장 선택 확장 방향

사용자와 [향 5개·맛 5개·입안 감각 5개를 기본 제공](sensory-vocabulary.md#default-vocabulary)하고 세부 표현은 단어장을 참고해 추가하는 방향을 정했다. 기본 제공과 이번 차의 감각 선택을 구분하고, 입안 감각 다섯 축의 실제 태그 문구는 구현 전에 다듬도록 했다. 공식 항목의 출처와 개인 원문을 보존하며 기존 14개·사용자 목록을 자동 삭제하거나 재분류하지 않는 전환 조건을 기록했다.

설계·로드맵·차 느낌 문서에 연결하고 조사 당시의 14개 유지 제안은 새 결정으로 대체됐음을 표시했다. 문서 링크·앵커·diff를 확인했으며 Phase E는 미구현으로 유지한다. 이 작업은 문서만 변경했고 코드·저장값·SW 변경, 커밋·병합·푸시·배포는 하지 않았다.

### 2026-08-31 — 대만 풍미륜과 기본 감각 태그 비교 조사

[대만 특색차 풍미륜 조사](research-taiwan-tea-flavor-wheel.md)에 2.0의 향·맛·입안 감각 구조와 기본 14개 태그 전수 대응표를 작성했다. 번체·영어·한국어 원본을 대조하고, 2.0에서 빠진 나무·흙은 1.0에서 확인했다. 직접 대응·부분 대응·버전별 미수록을 구분하고, 공통 단어·앱의 설명·개인 표현의 경계와 원전 이용 조건을 검토 사항으로 남겼다.

14개 이름·ID 및 판정 집계, 원전의 분류별 항목 수, 문서 링크·diff를 확인했다. 단어장 설계와 로드맵에 조사 링크를 연결했으며 Phase E는 미구현 상태를 유지한다. 앱 코드·사용자 데이터·SW 변경은 없고, 커밋·병합·원격 푸시·배포는 하지 않았다. 진행 중인 TW-05 작업 트리의 기존 변경은 이 조사에 포함하지 않는다.

<a id="tw-04-deferred"></a>

### 2026-08-31 — TW-04 보류·다음 착수 TW-05로 조정

사용자가 실제로 일찍 따라내는 일이 거의 없다는 경험을 확인해 TW-04 즉시 출탕을 보류했습니다. [결정 근거와 재검토 조건](DESIGN.md#early-pour-policy)을 기록하고 번호·이전 설계·검증안은 보존했습니다. 이번 TW 묶음의 구현·번역·필수 검증·배포 준비 조건에서 조기 출탕을 제외하며, 축소판 기능도 추가하지 않습니다.

ROADMAP·실행 계획·AGENTS의 다음 착수를 TW-05로 맞추고 차 느낌·다국어·배치 설계의 참조 범위를 정리했습니다. TW-08은 정상 완료·출탕·다음 포·세션 종료의 중복 처리 방지 검증을 유지합니다. 문서 변경만으로 런타임 기능 완료를 추가하지 않으며, 기존 TW-01–03의 구현·검증 이력은 그대로 보존합니다.

검증: `git diff --check`, 변경된 Markdown 8개 파일의 로컬 링크·앵커 168개 검사, 이전 착수 순서·조기 출탕 필수 검증 표현 검색과 전체 diff 검토를 통과했습니다. 런타임·자산·SW 변경이 없어 앱 테스트는 재실행하지 않았으며, 원격 푸시·main 반영·배포는 하지 않았습니다.

### 2026-08-31 — 이전 차 느낌 목업을 Git 외부로 이동

독립 검토용 `mockup-tea-feeling.html`을 저장소에서 제거하고 작업 전용 미리보기 폴더의 `legacy-tea-feeling/`에 원본 보관했습니다. 제목 편집·태그·카드 미리보기 등 과거 검토 내용을 남기기 위해 HTML을 수정하지 않고 상대 경로의 개완 이미지도 함께 복사했습니다. 현행 배치 미리보기는 계속 별도로 관리합니다.

HTML·이미지의 SHA-256 일치와 상대 경로 자산 존재, 앱·문서·서비스워커의 기존 참조 없음, 실제 앱 코드·자산·SW 불변을 확인했습니다. AGENTS에 목업의 Git 외부 관리 원칙을 추가하고 루트의 `mockup-*.html`은 ignore하도록 했습니다. 과거 Git 이력은 수정하지 않았으며 배포는 하지 않았습니다.

### 2026-08-31 — 공지 갱신일 위첨자

[공지사항 설계](design-navigation.md#announcements)에 메뉴명 바로 오른쪽의 `MMDD` 위첨자를 추가했습니다. 앱 배포일이나 예정일이 아닌 공지 묶음의 마지막 내용 갱신일을 사용하며, 열람 후에도 유지합니다. 공지 화면과 접근성 설명에는 연도를 포함한 전체 날짜를 제공하고 별도 읽음 상태는 저장하지 않습니다. 미리보기의 `0831`은 예시 날짜입니다.

미리보기의 한국어 375px 렌더, 번체 320px의 가로 넘침 없음·44px 메뉴 높이, 전체 날짜 안내, 열람·언어 변경 후 날짜 유지와 JavaScript 구문·브라우저 오류 로그·문서 diff를 확인했습니다. 실제 앱 코드·공지 게시·배포는 변경하지 않았습니다.

### 2026-08-31 — 공지사항 메뉴와 업데이트 안내 설계

사용자 요청으로 메뉴의 사용법 위에 스피커 아이콘+공지사항을 추가하는 [설계](design-navigation.md#announcements)를 반영했습니다. 실제 배포된 업데이트와 예정된 업데이트를 구분하고, 제목은 테두리 없이 가운데 정렬·본문은 왼쪽 정렬로 읽습니다. 자동 팝업·푸시 없이 같은 문서에서 읽으며 타이머·초안을 보존하는 방향입니다. 첫 공지에는 UI 개편 뒤 기능 위치와 기존 이미지 유지 여부를 실제 배포 내용에 맞춰 안내합니다.

TW-05에 정적 공지 진입·읽기, TW-06에 번체 공지, TW-08에 배포 내용 일치·기존 설치 업데이트·오프라인 검증을 연결했습니다. 공지 데이터는 앱과 함께 배포하며 별도 서버·CMS를 추가하지 않습니다. 기존 장기 기록·단어장·레시피의 Phase는 유지했습니다.

별도 미리보기에서 공지 메뉴·본문 펼침·타이머 복귀, 한국어 375px 렌더, 번체 320px의 제목 정렬·44px 이상 조작 영역·가로 넘침 없음, TW 단계의 공지사항/사용법/설정 노출, 공지 읽기 후 메모와 음소거 상태 유지를 확인했습니다. JavaScript 구문·브라우저 오류 로그·문서 링크·diff도 확인했습니다. 예시 공지와 설계만 변경했으며 실제 공지 게시·앱 런타임·자산·SW·배포 변경은 없습니다.

### 2026-08-31 — 느낌 시트 저장 버튼 문구

사용자 요청에 따라 장기 기록 버튼을 `모바일 기기 아이콘 + 브라우저 기록으로 남기기`, 초안 유지·닫기 버튼을 `임시저장`으로 정했습니다. [차 느낌](design-tea-feeling.md#draft-policy)·[배치 설계](design-navigation.md)의 문구와 아이콘 기준, DESIGN·ROADMAP·데이터 보존 문서의 연결 표현을 맞췄습니다. 임시저장은 현재 세션 메모리에 유지하는 기존 의미이며, 버튼 근처에 `임시저장은 이번 우림 동안만 유지됩니다.`를 표시합니다. 브라우저 기록 저장은 방문 기록·동기화가 아닌 앱의 로컬 기록이며 Phase B 구현 대기입니다.

별도 미리보기의 375px 렌더에서 문구·스마트폰 아이콘을 확인하고, 320px에서 가로 넘침 없음·44px 이상 버튼 높이·임시저장 후 입력 유지·기록 저장 예시 후 기록함 링크를 확인했습니다. JavaScript 구문·브라우저 오류 로그·문서 diff를 확인했습니다. 실제 앱의 저장 기능·코드·이미지 자산·SW는 변경하지 않았습니다.

### 2026-08-31 — 2단계 메뉴의 테두리 제거·가운데 정렬

사용자 검토를 반영해 [배치 설계 §2.1](design-navigation.md)에 사용법·설정 등 하위 메뉴 행의 테두리를 없애고 메뉴명을 가운데 정렬하는 기준을 추가했습니다. 아이콘 유무와 관계없이 텍스트 중심을 맞추고, 전역 메뉴·입력창·주요 실행 버튼은 기존 형태를 유지합니다. 터치 영역과 키보드 포커스 표시는 제거하지 않습니다.

별도 미리보기의 한국어 375px 렌더와 번체 320px 사용법·설정 행을 확인했습니다. 메뉴 행은 테두리 0px·가운데 정렬·높이 44px이며, 사용법 텍스트 중심 오차와 가로 넘침이 없었습니다. 브라우저 오류 로그·JavaScript 구문·문서 diff를 확인했으며 실제 앱 런타임 변경은 없습니다.

### 2026-08-31 — 후속 기능 배치와 아이콘·문구 기준

[화면 배치와 메뉴 표현](design-navigation.md)을 추가하고 DESIGN·ROADMAP·다국어·차 느낌·단어장·TW 실행 계획을 연결했습니다. 메인 우상단의 언어 선택+메뉴, 기록함·단어장·레시피의 단계별 메뉴 진입, 느낌 작성 중 단어장, 기록함 안 회고, 설정의 전체 백업·복원을 배치했습니다. 목적지는 아이콘+이름, 결과가 중요한 행동은 문구, 맥락이 분명한 작은 조작은 아이콘만 쓰는 기준도 정했습니다.

기존 설정 전용 언어 선택은 메인의 명시적 전환으로 확장했습니다. 같은 문서의 전환은 우림 상태를 보존하고, 별도 도움말·설정 문서로 나가는 동작은 계속 보호합니다. 첫 저장 후에만 기록 진입점을 노출하거나 홈에 개수 배지를 추가하는 제안은 채택하지 않았습니다. 기능 도입 후에는 빈 기록함도 메뉴에서 접근하며 첫 저장 성공 뒤 맥락 링크를 제공합니다.

별도 배치 미리보기에서 TW/후속 단계 메뉴 노출, 320px·375px 폭, 한국어·번체 상단 조작의 44px 높이와 가로 넘침, 느낌→단어장→느낌의 메모·포커스 보존, 언어 전환 뒤 메모·태그 유지, 명시적 저장 뒤 기록함 링크, 설정→백업 진입, Escape 닫기·포커스 복귀를 확인했습니다. 미리보기의 JavaScript 구문 검사와 브라우저 오류 로그, 문서 로컬 링크·앵커 및 diff를 확인했습니다. 화면 캡처는 초기 한국어 배치를 확인했으며, 일부 상호작용 후 캡처가 빈 이미지로 반환되어 그 상태의 시각 검증은 DOM 경계 확인과 구분합니다.

이는 배치 예시와 문서 검증이며 실제 타이머·영구 저장·전체 번역·스크린리더·모바일 PWA의 통과 결과가 아닙니다. TW-05에는 언어 선택·기본 메뉴만 포함하고 후속 기능의 Phase는 유지합니다. 런타임·SW·푸시·배포 변경은 없습니다.

### 2026-08-31 — TW 착수 전 동작 계약과 개발 흐름

[AGENTS.md](../AGENTS.md)에 통합 브랜치 `codex/tw-readiness` → TW별 기능 브랜치 → 설계·구현·검증 → 순차 병합 흐름과 문서 역할을 정리했습니다. 기존 대만 우선순위 문서와 [실행 계획](tw-implementation-plan.md)을 공통 기준점으로 함께 관리합니다.

[DESIGN §6.5](DESIGN.md), [차 느낌](design-tea-feeling.md#draft-policy), [설정 보존](data-portability.md#preferences-contract), 새 [다국어 설계](design-localization.md)에 구현 계약을 반영했습니다. 상시 느낌 진입·초안이 있을 때 선택 잠금·명시적 종료, 완료 0포 표현, 조기 출탕 시간과 중복 완료 방지, v1 부분 저장·ID 별칭·빈 태그 보존, 한국어 기본값과 대만 번체 선택·안전한 페이지 이동, 기존 제품명 유지가 대상입니다.

검증은 현재 코드 대조와 문서 일관성·로컬 링크·diff 확인입니다. 데이터 fixture와 타이머 경계·언어·기기 시나리오는 실행할 완료 조건으로 정의했으며 아직 통과 결과가 아닙니다. TW-01–08은 모두 구현 대기이고 런타임·SW 캐시·배포 변경은 없습니다. 다음 구현 항목은 TW-01입니다.

### 2026-08-31 — 대만 실사용 우선 처리 묶음 추가

대만에서 약 3개월간 중국어를 공부하고 사람들과 차를 마시는 실사용을 위해, [현재 우선 처리](ROADMAP.md#taiwan-priority)를 추가했습니다. Phase A의 태그 보존·시트 접근성·초안 경계·즉시 출탕과 Phase D-1의 번체 중국어 지원을 TW-01–08로 묶고, 주분류·완료 확인·선행 결정·제외 범위를 적었습니다.

기존 A 전체→B→C→D 순서에서 번체 지원만 A 핵심 안정화와 함께 앞당깁니다. 영어·간체·일본어는 D-2로 남기고, 백그라운드 완료 신호 예약은 별도 검증합니다. 감각 연출·스와이프·전체 폰트 셀프호스팅과 장기 기록·백업은 이번 묶음의 선행 조건이 아닙니다. 카테고리 정의와 기존 완료 내역은 유지했으며, 기능 구현·완료·배포를 뜻하는 변경은 아닙니다.

### 2026-08-31 — 개발 카테고리 정의와 완료 내역 분리

기존 날짜순 표의 완료 항목 12개에 주분류를 부여해 6개 주분류(우림 감각·다구 표현, 조작·세션 흐름, UI·화면 구조·내비게이션, 차 느낌·감각 확장, 성능·PWA·오디오, 도움말·설정·언어)로 색인했습니다. 데이터 보존·이동성은 독립 완료 항목이 아직 없어 완료 색인에서는 생략했습니다.

분류 기준은 다음과 같이 정리했습니다. `우림 상태의 감각 표현`·`차 선택의 시각 단서`는 우림 감각·다구 표현으로, `짧은 조작의 편의`는 조작·세션 흐름으로 옮겼습니다(제스처·배치는 UI 책임). `접근성과 피드백 중복성`은 UI 기준이자 모든 카테고리에 공통인 품질 기준으로 두고, 신호 표현은 우림 감각에 둡니다. `설치형 앱 완성도`는 성능·PWA·오디오로, `한 잔의 감각 기록`은 차 느낌·감각 확장과 데이터 보존·이동성으로 분리했습니다. 도움말·설정·언어는 기존 완료 내역과 다국어 계획의 공통 책임으로, UI는 아이콘·탭·메인 역할 등 확장 전 화면 구조 설계의 책임으로 명시했습니다.

변경 이유는 앱 철학을 책임 범위와 연결하고, 기능 확장 때 생기는 UI·데이터·기록의 경계 혼동을 줄이며, 로드맵에서 전체 진행 상황을 쉽게 훑을 수 있게 하기 위해서입니다. Phase·완료일·구현 상태와 기존 12개 항목의 구현 범위는 유지했습니다. 런타임 변경은 없습니다.

## 2026-08-01

<a id="asset-loading"></a>

### 다구 이미지 on-demand와 오디오 잔여음 정리

분류: 성능·PWA·오디오

평상시 선택 다구의 SVG `href`만 유지하고 전환 대상은 `Image.decode()`와 실제 SVG load 뒤 교체했습니다. 새 화면 페인트 후 이전 `href`를 제거했습니다. 폰트를 기다리지 않고 시작 자산과 UI를 초기화하며 음소거·초기화 시 활성 oscillator를 정지했습니다. SW 캐시 v57을 적용했습니다.

관련 커밋: [`89a5043`](https://github.com/winterrainlee/tea-timer/commit/89a5043) (`Load vessel artwork on demand`)

<a id="rendering-cache"></a>

### 모바일 렌더·PWA 캐시 최적화

분류: 성능·PWA·오디오

상태 기반 단일 스케줄러, 진행 중 30fps·완료 대기 12fps, idle·배수 완료 정지, reduced-motion 정적 렌더, 김 노드 재사용을 적용했습니다. 문서 network-first와 정적 자산 cache-first를 shell/static 캐시로 분리했습니다.

관련 커밋: [`aefbfa4`](https://github.com/winterrainlee/tea-timer/commit/aefbfa4) (`Reduce mobile rendering and cache overhead`)

## 2026-07-28

<a id="small-screen-safari"></a>

### 작은 화면·Safari PWA 안정화

분류: UI·화면 구조·내비게이션

iPhone Mini급 레이아웃 붕괴와 설정 입력 폭을 교정하고, 카드 PNG는 Web Share 파일 공유를 우선한 뒤 다운로드로 폴백하도록 보강했습니다.

관련 커밋: [`733c8ca`](https://github.com/winterrainlee/tea-timer/commit/733c8ca) (`fix: improve PWA layout and settings guidance`), [`1745c51`](https://github.com/winterrainlee/tea-timer/commit/1745c51) (`fix: share card images in Safari`), [`48ca7b6`](https://github.com/winterrainlee/tea-timer/commit/48ca7b6) (iPhone 13 mini 여백 붕괴 수정), [`e67b5e0`](https://github.com/winterrainlee/tea-timer/commit/e67b5e0) (설정 입력 폭·터치 영역 수정)

<a id="feeling-mvp"></a>

### `[이번 차의 느낌]` MVP

분류: 차 느낌·감각 확장

현재 세션 요약, 기본·커스텀 태그, 제목·자유 문장, 세션 초안, 텍스트 복사, 680×900 Canvas 카드 공유·다운로드를 구현했습니다. `help.html`과 `settings.html` 역할을 분리했습니다. 영구 기록 저장은 별도 미완료 범위입니다.

관련 커밋: [`6bbaa93`](https://github.com/winterrainlee/tea-timer/commit/6bbaa93) (`feat: [차 느낌] 시음 카드 바텀시트 모달 및 help.html 3대 섹션 개인설정 통합 구현`), [`78e6174`](https://github.com/winterrainlee/tea-timer/commit/78e6174) (사용법·개인설정 페이지 분리)

<a id="session-draining"></a>

### 세션 보호와 모든 다구 출탕 단계

분류: 조작·세션 흐름

1포 이상 완료 후 차·다구 변경을 `우림 끝`까지 잠그고, 일반 다구와 표일배 모두 우림 완료 뒤 실제 따라내기·배수 단계를 거쳐 다음 포로 이동하도록 했습니다.

관련 커밋: [`97f005d`](https://github.com/winterrainlee/tea-timer/commit/97f005d) (`feat: 우림 세션 보호(Session Lock) 구현`), [`33f5fd6`](https://github.com/winterrainlee/tea-timer/commit/33f5fd6) (`feat: 모든 다구 우림 완료 후 따라내기(出湯) 단계 및 기물별 시각화 구현`)

## 2026-07-17

<a id="ios-audio"></a>

### iOS 오디오 인터럽션 복구

분류: 성능·PWA·오디오

`AudioContext`가 `suspended`뿐 아니라 `interrupted` 등 모든 non-running 상태일 때 재개하고, `closed`면 재생성하도록 했습니다. 앱 복귀·다음 포 시작·매 재생 직전에 복구하며, `sw.js` 캐시를 v49로 갱신했습니다. 백그라운드 완료 신호 선예약은 별도 미완료 항목입니다.

관련 커밋: [`516146b`](https://github.com/winterrainlee/tea-timer/commit/516146b) (`Restore tea timer audio after iOS interruptions`)

## 2026-07-12

<a id="feeling-entry"></a>

### `[이번 차의 느낌]` 선행 정리

분류: UI·화면 구조·내비게이션

메인 화면 푸터의 응원 박수(👏)·모달·제작자 반응 전송 코드를 제거하고 도움말의 박수만 유지했습니다. 그 자리에 음소거 버튼과 같은 실루엣의 연필 라인 아이콘 노트 버튼과 당시 `준비 중` 안내 모달을 추가했습니다. 상세 설계는 [design-tea-feeling.md](design-tea-feeling.md) §7을 따릅니다.

관련 커밋: [`edce834`](https://github.com/winterrainlee/tea-timer/commit/edce834) (`Extract tea-feeling design doc and seed footer note button`)

<a id="help-tone"></a>

### 도움말 톤 정리

분류: 도움말·설정·언어

`help.html` 전체를 정중한 합니다체로 통일했습니다(해요체는 과하게 친근해 배제, 요청문만 `-해 주세요`). 앱 소개 섹션을 추가하고, 사용법에 권장 눈금·泡 설명을 반영했습니다. 포크/오픈소스 문구를 사용자 관점(`자유롭게 쓰세요`)으로 재작성하고, 기억한 설정에 "이 기기에만 저장"을 명시했으며, 박수 모달 `싫어요`→`괜찮습니다` 등 문구를 순화했습니다.

관련 커밋: [`1a4783f`](https://github.com/winterrainlee/tea-timer/commit/1a4783f) (`Switch help copy from haeyo to hamnida register`), [`18b1ddb`](https://github.com/winterrainlee/tea-timer/commit/18b1ddb) (`Soften help page tone for tea beginners`)

<a id="preferences"></a>

### 반복 사용 상태 영속

분류: 조작·세션 흐름

`teaTimer.preferences.v1`에 차·다구·차별 권장 대비 보정값(델타)·음소거를 저장·복원했습니다. 슬라이더 권장 지점 눈금(금색 채움 위에서도 식별되도록 어두운 테두리), `aria-valuetext`로 권장 시간 병기, 도움말 `기억한 설정 지우기`, 우림 중 `−`/`+` 비활성화를 적용했습니다. 상세 스펙은 [DESIGN.md](DESIGN.md) §6.2를 따릅니다.

관련 커밋: [`8b25b4b`](https://github.com/winterrainlee/tea-timer/commit/8b25b4b) (`Persist repeated-use preferences as per-tea deltas`)

## 2026-06-28

<a id="tea-visuals"></a>

### 우림 시각 표현 보강

분류: 우림 감각·다구 표현

개완·표일배의 김 배출 위치를 뚜껑과 림 사이 틈으로 교정하고, 7종 차의 젖은 잎 형태를 분리했으며, 유리 다구의 찻잎을 idle부터 표시했습니다.

관련 커밋: [`3c9508c`](https://github.com/winterrainlee/tea-timer/commit/3c9508c) (`Fix steam emission positions for gaiwan and piaoyibei`), [`684428e`](https://github.com/winterrainlee/tea-timer/commit/684428e) (`Differentiate wet leaf shapes per tea type`), [`892541e`](https://github.com/winterrainlee/tea-timer/commit/892541e) (`Show tea leaves from the start of brewing`)

<a id="separator-liquid"></a>

### 표일배 액체·찻잎 위치 교정

분류: 우림 감각·다구 표현

윗 챔버의 배수구 위로 찻잎과 액체 하단을 올려 거름망 안에서 우러나는 물리적 구조에 맞췄습니다.

관련 커밋: [`b82ef6d`](https://github.com/winterrainlee/tea-timer/commit/b82ef6d) (`Fix piaoyibei leaf and liquid positions above drain spout`)

## 2026-06-18

<a id="reactions"></a>

### 도움말 제작자 반응 집계

분류: 도움말·설정·언어

`help.html`의 박수/괜찮습니다 모달에서 앱·반응값만 Cloudflare Worker로 보내고 D1에 집계했습니다. 허용 Origin과 입력값 검증, Bearer 토큰 기반 비공개 합계 API, 실패 시 사용자 흐름을 막지 않는 전송 방식을 적용했습니다.

관련 커밋: [`82a0260`](https://github.com/winterrainlee/tea-timer/commit/82a0260) (`Add creator reaction tracking`), [`9be2e88`](https://github.com/winterrainlee/tea-timer/commit/9be2e88) (`Document applause privacy note`), [`4c8bb07`](https://github.com/winterrainlee/tea-timer/commit/4c8bb07) (`Add applause button to help page`)
