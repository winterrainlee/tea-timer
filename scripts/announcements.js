// Content bundled with the app. Planned items do not claim a release date.
const TeaAnnouncements = Object.freeze({
  updatedOn: "2026-08-31",
  items: Object.freeze([
    Object.freeze({ id: "language-menu-preview", status: "planned", titleKey: "announcements.plannedTitle", bodyKey: "announcements.plannedBody" }),
  ]),
});
