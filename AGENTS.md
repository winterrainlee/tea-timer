# 개발 진행 원칙과 작업 흐름

이 지침은 `tea-timer` 저장소 전체에 적용한다. 사용자와 합의한 개발 범위를 기준으로 한국어로 소통하며, 제품의 확정 동작과 현재 구현 상태를 구분한다. 전역 에이전트 지침을 중복해서 복사하지 않고 이 저장소의 작업 흐름을 정의한다.

## 1. 작업 시작

- 먼저 현재 브랜치, 작업 트리의 변경, 관련 코드와 문서를 확인한다. 기존 사용자 변경을 임의로 되돌리거나 다른 기능의 커밋에 섞지 않는다.
- 우선순위·범위·완료 조건은 [ROADMAP.md](docs/ROADMAP.md), 제품 원칙과 확정 동작은 [DESIGN.md](docs/DESIGN.md)를 따른다.
- 실행 방법은 [README.md](README.md), 코드 지도와 검증 환경의 주의사항은 [CLAUDE.md](CLAUDE.md)를 참고한다. 과거 현황 설명과 현재 코드가 다르면 실제 구현을 확인하고 문서의 시점을 구분한다.
- 착수할 항목의 선행 조건, 변경 범위, 비목표, 검증 방법을 먼저 정한다. 이미 합의한 범위의 일상적인 구현 선택마다 재확인을 요청하지 않는다.
- 사용자 데이터의 삭제·이관, 초안 경계, 타이머 상태, 언어 전환처럼 동작 의미를 바꾸는 결정은 구현 전에 관련 설계 문서에 근거와 함께 기록한다. 검토안이 자동으로 확정 정책이 되는 것은 아니다.

## 2. 브랜치 운영

**통합 브랜치 → 기능 브랜치 → 설계·구현·검증 → 통합 브랜치 병합 → 다음 기능 분기**를 기본 흐름으로 한다.

- 여러 기능을 묶은 작업은 통합 브랜치를 두고, 개별 기능은 최신 통합 브랜치에서 만든 `codex/<항목>-<설명>` 브랜치에서 진행한다.
- 기능의 설계 문서 변경, 코드, 필요한 테스트, 검증 기록은 같은 기능 브랜치에서 함께 관리한다.
- 다음 기능 브랜치는 앞 기능이 병합된 통합 브랜치에서 만든다. 의존성이 있는 브랜치를 처음부터 모두 만들지 않는다.
- 한 기능을 검토 가능한 작은 커밋으로 나눈다. 관련 없는 정리·리팩터링·문서 변경은 섞지 않는다. 기능 브랜치 병합 경계와 포함된 커밋을 추적할 수 있게 한다.
- 병합 전 전체 diff와 완료 조건을 확인한다. 통합 브랜치에 변경이 생겼다면 이를 반영하고 영향받은 검증을 다시 실행한다. 충돌 해결 후에는 해당 동작을 다시 확인한다.
- 병합 직후 통합 상태에서 핵심 타이머 흐름과 해당 기능을 재확인한다. 통합 브랜치를 다음 작업의 실행 가능한 출발점으로 유지한다.
- 기능 병합, `main` 반영, 원격 푸시, 배포는 서로 다른 단계다. 통합 브랜치 병합만으로 배포 완료라고 보고하지 않으며, 원격 반영과 배포는 사용자가 요청한 범위에 따른다.
- 문서만 다루는 공통 작업은 통합 브랜치의 문서 전용 커밋으로 관리할 수 있다. 기존 미커밋 문서는 변경 출처와 포함 범위를 확인한 뒤 묶고 기능 완료 기록과 혼합하지 않는다.

### 현재 TW 묶음

- 통합 브랜치는 **`codex/tw-readiness`**다.
- 현재 순서는 **TW-01 → TW-02 → TW-03 → TW-05 → TW-06 → TW-07 → TW-08**이다. TW-01–03·TW-05는 구현과 한국어 검토를 완료했다. TW-05 통합 상태는 [마감 기록](docs/CHANGELOG.md#tw-05-close)을 따르며 다음 항목 TW-06은 별도 착수한다. TW-04는 실사용 수요 확인 후 재검토하는 보류 항목으로, 이번 묶음의 선행 조건·완료 기준에서 제외한다. 상세 의존성과 사전 점검은 [구현 순서 검토](docs/tw-implementation-plan.md)를 따른다.
- 기능 브랜치 예: `codex/tw-01-preferences`, `codex/tw-02-accessibility`. 각 항목 착수 시 최신 통합 브랜치에서 생성한다.
- TW-01 때 언어 설정·태그 호환 계약을 함께 검토하고, TW-05 초반에 번체 글꼴 사전 점검을 한다. 최종 TW-07·08 통합 검증을 대체하지 않는다.
- 각 TW의 완료와 전체 묶음의 배포 준비 완료를 구분한다. 전체 완료는 TW-07·08의 필수 검증까지 확인한 뒤 판단한다.
- TW 묶음이 끝나거나 우선순위가 바뀌면 이 절과 관련 실행 계획을 함께 갱신한다.

## 3. 문서의 역할

TW마다 설계서와 개발 기록을 기계적으로 새로 만들지 않는다. 기존 문서를 먼저 갱신하고, 장기적으로 독립 관리할 필요가 있는 기능만 상세 설계를 분리한다.

| 문서 | 기록할 내용 |
|---|---|
| [ROADMAP.md](docs/ROADMAP.md) | 항목별 우선순위·범위·완료 조건·상태와 관련 문서 링크 |
| [tw-implementation-plan.md](docs/tw-implementation-plan.md) | TW 묶음의 실행 순서·의존성·검토 위험. 현재 규칙과 오래된 검토 내용을 구분 |
| [DESIGN.md](docs/DESIGN.md) | 제품 원칙, 확정 동작, 중요한 결정의 이유와 상세 설계 링크 |
| 기존 기능 설계 문서 | 기능의 상태·입력·저장 경계·호환 규칙과 상세 동작 |
| [CHANGELOG.md](docs/CHANGELOG.md) | 실제 변경 내용, 검증 결과, 남은 제약, 관련 커밋과 상세 문서 링크 |

- 차 느낌·초안·카드 변경은 [design-tea-feeling.md](docs/design-tea-feeling.md), 데이터 보존·이관은 [data-portability.md](docs/data-portability.md)를 우선 활용한다.
- 다국어처럼 여러 화면·데이터·공유 경로를 가로지르는 설계는 별도 문서로 분리할 수 있다. 파일명은 일회성 TW 번호보다 기능의 지속적인 이름을 사용하고 ROADMAP·DESIGN에서 연결한다.
- 상세 문서에는 문제·목표·비목표, 동작과 데이터 계약, 결정 이유, 호환·이관, 완료 조건을 필요한 만큼 적는다. 작은 수정에 형식만 채우는 문서를 만들지 않는다.
- 하나의 결정이나 개발 내역을 여러 파일에 전문 복제하지 않는다. 담당 문서에 상세를 두고 나머지는 요약과 링크로 연결한다.
- 설계 결정은 코드 적용 전에 기록하되, 구현 완료·검증 완료 표시는 실제 결과를 확인한 뒤 갱신한다. 문서만 변경한 것을 런타임 기능 완료로 적지 않는다.
- 독립 UI 목업·배치 미리보기는 Git 외부의 작업 전용 폴더에서 관리한다. 필요한 결정과 검증 결과만 설계 문서에 남긴다. 기존 목업을 이전할 때는 상대 경로 자산도 함께 보존하고, 앱·문서·서비스워커 참조 여부를 확인한 뒤 저장소에서 제거한다. 실제 앱 코드·제품 자산과는 구분한다.

## 4. 기능별 진행 절차

1. **범위 확인:** 로드맵 항목과 현 구현을 대조하고 선행 조건·완료 기준을 정한다. 관련 없는 후속 기능은 제외한다.
2. **분기와 설계:** 최신 통합 브랜치에서 기능 브랜치를 만들고, 필요한 동작 결정을 기존 또는 별도 기능 설계에 반영한다.
3. **구현:** 사용자 데이터와 기존 동작을 보존하면서 해당 범위를 구현한다. 버그를 발견하면 현재 항목의 완료에 필요한 수정인지 후속 항목인지 구분한다.
4. **검증과 수정:** 완료 조건을 실제 동작·데이터·산출물로 확인하고 발견한 결함을 수정한다. 구현과 똑같은 로직을 되풀이하는 테스트보다 사용자에게 중요한 경계와 실패를 검증한다.
5. **문서와 변경 검토:** 설계·구현·도움말의 불일치를 정리하고 ROADMAP·CHANGELOG를 실제 상태에 맞춘다. 전체 diff, 실행한 검증, 남은 위험을 확인한다.
6. **통합:** 완료 조건을 통과한 코드와 문서를 함께 통합 브랜치에 병합하고 통합 후 회귀를 확인한다. 필수 검증이 남았다면 완료로 표시하지 않는다.
7. **다음 항목:** 갱신된 통합 브랜치에서 다음 기능을 분기한다. 보고에는 변경 내용·검증 결과·미검증 범위·병합 상태를 간결하게 남긴다.

## 5. 공통 품질 기준

- 타이머만 사용해도 완전한 앱이어야 한다. 기록·설정·언어 작업이 진행 중 우림을 중단하거나 작성 내용을 잃게 해서는 안 된다.
- 사용자 차 이름·커스텀 태그·메모는 원문을 보존한다. 기존 ID와 저장값을 바꿀 때는 호환 읽기·이관·충돌 규칙부터 정한다.
- 저장·상태 전이 변경은 실제 기존 데이터와 경계 조건으로 검증한다. 반복 입력, 종료 직전 동작, 저장 실패 등 변경과 관련된 위험을 확인한다.
- UI 변경은 키보드·접근성 의미·포커스·44px 유효 터치 영역과 작은 화면을 확인한다. 시각 변경은 실제 렌더와 스크린샷으로 검증한다.
- 공유 변경은 DOM 미리보기뿐 아니라 실제 텍스트·PNG 산출물과 공유/다운로드 경로를 확인한다. TW 기준 화면은 375×660, PNG는 680×900이다.
- 이 앱은 빌드 없는 정적 앱이다. 오디오·Wake Lock·서비스워커는 로컬 HTTP 환경에서 검증하고, 서비스워커의 오래된 캐시와 비활성 페이지의 rAF 정지를 코드 결함과 구분한다.
- 새 런타임 자산은 추가 단계부터 오프라인 캐시 정책에 포함한다. 배포할 런타임 변경에는 필요한 SW 갱신을 함께 준비하되 문서만 바꿀 때 캐시 버전을 올리지 않는다.
- 자동화·데스크톱 미리보기 통과와 실제 모바일 브라우저·설치형 PWA 검증을 구분한다. 확인하지 못한 언어·기기·오디오·오프라인 조합은 미검증으로 기록한다.
- 검증 근거는 실행 명령과 결과, diff, fixture, 스크린샷, 원본 export 등으로 남긴다. 문서만 바꾸는 작업에는 링크·내용·diff 확인을 하고 불필요한 런타임 테스트를 추가하지 않는다.

## 6. 에이전트 협업

- 단순하고 짧은 작업은 메인 에이전트가 직접 처리한다. 복잡한 작업 중 독립적으로 분리할 수 있고 병렬화 이점이 있는 범위만 위임한다.
- 위임할 때 수정 가능 파일·비목표·입출력·완료 조건을 명시한다. 모델과 effort는 전역 위임 지침을 따른다.
- 같은 `index.html`의 상태·시트·문구를 동시에 수정하는 작업은 순차 진행을 우선한다. 병렬화는 독립 조사·번역 초안·테스트 시나리오·별도 검토에 먼저 사용한다.
- 서브에이전트의 성공 보고만으로 완료 판정하지 않는다. 메인 에이전트가 diff, 검증 로그, 실제 산출물과 통합 결과를 확인한다.
